# TASKS.md

Задачи идут в порядке выполнения. Каждая самодостаточна, проверяема и содержит ссылку на спецификацию — не нужно заново поднимать архитектуру, чтобы понять задачу.

---

## Phase 0 — Project Foundation

### Task 0.1 — Инициализация репозитория
**Цель:** создать git-репозиторий (local + remote GitHub), базовую структуру папок (`/docs`, `/src`, `/tests`).
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md`, Phase 0.
**Готово, когда:** репозиторий создан, README есть, первый коммит запушен в remote.

### Task 0.2 — Настройка технического стека
**Цель:** инициализировать Next.js + TypeScript проект, подключить Prisma, создать структуру папок согласно слоям (Data/Analysis/Knowledge/Decision/AI/UI). Сразу заложить обязательные абстракции из `CLAUDE.md` §4.1: `AIProvider`, `ObjectStorageService`, data-access слой для Postgres, заготовку под `ExternalIntegration`-адаптеры — даже пустыми, чтобы domain-код с первого дня не мог напрямую импортировать Anthropic SDK или провайдерские клиенты.
**Спецификация:** `CLAUDE.md` §3–4.1, `24_TECHNICAL_ARCHITECTURE.md`, `29_AI_LAYER.md`.
**Готово, когда:** `npm run dev` поднимает пустое приложение локально без ошибок, а структура папок физически отражает границы слоёв.

### Task 0.3 — Окружения и конфигурация
**Цель:** development и staging окружения, структура `.env`, secrets management вне git.
**Спецификация:** `30_SECURITY_PRIVACY.md` §62–63; `46_PRODUCTION_OPERATIONS_AND_RELIABILITY.md` §3–4.
**Готово, когда:** dev и staging изолированы по credentials и конфигурации.
**Статус:** repo-часть готова — `ENVIRONMENTS.md` (стратегия трёх окружений, правило изоляции), расширенный `.env.example`, `.gitignore` подтверждён. Инфраструктурная часть (реальные Vercel/Neon/Upstash/R2 ресурсы для staging/production) не создана — требует доступа Olga к внешним дашбордам, Claude Code не имеет соответствующих аккаунтов/API-ключей.

### Task 0.4 — Базовый CI
**Цель:** GitHub Actions pipeline (lint, type-check, tests, build) на каждый PR.
**Спецификация:** `46_PRODUCTION_OPERATIONS_AND_RELIABILITY.md` §7; `35_TESTING_QUALITY_CONTROL.md` §53.
**Готово, когда:** PR не мержится при падении критичных проверок.
**Важно:** при `npm install` в CI (как и локально при верификации Task 0.2) `@prisma/client`-postinstall не всегда генерирует клиент автоматически (наблюдалось из-за `allow-scripts`-политики npm). CI pipeline должен явно включать шаг `npx prisma generate` перед `type-check`/`build` — иначе сборка будет падать на отсутствии сгенерированного Prisma Client.
**Статус:** pipeline готов (`.github/workflows/ci.yml`: lint → type-check → build; шаги для тестов добавляются в Task 0.5), все шаги проверены локально. **Не сделано:** required status checks / branch protection для `main` в GitHub Settings — в этой среде нет `gh` CLI, и это repo-настройка вне scope автоматического выполнения агентом. Без этого шага PR технически может быть смёрджен при падении CI — буквальный Definition of Done («PR не мержится при падении критичных проверок») требует ручного действия Olga: Settings → Branches → Branch protection rule для `main` → Require status checks to pass → выбрать check `Lint, type-check, build`.

### Task 0.5 — Инициализация тестового фреймворка
**Цель:** настроить Vitest (unit/integration) и Playwright (E2E), по одному smoke-тесту на каждый.
**Спецификация:** `35_TESTING_QUALITY_CONTROL.md`.
**Готово, когда:** `npm test` и E2E smoke проходят локально и в CI.
**Статус:** Завершена. `vitest.config.ts` + `tests/unit/ai-service.smoke.test.ts`, `playwright.config.ts` + `tests/e2e/home.spec.ts`. `npm test` и `npm run test:e2e` проверены локально — оба проходят (1 passed каждый). CI (`.github/workflows/ci.yml`) расширен шагами `npm test` и `npx playwright install --with-deps chromium` + `npm run test:e2e` — фактический прогон в CI не подтверждён (нет открытого PR/push, требующего срабатывания workflow; будет подтверждено первым же PR/push после этого коммита).

### Task 0.6 — Проверка завершения Phase 0
**Цель:** подтвердить критерий готовности Phase 0.
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §4.
**Готово, когда:** можно запустить проект → прогнать тесты → собрать приложение → задеплоить в staging → безопасно работать с конфигурацией.
**Статус:** Завершена по всем локально проверяемым критериям — запуск проекта (Task 0.2), тесты (Task 0.5), сборка (Task 0.2/0.4), безопасная работа с конфигурацией (Task 0.3, `ENVIRONMENTS.md`). Критерий «deploy в staging» сознательно отложен — продуктовое решение Olga, см. `DECISIONS.md` D-0007. Перенесён в Backlog ниже. **Phase 0 считается завершённым.**

---

## Phase 1 — Database Foundation (следующее после Phase 0)

### Phase 1 completion — отложенный критерий «connected account»
`42_IMPLEMENTATION_ROADMAP.md` §8 (PHASE 1 COMPLETION) включает критерий «создать connected account». В 10-сущностной схеме Task 1.1 (`DECISIONS.md`, D-0008) сущности `ExternalAccount` нет — ожидаемо, не пропущено. `ExternalAccount` тесно связана с `ExternalIntegration` (`src/integrations/`), которая намеренно оставлена пустым интерфейсом до Task 3.0 (техническая проверка Meta/Instagram Graph API — permissions, token lifecycle). Проектировать таблицу connected account раньше, чем понятен реальный OAuth/token lifecycle — та же логика, что в D-0003 (Instagram/Auth boundary, YELLOW, не блокирует Phase 0/1/2). **Критерий «создать connected account» отложен до Phase 3 / Task 3.0. Не блокирует закрытие Phase 1 по остальным критериям.**

### Task 1.1 — Схема БД: core entities
**Цель:** миграции Prisma для User, Goal, Content, ContentFeature, PerformanceMetric, Pattern, Memory, Recommendation, UserDecision, Experiment.
**Спецификация:** `18_DATA_MODEL.md`, `22_DATA_MODEL.md`, `25_DATABASE_SCHEMA.md`.
**Готово, когда:** миграции применяются и откатываются чисто, связи соответствуют data model.
**Статус:** Завершена. `prisma/schema.prisma` — 10 моделей + 5 enum (см. `DECISIONS.md`, D-0008 по поводу scope и enum/String правила). Миграция `20260812142954_init_core_entities` применена и провалидирована на локальном PostgreSQL (Postgres.app): `prisma migrate dev` — применена на dev-базе `olga`; чистый rollback/reapply подтверждён через `prisma migrate reset` на отдельной одноразовой базе `project_bootstrap_migration_check` (создана и удалена только для этой проверки, база `olga` не тронута). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` — без ошибок после изменений.

### Task 1.2 — Data-access слой (repository)
**Цель:** repository-слой в `src/data/` поверх Prisma-моделей из Task 1.1, для всех 10 сущностей — базовые `create`/`findById`/`update`-функции, не более того, что реально понадобится ближайшим фазам (без delete/soft-delete, без поиска по связям — добавляются по мере необходимости).
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §5 (repository/data-access layer как часть Phase 1), `CLAUDE.md` §4.1 (единственная точка доступа к `@prisma/client`).
**Готово, когда:** по репозиторию на каждую из 10 сущностей с `create`/`findById`/`update`; smoke-тест, реально проходящий по оставшимся критериям Phase 1 completion (`42_IMPLEMENTATION_ROADMAP.md` §8): создать user → сохранить данные → получить данные обратно.
**Статус:** Завершена. `src/data/repositories/` — 10 файлов (`userRepository`, `goalRepository`, `contentRepository`, `contentFeatureRepository`, `performanceMetricRepository`, `patternRepository`, `memoryRepository`, `recommendationRepository`, `userDecisionRepository`, `experimentRepository`), реэкспорт через `src/data/index.ts`. Интеграционный smoke-тест `tests/integration/user-data-flow.smoke.test.ts` (новая папка `tests/integration/`, добавлена в `vitest.config.ts`): создаёт user → создаёт связанный goal → читает оба обратно → проверяет данные → убирает за собой (`afterAll` + `onDelete: Cascade`). CI (`.github/workflows/ci.yml`) получил `postgres:16` service-контейнер + шаг `prisma migrate deploy` перед тестами — интеграционный тест теперь реально прогоняется и в CI, не только локально. Проверено локально: `npm test` (2/2 passed), `npm run test:e2e` (1/1 passed), `npx tsc --noEmit`, `npm run lint`, `npm run build` — без ошибок; после прогона в БД `olga` не осталось тестовых строк (проверено `psql`).

**Phase 1 — Database Foundation считается завершённой** по всем критериям `42_IMPLEMENTATION_ROADMAP.md` §8, кроме «connected account» (сознательно отложен до Phase 3/Task 3.0 — см. выше).

*(остальные задачи Phase 1+ добавляются по мере продвижения — весь backlog заранее не расписывается, чтобы не рассинхронизироваться с реальностью)*

---

## Phase 2 — Authentication (следующее после Phase 1)

### Task 2.1 — Auth.js: техническая основа (schema + конфигурация)
**Цель:** подключить Auth.js для **App Authentication** (не Instagram Integration — это принципиально разные слои, CLAUDE.md §3.2/§4.1). Расширить `prisma/schema.prisma` под Auth.js (`Account`, `Session`, `VerificationToken` + auth-поля на существующей модели `User` — email, password hash и т.п., в зависимости от выбранного механизма). Настроить базовый Auth.js route handler и session strategy с Prisma adapter. **Не входит в эту задачу:** UI/API для registration/login/logout, account recovery, authorization/roles — отдельные последующие задачи, не расписываются заранее.
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §9–11 (Phase 2: registration/login/logout/session management/password requirements/account recovery; Phase 2 completion criterion — «создать account → войти → получить доступ только к своим данным»); `CLAUDE.md` §3.2/§4/§4.1 (App Authentication ≠ Instagram Integration, Auth.js — выбранный инструмент); `30_SECURITY_PRIVACY.md` §6–9 (authentication mechanisms, passwords, session security, authorization).
**Готово, когда:** миграция под auth-таблицы применяется и откатывается чисто (как в Task 1.1); Auth.js может создать и прочитать сессию для тестового пользователя (integration smoke-тест по аналогии с Task 1.2); конкретный auth-механизм для MVP выбран и явно задокументирован как решение в `DECISIONS.md` — `30_SECURITY_PRIVACY.md` §6 прямо оставляет этот выбор («OAuth; token-based authentication; passwordless authentication... Конкретный механизм определяется на этапе реализации»).
**Статус:** Завершено. `prisma/schema.prisma` расширен под Auth.js (`Account`/`Session`/`VerificationToken` + `email`/`emailVerified`/`name`/`image`/`passwordHash` на `User`), миграция `20260812163141_auth_js_foundation` применена на dev-базе `olga` и проверена на чистое применение полной истории миграций на одноразовой базе (аналогично Task 1.1). Механизм — **Credentials (email+password)**, выбран специально чтобы не создавать RED-зависимость от внешнего email-сервиса (см. `DECISIONS.md`, D-0009, по прямому ограничению Olga). `src/auth/` — `config.ts` (`NextAuth()` + `PrismaAdapter`, JWT session strategy), `credentials.ts` (`authenticateWithCredentials`, вынесена из provider'а для прямой тестируемости), `password.ts` (`bcryptjs`), `types.d.ts` (module augmentation `session.user.id`), route handler `src/app/api/auth/[...nextauth]/route.ts`. Integration smoke-тест (`tests/integration/auth-credentials.smoke.test.ts`) проверяет `authenticateWithCredentials` против живой БД: верный пароль → пользователь, неверный пароль → null, неизвестный email → null; корректно чистит за собой. **Не проверялось** (за пределами scope Task 2.1): полный HTTP sign-in flow — issuance/чтение cookie-based JWT-сессии через реальный `/api/auth/*` endpoint в браузере; это относится к следующим задачам Phase 2 (login UI/API). `npx tsc --noEmit`, `npm run lint`, `npm run build` (включая регистрацию `/api/auth/[...nextauth]` как dynamic route), `npm test` (3/3), `npm run test:e2e` (1/1) — все прошли локально; CI (`.github/workflows/ci.yml`) дополнен `AUTH_SECRET` (CI-only, не реальный секрет — нужен, т.к. `NextAuth()` конструируется на этапе загрузки модуля).

### Task 2.2 — Registration / Login / Logout (UI + API) + верифицированный HTTP session cycle
**Цель:** поверх технической основы Task 2.1 (`src/auth/`) реализовать практический вход в приложение: страницы/формы registration и login, действие logout, API-роуты для них, и один минимальный protected route (например `/api/me`), чтобы подтвердить критерий Phase 2 completion (`42_IMPLEMENTATION_ROADMAP.md` §11: «создать account → войти → получить доступ только к своим данным»). **Обязательно закрыть пробел, явно оставленный в Task 2.1**: e2e-тест (Playwright) должен пройти полный HTTP-цикл через реальный браузер — регистрация → login → issuance cookie-based JWT-сессии через реальный `/api/auth/*` endpoint → доступ к protected route → logout → сессия инвалидирована, protected route снова недоступен. Не просто unit/integration-уровень `authenticateWithCredentials` (это уже сделано в Task 2.1) — именно реальный HTTP round-trip.
**Обязательные требования по ходу реализации** (не «потом»): input validation на registration/login endpoints (формат email, минимальная длина/сложность пароля — `CLAUDE.md` §6, §7 `30_SECURITY_PRIVACY.md`); понятная обработка попытки регистрации на уже занятый email (без утечки существования/несуществования аккаунта сверх необходимого); logout корректно инвалидирует сессию (`30_SECURITY_PRIVACY.md` §8); базовая user isolation — protected route у двух разных зарегистрированных пользователей возвращает каждому только его собственные данные, e2e-тестом подтверждается на двух созданных аккаунтах, не на одном (`CLAUDE.md` §6, §7; `30_SECURITY_PRIVACY.md` §5, §10).
**Явно не входит в эту задачу** (следующие отдельные задачи, не расписываются заранее): account recovery / password reset — обычно требует email-сервиса, потенциальная новая RED-точка по той же логике, что и в D-0009; authorization/roles/admin permissions (`42_IMPLEMENTATION_ROADMAP.md` §10 — team accounts, будущее); rate limiting на auth endpoints — `CLAUDE.md` §6 требует его для публичных endpoints как часть MVP, не «потом», но stack-решение для этого — BullMQ/Redis (D-0001), а реальный Redis/Upstash сознательно отложен (`DECISIONS.md` D-0007); это осознанный, явно зафиксированный здесь gap, не тихий пропуск — закрывается либо при появлении Redis-инфраструктуры, либо отдельным YELLOW-решением до этого момента.
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §9, §11; `CLAUDE.md` §3.2/§4.1/§6/§7; `30_SECURITY_PRIVACY.md` §5–8.
**Готово, когда:** e2e-тест (Playwright) проходит полный HTTP-цикл, описанный выше, для минимум двух независимых пользователей и подтверждает user isolation; registration/login отклоняют невалидный ввод (пустой/некорректный email, слишком короткий пароль) с понятным ответом, не 500-ошибкой; повторная регистрация на тот же email не создаёт дубликат и не роняет сервер; `npm run test:e2e`, `npx tsc --noEmit`, `npm run lint`, `npm run build` проходят локально и в CI.
**Статус:** Завершено. `src/auth/register.ts` — `registerUser(email, password)`: валидация email/пароля (мин. 8 символов), проверка занятого email, хеширование (`bcryptjs`, переиспользован из Task 2.1), создание через `userRepository`. `src/app/api/register/route.ts` — 201/400/409 по типу ошибки. `src/app/api/me/route.ts` — единственный protected route: 401 без сессии, иначе перечитывает пользователя из БД по `session.user.id` (не доверяет claims напрямую, resource ownership). `src/app/login/page.tsx`, `src/app/register/page.tsx` — клиентские формы на `signIn('credentials', ...)` / `fetch('/api/register')`. `src/app/_components/LogoutButton.tsx` — `signOut({ callbackUrl: '/' })`. `src/app/page.tsx` — теперь async server component, показывает logged-in/logged-out состояние через `auth()`. `src/auth/config.ts` дополнен `pages: { signIn: '/login' }`. **Пробел из Task 2.1 закрыт**: `tests/e2e/auth-flow.spec.ts` (5 тестов, реальный браузер через Playwright) — register issues session → `/api/me` 200 → logout → `/api/me` 401; login с верным паролем работает; login с неверным паролем не создаёт сессию (`/api/me` остаётся 401); два независимо залогиненных пользователя (два `browser.newContext()`) видят через `/api/me` только свои собственные данные (user isolation). Один race condition найден и исправлен в самом e2e-тесте по ходу работы (logout — асинхронная навигация, тест не дожидался её завершения перед переходом на `/login`, из-за чего старая cookie иногда переживала переход) — не баг приложения, задокументировано как урок в TASKS.md. `tests/integration/auth-registration.smoke.test.ts` — `registerUser` против живой БД: хеширование пароля (не plaintext в БД), отклонение дубля email/невалидного email/короткого пароля. Rate limiting на auth endpoints сознательно не реализован в этой задаче (блокер — Redis не поднят, см. `DECISIONS.md` D-0007) — не тихий пропуск, см. выше. Account recovery/authorization/roles — не в этой задаче, следующие шаги Phase 2. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (4 файла/7 тестов), `npm run test:e2e` (5/5) — все прошли; dev-БД `olga` проверена — 0 тестовых пользователей после прогонов.

### Task 2.3 — Authorization foundation: переиспользуемый ownership-check + первый защищённый ресурс за пределами User
**Цель:** реализовать `30_SECURITY_PRIVACY.md` §9–10 (authorization + resource ownership) как переиспользуемый паттерн, а не одноразовый код внутри `/api/me`. Вынести получение user id из сессии в общий хелпер в `src/auth/`, использовать его в `/api/me` (рефакторинг существующего кода) и в новом ownership-protected endpoint для сущности, отличной от `User`. Для этого шага выбрана `Goal` — простейшая существующая сущность с полем `userId` (`prisma/schema.prisma`, `src/data/repositories/goalRepository.ts`) — чтобы доказать, что паттерн обобщается на произвольный ресурс, не жёстко прибит к одному route.
**Явно не входит в эту задачу** (сознательно откладывается, не расписывается заранее): role model (owner/admin/editor/viewer) — `30_SECURITY_PRIVACY.md` §11 прямо говорит «для MVP достаточно user-level authorization», team accounts не в scope продукта на этом этапе; service permissions (машина-к-машине, авторизация BullMQ-воркеров) — фоновых задач ещё нет, нечего авторизовывать; password reset / account recovery — сознательно отложено ещё в D-0009, к этому вернёмся отдельно, чтобы не упереться в RED-точку с email-сервисом раньше времени; полный CRUD для `Goal` (list/delete) — только то, что нужно для проверки ownership-механизма (create + get by id), по тому же принципу минимализма repository-слоя, что и в Task 1.2/2.1 (`не более того, что реально понадобится ближайшим фазам`).
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §10 (из перечисленных там user permissions/service permissions/administrative roles — в этой задаче только user permissions); `30_SECURITY_PRIVACY.md` §9–11 (authorization, resource ownership — пример из документа буквально «content_id сам по себе не является разрешением на получение content»; role model — явно вне MVP-scope).
**Готово, когда:** общий хелпер (например `requireSessionUserId()`) в `src/auth/` используется и в `/api/me`, и в новом endpoint; `POST /api/goals` создаёт `Goal` с `userId` из сессии — тело запроса не может указать чужой `userId` (сам этот факт — часть authorization control, не отдельная проверка); `GET /api/goals/:id` возвращает goal только если `goal.userId === session.user.id`; если goal принадлежит другому пользователю ИЛИ не существует — единообразный `404` (не `403`), чтобы не раскрывать факт существования чужого ресурса (`30_SECURITY_PRIVACY.md` §10); тест подтверждает: пользователь A создаёт goal → A может его прочитать → пользователь B (другая сессия) получает `404` на тот же `goal_id`, не доступ к данным и не намёк на существование; `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test`/`npm run test:e2e` проходят локально и в CI.
**Статус:** Завершено. `src/auth/session.ts` — `requireSessionUserId()`, единая точка получения user id из сессии; `src/app/api/me/route.ts` отрефакторен на её использование (без изменения поведения). `src/app/api/goals/route.ts` (`POST` — создаёт `Goal` с `userId` строго из сессии, тело запроса не может указать чужой `userId`) и `src/app/api/goals/[id]/route.ts` (`GET` — `404` единообразно и для несуществующего, и для чужого goal, чтобы не раскрывать факт существования). `tests/e2e/helpers/auth.ts` — общий `registerAndLogin`/`uniqueEmail`-хелпер, вынесен из `auth-flow.spec.ts` (Task 2.2) для переиспользования, без дублирования между спеками. `tests/e2e/goal-authorization.spec.ts` (3 теста, реальный браузер) подтверждает: неаутентифицированный запрос к `/api/goals` — `401`; владелец создаёт и читает свой goal; второй залогиненный пользователь получает `404` и на чужой goal, и на несуществующий id — коды ответа неотличимы (доказательство, что ownership-check не палит существование). Role model, service permissions, password reset — сознательно не в этой задаче (см. выше). `npx tsc --noEmit`, `npm run lint`, `npm run build` (роуты `/api/goals`, `/api/goals/[id]` зарегистрированы), `npm test` (4 файла/7 тестов, без изменений — authorization проверена только e2e), `npm run test:e2e` (8/8) — все прошли; dev-БД `olga` — 0 пользователей и 0 goals после прогонов.

**Phase 2 — Authentication считается завершённой** по критерию `42_IMPLEMENTATION_ROADMAP.md` §11 («создать account → войти → получить доступ только к своим данным») — подтверждено e2e-тестами через реальный браузер в Task 2.2 и Task 2.3, включая ownership-check на ресурсе за пределами `User`. Решение Olga (12 августа 2026): закрыть Phase 2 на текущем объёме. Account recovery/password reset и role model (owner/admin/editor/viewer) сознательно остаются вне MVP-scope — не забыты, а не входят в критерий готовности фазы (`30_SECURITY_PRIVACY.md` §11 сам оставляет role model до появления team accounts; password reset — потенциальная RED-точка с email-сервисом, см. D-0009). Возвращаются как отдельные задачи, если/когда появится продуктовая необходимость — не расписываются заранее.

---

## Phase 3 — предварительное условие (зафиксировано по итогам review, D-0003)

### Task 3.0 — Техническая проверка Instagram/Meta Graph API
**Цель:** проверить актуальные требования Meta/Instagram Graph API перед реализацией OAuth-интеграции: необходимые permissions, business/creator account requirements, token lifecycle (issuance, refresh, expiry, revocation), доступность нужных метрик через API, rate limits, webhook-поддержку.
**Спецификация:** `CLAUDE.md` §4.1 (Instagram/Auth boundary), `08_METRICS_FRAMEWORK.md`, `26_DATA_PIPELINE.md`.
**Готово, когда:** есть письменное резюме — какие permissions запрашивать, какие данные реально доступны, как устроен token lifecycle. Только после этого начинается реализация Integration Service.
**Статус:** Завершено (research-only). Письменное резюме — [`INSTAGRAM_API_REVIEW.md`](INSTAGRAM_API_REVIEW.md) (корень репозитория). Ключевые находки: (1) современный путь подключения — **Business Login for Instagram**, Facebook Page не требуется (в отличие от старого Facebook Login flow); (2) вероятно достаточно одного scope — `instagram_business_basic`, остальные (content_publish/manage_messages/manage_comments) не нужны для MVP-цели (аналитика, не постинг/messaging); (3) token lifecycle: short-lived (~1ч) → long-lived (60д) → refresh (не раньше 24ч, не позже истечения, снова 60д) — официальная документация не описывает явно сценарий полного истечения без refresh, спроектировано с расчётом на graceful reconnect-flow; (4) метрика `impressions` **deprecated с апреля 2025**, заменена на `views` — это меняет маппинг метрик из `08_METRICS_FRAMEWORK.md` §2 на этапе реализации Data layer, не сейчас; (5) история метрик у Instagram хранится **90 дней** — подтверждает, не расширяет, цель «~3 месяца» из `08_METRICS_FRAMEWORK.md` §12; (6) rate limiting — per-account (4800 × охват за 24ч), не глобальный лимит на приложение; (7) webhooks не покрывают performance-метрики (только comments/mentions/story-insights) — подтверждает, что polling/incremental sync (уже заложенный в `26_DATA_PIPELINE.md`) остаётся правильной архитектурой; (8) **организационное следствие**: реальный запуск для сторонних пользователей потребует Meta App Review + Business Verification (Advanced Access), не только Standard Access, доступный для разработки — стоит учитывать в планировании таймлайна MVP launch, не только в архитектуре. Явные пробелы в исследовании (endpoint revocation, точная scope-to-endpoint матрица) зафиксированы честно в файле, не выданы за проверенный факт. Meta developer-аккаунт/приложение **не создавались** — RED, ждёт Olga. Не блокирует Phase 3 планирование дальше.
**Уточнение (12 августа 2026):** Olga лично прошла реальную авторизацию Instagram (существующий flow, не создание нового Meta developer-аккаунта/приложения) и подтвердила фактический набор permissions — это исправило вывод исходной версии резюме. Разрешены: `instagram_business_basic` (обязательный) + **отдельное insights-разрешение** (на консент-экране показано отдельно от `instagram_business_basic`, не частью его, как предполагалось изначально). Осознанно отключены: comments/messages/content-publish. `INSTAGRAM_API_REVIEW.md` §3 обновлён — исправленный вывод для MVP и статус связанного gap в §8 (точное каноническое имя scope для insights в Business Login остаётся открытым вопросом, но сам факт отдельного разрешения подтверждён первичным источником — Olga, выше документации по `CLAUDE.md` §2).

### Task 3.1 — Integration Service: контракт `IntegrationProvider` + `IntegrationService` (design)
**Цель:** спроектировать общий интерфейс `ExternalIntegration`-адаптеров (`CLAUDE.md` §4.1) на основе подтверждённых Task 3.0 деталей — контракт `IntegrationProvider` (что обязан уметь каждый платформенный адаптер) и domain-facing фасад `IntegrationService`, по аналогии с `AIProvider`/`AIService` (`src/ai/`). Именно дизайн интерфейса — не реализация конкретного Instagram-адаптера (это следующий шаг) и не создание Meta developer-аккаунта (RED).
**Спецификация:** `CLAUDE.md` §3.2/§4.1; `INSTAGRAM_API_REVIEW.md` (Task 3.0 + уточнение permissions); `08_METRICS_FRAMEWORK.md` §11, §16 (data completeness distinction, multi-platform future).
**Готово, когда:** `IntegrationProvider` покрывает подтверждённый жизненный цикл (authorize URL → code exchange → refresh → account/media insights → best-effort disconnect) с типами, отражающими реальные находки Task 3.0 (terminal-state ошибка для отозванного токена отдельно от rate-limit ошибки; различие "метрика не пришла" vs "не запрашивалась"); `IntegrationService` не завязан на конкретные провайдеры (registry, не hardcoded); `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят без регрессий в уже существующих модулях.
**Статус:** Завершено. [`src/integrations/IntegrationProvider.ts`](src/integrations/IntegrationProvider.ts) — контракт: `getAuthorizationUrl`/`exchangeCodeForTokens`/`refreshTokens`/`getAccountInsights`/`listRecentMedia`/`getMediaInsights`/опциональный `disconnect`; `IntegrationAuthError` (терминальное состояние — токен отозван/невалиден, не ретраится) и `IntegrationRateLimitError` (ретраится с backoff) как разные типы ошибок — прямое следствие того, что нашлось в `INSTAGRAM_API_REVIEW.md` §4/§6; `IntegrationInsightsResult.unavailableMetrics` — прямая реализация правила из `08_METRICS_FRAMEWORK.md` §11 (data completeness). [`src/integrations/IntegrationService.ts`](src/integrations/IntegrationService.ts) — фасад на registry (`registerIntegrationProvider`), не hardcoded provider (в отличие от `AIService`) — осознанно, т.к. платформ будет больше одной и конкретных провайдеров пока нет. Старый placeholder `ExternalIntegration.ts` удалён (не использовался нигде в коде, только упоминался в README/докладах) — заменён на `IntegrationProvider`/`IntegrationService`, терминология `CLAUDE.md` §4.1 ("ExternalIntegration-адаптеры") не переписана, только её код-реализация названа по аналогии с `AIProvider`/`AIService`. `src/integrations/README.md`, `instagram/README.md`, `src/README.md` обновлены. **Не входит в эту задачу и не сделано:** конкретный `providers/instagram.ts` (реальные HTTP-вызовы к `graph.instagram.com`/`api.instagram.com`) — следующий шаг, будет типизирован по документированным endpoints, но не может быть end-to-end протестирован без реального Meta-приложения (RED, ждёт Olga); `ExternalAccount` Prisma-модель для хранения токенов — Data-layer задача, тоже следующий шаг, не эта задача. `npx tsc --noEmit`, `npm run lint`, `npm test` (4 файла/7 тестов, без изменений), `npm run build` — без регрессий, новый модуль не подключён ни к одному route.

### Task 3.2 — Instagram-адаптер: `providers/instagram.ts` против `IntegrationProvider` + живая проверка через реальный API
**Цель:** реализовать `src/integrations/providers/instagram.ts` — конкретную реализацию `IntegrationProvider` (Task 3.1) для Instagram Business Login, против документированных endpoints из `INSTAGRAM_API_REVIEW.md`. **Отличие от исходного плана**: у Olga уже есть работающее Meta-приложение (Business/Instagram Login), подтверждённый тестировщик и вручную сгенерированный тестовый access token — значит минимум один реальный запрос (например, `getAccountInsights`) можно и нужно проверить end-to-end против настоящего Instagram API, не только типизировать вслепую. App ID/Secret и тестовый токен Olga добавляет в реальный `.env` сама — они не создаются и не запрашиваются этой задачей.
**Спецификация:** `src/integrations/IntegrationProvider.ts` (контракт, Task 3.1); `INSTAGRAM_API_REVIEW.md` §2–6 (OAuth flow, permissions, token lifecycle, метрики, rate limits) — endpoints и формы ответов брать оттуда, не по памяти.
**Что реально можно проверить живым запросом, а что нет:**
- `getAuthorizationUrl` — чистая функция (URL builder), без сетевого вызова — покрывается обычным unit-тестом, сеть не нужна.
- `refreshTokens` и `getAccountInsights` (и, если легко получится, `listRecentMedia`/`getMediaInsights`) — **можно проверить по-настоящему**: у Olga уже есть валидный long-lived токен (сгенерирован вручную, не через наш собственный `exchangeCodeForTokens`), значит эти вызовы не требуют полного browser-driven OAuth round trip, только сам токен.
- `exchangeCodeForTokens` — **нельзя** проверить в этой задаче: для этого нужен свежий `code` от реального Instagram consent screen через настоящий редирект (браузер + зарегистрированный `redirect_uri` в Meta App Dashboard) — это отдельный, более сложный сценарий (аналог того, как для Auth.js в Task 2.2 полный HTTP sign-in cycle тестировался через реальный браузер) — реализовать метод по документации можно и нужно, но e2e-проверку самого code exchange стоит вынести в отдельную последующую задачу, когда будет смысл подключать реальный redirect endpoint в приложении.
**Готово, когда:** `providers/instagram.ts` реализует все обязательные методы `IntegrationProvider`, ошибки платформы (401/403 → `IntegrationAuthError`, rate limit/80002 → `IntegrationRateLimitError`) замаплены корректно; `getAuthorizationUrl` покрыт unit-тестом; интеграционный тест делает реальный вызов (минимум `getAccountInsights`) с тестовым токеном Olga из `.env` и проверяет, что пришли ожидаемые по форме данные (метрики вроде `views`/`reach`, не устаревший `impressions`) — тест **пропускается**, если нужные env-переменные не заданы (значит не требует Instagram-секретов в CI/GitHub Actions — сознательный выбор, не тихий пропуск, фиксируется явно в отчёте); `.env.example` дополнен плейсхолдерами (`INSTAGRAM_APP_ID`, `INSTAGRAM_APP_SECRET`, тестовый токен для локальной проверки — с пометкой, что он не для продакшена); `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят.
**Явно не входит в эту задачу:** e2e-проверка полного OAuth code exchange через реальный браузер (следующий шаг); wiring `IntegrationService` в реальные app routes, чтобы конечный пользователь мог подключить свой Instagram из UI (тоже следующий шаг — потребует `ExternalAccount` Data-layer сущность); добавление Instagram-секретов в CI — сознательно не делается в этой задаче.
**Уточнение Olga перед стартом:** токен, полученный через кнопку "Generate Token" в Meta App Dashboard, может быть как short-lived (~1ч), так и уже long-lived — сама документация Meta это не проясняет однозначно (`INSTAGRAM_API_REVIEW.md` §8). Решено проверить это эмпирически, не полагаться на предположение — см. ниже, как это сделано в `exchangeForLongLivedToken`.
**Статус:** Завершено, включая живую проверку с реальными credentials Olga. [`src/integrations/providers/instagram.ts`](src/integrations/providers/instagram.ts) реализует все обязательные методы `IntegrationProvider` против документированных endpoints (`api.instagram.com`/`graph.instagram.com`); `parseGraphResponse` маппит 401/403 → `IntegrationAuthError`, код `80002`/HTTP 429 → `IntegrationRateLimitError`, остальное — обычная `Error`. **Найдено по ходу реализации (не было известно на Task 3.0/3.1)**: большинство account-level метрик, кроме `reach`, требуют явный `metric_type=total_value` в запросе — добавлено опциональное поле `metricType` в `IntegrationAccountInsightsParams` (`IntegrationProvider.ts`), обратно совместимо (optional). `getAuthorizationUrl` покрыт unit-тестом ([`tests/unit/instagram-authorize-url.test.ts`](tests/unit/instagram-authorize-url.test.ts), 2 теста, без сети) — проверяет корректный URL и permissions (`instagram_business_basic` + insights, без comments/messages/content-publish). `.env.example` дополнен (`INSTAGRAM_APP_ID`, `INSTAGRAM_APP_SECRET`, `INSTAGRAM_API_VERSION`, `INSTAGRAM_TEST_ACCESS_TOKEN`). Старая пустая папка `src/integrations/instagram/` удалена — реальный адаптер лежит в `providers/instagram.ts`, как `providers/anthropic.ts`/`providers/r2.ts`, не в отдельной per-platform папке.

**Второй найденный по ходу баг (не связан с Instagram напрямую, но обнаружен через этот тест)**: `vitest.config.ts` не грузил `.env` в `process.env` вообще — `DATABASE_URL` у прежних Prisma-тестов «работал» только случайно, как побочный эффект того, что `@prisma/client` сам грузит `.env` при первом импорте (и заодно на весь процесс). Тест на Instagram не импортирует Prisma, поэтому без явной загрузки `INSTAGRAM_*`-переменные были не видны вообще — при первом запуске живой тест корректно **пропустился**, хотя Olga уже заполнила `.env`. Исправлено: `vitest.config.ts` теперь явно грузит `.env` через `loadEnv` из `vite` (`test.env`) — общий фикс, не костыль только под Instagram, полезен для любых будущих кастомных переменных.

**Результат живой проверки (Olga вписала реальные `INSTAGRAM_APP_ID`/`INSTAGRAM_APP_SECRET`/`INSTAGRAM_TEST_ACCESS_TOKEN`, тест прогнан после фикса выше)**: `getAccountInsights({ metrics: ['reach'], period: 'day' })` **успешно вернул реальные данные** с настоящего Instagram-аккаунта Olga — тест прошёл (`✓`). Ответ на исходный вопрос Olga (short-lived vs long-lived у токена из "Generate Token"): попытка `exchangeForLongLivedToken` (grant_type=ig_exchange_token) вернула ошибку **`Session key invalid. This could be because the session key has an incorrect format, or because the user has revoked this session`** — тест не проглотил это молча (`console.warn`), продолжил с исходным токеном напрямую, и именно он оказался рабочим для реального insights-запроса. Точная причина этой конкретной ошибки экспериментально не подтверждена (не факт, а осторожная интерпретация): наиболее вероятно, что токены, выданные через кнопку "Generate Token" в Meta App Dashboard, устроены иначе, чем токен, полученный через штатный OAuth code exchange — либо уже не нуждаются/не подходят под `ig_exchange_token`, либо API не считает такой session key валидным форматом для этой конкретной операции. Что подтверждено твёрдо: токен рабочий и валидный для реальных API-запросов прямо сейчас; проверить, действительно ли он "long-lived" (60 дней) в строгом смысле, по-прежнему нельзя без ожидания ≥24ч и последующего успешного `refreshTokens` — остаётся открытым вопросом, не решаемым в рамках Task 3.2. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (6/6 файлов, 10/10 тестов включая живой Instagram-тест) — без регрессий; dev-БД `olga` — 0 users/0 goals после прогонов.

**Повторная проверка (13 августа 2026, Olga сгенерировала свежий токен специально, чтобы исключить просроченность)**: та же ошибка `Session key invalid` на попытке `exchangeForLongLivedToken`, воспроизведена один-в-один на заведомо свежем токене — это отсекает версию «токен просто успел протухнуть» и усиливает гипотезу, что дело в самом типе токена из "Generate Token", а не в его возрасте. `getAccountInsights` снова успешно отработал с исходным токеном напрямую, вернул реальные данные аккаунта (`reach` за два дня, конкретные значения — в консоли у Olga, не дублируются в TASKS.md намеренно, т.к. это персональные данные аккаунта, не техническая деталь). Добавлено логирование полного результата `getAccountInsights` в сам тест (`console.log`) — не только факт прохождения теста, а видимые в консоли реальные данные, по прямому запросу Olga.

**Перепроверка реализации по запросу Olga (13 августа 2026, три конкретных пункта)**: метод (`GET`, не `POST`), хост (`graph.instagram.com`, не `graph.facebook.com`) и `grant_type` (`ig_exchange_token`, не `fb_exchange_token`) — все три уже были корректны, не баг. Но целенаправленная перепроверка вскрыла **другое реальное расхождение**: `exchangeForLongLivedToken`/`refreshTokens` строили URL через версионированный `GRAPH_BASE` (`https://graph.instagram.com/v25.0/access_token`), тогда как сам `INSTAGRAM_API_REVIEW.md` §2/§4 документирует эти два endpoint'а **без версии в пути** (`https://graph.instagram.com/access_token`, `/refresh_access_token`) — в отличие от версионированных data-endpoints (`/v25.0/me`, `/v25.0/{id}/insights`). Исправлено — оба теперь используют отдельные неверсионированные константы. **Перезапуск с тем же токеном из `.env` (без повторной генерации) дал тот же результат**: `exchangeForLongLivedToken` по-прежнему `Session key invalid`, `getAccountInsights` по-прежнему успешен с исходным токеном. Значит версионирование пути не было причиной ошибки — фикс корректен и оставлен (соответствует документации), но точная причина `Session key invalid` остаётся неустановленной. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (6/6, 10/10) — без регрессий.

**Диагностика полного raw-ответа (по запросу Olga) и вывод**: полный JSON-ответ endpoint'а обмена показал `type: "OAuthException"`, `code: 452`, `error_subcode: 2207055`. Подтверждено независимым поиском: `code 452` — легаси-код `API_EC_SESSION_INVALID` из дореформенного Facebook REST API, буквально то, что заподозрила Olga по формулировке ошибки. Договорились не гадать дальше по конкретному subcode, а закрыть вопрос напрямую — реальным browser OAuth round trip (это и так был следующий по плану шаг).

**Реальный browser OAuth round trip выполнен и прошёл чисто (13 августа 2026)**: добавлены [`src/integrations/bootstrap.ts`](src/integrations/bootstrap.ts) (регистрирует `createInstagramProvider()` в `IntegrationService` — side-effect import, registry-паттерн Task 3.1) и [`src/app/api/integrations/instagram/callback/route.ts`](src/app/api/integrations/instagram/callback/route.ts) (диагностический callback: читает `code`/`state`/`error` из редиректа, вызывает `IntegrationService.exchangeCodeForTokens`, показывает сырой результат — не production connect-flow, persistence ещё нет). Redirect URI потребовал `https://` — Meta Dashboard отказалась сохранять `http://localhost` (ngrok как альтернатива отклонён: требует создания нового внешнего аккаунта, тот же RED, что Meta developer account, — вместо этого использован встроенный `next dev --experimental-https`, локально доверенный сертификат через mkcert, без внешних сервисов). Olga лично прошла flow: `getAuthorizationUrl` → согласие на реальном консент-экране → `code` → `exchangeCodeForTokens` — **успех с первой попытки**, `{"success":true,"accessTokenPreview":"IGAAWDRqSeHW...","obtainedAt":"2026-08-13T07:57:12.954Z","expiresAt":"2026-10-11T21:41:07.954Z"}` — токен живёт ~60 дней, как и документировано.

**Вывод (закрывает открытый вопрос из §4.1 `INSTAGRAM_API_REVIEW.md`)**: `Session key invalid` — не баг `exchangeForLongLivedToken`/`refreshTokens` (оба метода подтверждены рабочими — именно они обработали успешный обмен в реальном flow) и не общее свойство API. Причина — токены из ручной кнопки "Generate Token" в Meta App Dashboard имеют иную природу на стороне Meta и не предназначены для повторного обмена через `ig_exchange_token`. Реальные пользователи продукта всегда идут через настоящий `authorization_code` flow — эта ошибка в продакшене не встретится.

**Что живым запросом подтверждено, а что нет (честно, на конец Task 3.2)**: подтверждено — `getAuthorizationUrl` (unit-тестом), `exchangeCodeForTokens` (реальный browser round trip), `getAccountInsights` (дважды, реальные данные аккаунта). Не подтверждено живым запросом — `refreshTokens` (нужен токен ≥24ч), `listRecentMedia`/`getMediaInsights`, `disconnect` (метод не реализован, endpoint неизвестен). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (6/6, 10/10) — без регрессий. `.gitignore` дополнен (`certificates` — приватный ключ mkcert, сгенерированный локально при `next dev --experimental-https`, не коммитится).

### Task 3.3 — `ExternalAccount`: data layer для хранения подключения Instagram
**Цель:** реализовать Prisma-модель `ExternalAccount` (`25_DATABASE_SCHEMA.md` §7 EXTERNAL_ACCOUNTS) для хранения факта подключения внешней платформы к пользователю приложения, repository-слой (`create`/`findByUserId`/`update`, по установленному паттерну Task 1.2), и переписать диагностический callback route Task 3.2 так, чтобы он реально сохранял результат OAuth-обмена, а не только показывал его.
**Спецификация:** `25_DATABASE_SCHEMA.md` §7 (EXTERNAL_ACCOUNTS — точные поля: `external_account_id`/`user_id`/`platform`/`external_user_id`/`status`/`connected_at`/`last_synced_at`), §53 (unique constraint `platform` + `external_user_id`), §70 (FUTURE_MULTI_PLATFORM — схема не должна предполагать одну платформу на пользователя навсегда); `30_SECURITY_PRIVACY.md` §22–24 (credentials внешних платформ — отдельное хранение, encryption at rest, token rotation).
**Готово, когда:** модель добавлена (поля §7 + `accessToken`/`tokenExpiresAt` сверх §7, практически необходимые для самой интеграции), миграция применяется и полная история переприменяется чисто на одноразовой базе (как в Task 1.1/2.1); repository покрывает `create`/`findByUserId`/`update`; callback route сохраняет `ExternalAccount` через repository вместо простого показа raw JSON; security-gap (encryption at rest, `30_SECURITY_PRIVACY.md` §23) явно зафиксирован в `DECISIONS.md`, не тихо пропущен; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят.
**Статус:** Завершено. [`prisma/schema.prisma`](prisma/schema.prisma) — модель `ExternalAccount` с полями `userId`/`platform`/`externalUserId`/`status` (`ExternalAccountStatus` enum: `connected`/`expired`/`disconnected` — закрытый набор состояний, `30_SECURITY_PRIVACY.md` §25 явно требует отдельный статус disconnected при отзыве, не молчаливое удаление)/`connectedAt`/`lastSyncedAt` — буквально из §7; `accessToken`/`tokenExpiresAt` — сверх §7, практически необходимы. `platform` — `String`, не enum (открытая категория, §70 + `08_METRICS_FRAMEWORK.md` §16 про TikTok в будущем). Unique constraint `[platform, externalUserId]` (§53). Миграция `20260813082115_external_accounts` применена на dev-базе `olga` и проверена на чистое переприменение полной истории на одноразовой базе (та же процедура, что Task 1.1/2.1). **Security-gap зафиксирован явно** — [`DECISIONS.md` D-0010](DECISIONS.md): `accessToken` хранится без encryption at rest (`30_SECURITY_PRIVACY.md` §23 не выполнен) — сознательно, т.к. secret manager/KMS отложены вместе с остальным cloud-стеком (D-0007), с явной зарубкой «закрыть до production». [`src/data/repositories/externalAccountRepository.ts`](src/data/repositories/externalAccountRepository.ts) — `create`/`findByUserId` (массив — пользователь может иметь несколько подключённых аккаунтов, §70)/`update`, тот же минимальный паттерн, что у остальных репозиториев. **Найдено по ходу реализации**: контракт `IntegrationProvider` (Task 3.1) не возвращал id аккаунта на стороне платформы — нужен для `externalUserId` при сохранении. Добавлен новый метод `getAccountIdentity(accessToken)` (аддитивно, не меняет существующие методы) в `IntegrationProvider`/`IntegrationService`/`providers/instagram.ts` — реализован через тот же `/me?fields=user_id,username` запрос, что раньше был приватным `fetchOwnIgUserId` (переименован в `fetchAccountIdentity`, переиспользуется `getAccountInsights`/`listRecentMedia`). [`src/app/api/integrations/instagram/callback/route.ts`](src/app/api/integrations/instagram/callback/route.ts) переписан: теперь требует активную сессию приложения (`requireSessionUserId()` — 401 без сессии, подключение внешнего аккаунта должно быть привязано к конкретному пользователю), после успешного обмена ищет существующий `ExternalAccount` для (userId, platform, externalUserId) и обновляет его (реконнект) либо создаёт новый — не создаёт дубли при повторном подключении того же аккаунта. Интеграционный smoke-тест [`tests/integration/external-account-data-flow.smoke.test.ts`](tests/integration/external-account-data-flow.smoke.test.ts) проверяет `create`/`findByUserId`/`update` против живой БД. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (7/7 файлов, 11/11 тестов) — без регрессий; dev-БД `olga` — 0 users/0 externalAccounts после прогонов. **Не проверено живым OAuth round trip** (не требовалось для этой задачи — persistence логика проверена репозиторным тестом, не через повторный реальный Instagram-flow).

### Task 3.4 — UI: экран подключения/отключения Instagram
**Цель:** построить первый реальный (не диагностический) product-flow поверх готового data layer (Task 3.3) и Integration Service (Task 3.1/3.2): кнопка «Подключить Instagram» (ведёт на authorize URL), отображение текущего статуса подключения, кнопка «Отключить».
**Спецификация:** `src/integrations/` (Task 3.1/3.2), `externalAccountRepository` (Task 3.3), `30_SECURITY_PRIVACY.md` §25 (TOKEN REVOCATION — disconnect обязан переводить в статус `disconnected`, не удалять запись, и останавливать дальнейшие sync jobs, когда они появятся), `INSTAGRAM_API_REVIEW.md` §2 (`state` — обязательная CSRF-защита OAuth flow).
**Что входит:**
- Route для инициации подключения — заменяет ручной throwaway-скрипт из Task 3.2: генерирует `state`, редиректит на authorize URL.
- **Закрыть существующий gap, раз кнопка становится по-настоящему кликабельной, не только диагностической**: `callback/route.ts` (Task 3.3) сейчас читает `state` из редиректа, но никак не сверяет его — эквивалент отсутствия CSRF-защиты. Нужен механизм хранения `state` между запросом на authorize и возвратом на callback (например short-lived cookie) и сверка при возврате.
- Страница/секция с состоянием подключения: не подключено (или `status: disconnected`/`expired`) → кнопка «Подключить»; подключено → username, дата подключения, статус, кнопка «Отключить».
- Route отключения: session-protected, находит `ExternalAccount` пользователя, переводит `status → disconnected` (не удаляет запись), best-effort вызывает `IntegrationService.disconnect` (для Instagram сейчас no-op — endpoint revocation неизвестен, `INSTAGRAM_API_REVIEW.md` §8 gap 1, это ожидаемо, не баг).
**Явно не входит:** encryption at rest для `accessToken` (`DECISIONS.md` D-0010 — отдельная будущая задача, блокирует production, не этот UI-шаг); реальные sync jobs, которые должны будут учитывать статус `disconnected` (sync jobs пока не существуют вообще — это Phase 4+); поддержка нескольких одновременных аккаунтов/платформ в UI (data layer это уже поддерживает по конструкции, но UI на этом шаге рассчитан на один Instagram-аккаунт на пользователя — достаточно для MVP); Advanced Access/App Review (организационная зависимость для реальных пользователей, `INSTAGRAM_API_REVIEW.md` §3, не блокирует эту задачу — Olga тестирует как единственный test user).
**Готово, когда:** кнопка «Подключить» реально работает (без ручного скрипта) — Olga проходит flow через реальный браузер и видит статус «подключено» с username; `state` генерируется и сверяется, несовпадение отклоняется с понятной ошибкой; кнопка «Отключить» переводит запись в `disconnected` (не удаляет), повторное подключение после отключения работает как reconnect (уже покрыто repository-логикой Task 3.3, но UI должен довести это до конца); route отключения и логика статуса покрыты автоматическим тестом без необходимости живого Instagram-запроса; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят. Финальная ручная проверка кнопок через реальный браузер — на Olga (то же ограничение, что в Task 3.2/3.3: реальный consent screen нельзя пройти автоматизированным тестом).
**Статус:** Реализовано, ждёт финальной ручной проверки Olga через реальный браузер (см. выше). [`src/integrations/config.ts`](src/integrations/config.ts) — общие `INSTAGRAM_REDIRECT_URI`/`INSTAGRAM_OAUTH_STATE_COOKIE`, чтобы redirect_uri не дублировался литералом в двух route'ах. [`src/app/api/integrations/instagram/authorize/route.ts`](src/app/api/integrations/instagram/authorize/route.ts) — реальная инициация (заменяет throwaway-скрипт Task 3.2): без сессии редиректит на `/login`; генерирует `state`, кладёт в short-lived httpOnly cookie (`sameSite: lax`, 10 минут), редиректит на authorize URL. [`callback/route.ts`](src/app/api/integrations/instagram/callback/route.ts) дополнен CSRF-проверкой (`state` из cookie сверяется с `state` из редиректа, cookie одноразовая — удаляется независимо от исхода) и больше не отдаёт raw JSON — редиректит на `/integrations` с понятным исходом в query (`connected=1` либо `error=<code>`), это и есть «реальный product-flow», не диагностика. [`disconnect/route.ts`](src/app/api/integrations/instagram/disconnect/route.ts) — `POST` (не `GET`, state-changing действие), переводит запись в `disconnected`, best-effort вызывает `IntegrationService.disconnect` (no-op для Instagram, ожидаемо). [`src/app/integrations/page.tsx`](src/app/integrations/page.tsx) + [`DisconnectButton.tsx`](src/app/integrations/DisconnectButton.tsx) — экран статуса; не залогинен → приглашение войти; не подключён → ссылка «Подключить Instagram»; подключён → статус/username/дата подключения/кнопка «Отключить». Ссылка на `/integrations` добавлена на главную страницу. Интеграционный тест на реальном Instagram-запросе технически невозможен (та же причина, что Task 3.2/3.3) — вместо этого [`tests/e2e/integrations-instagram.spec.ts`](tests/e2e/integrations-instagram.spec.ts) (4 теста, реальный браузер + реальная БД, «подключённое» состояние сеется напрямую через Prisma в обход authorize-шага) проверяет: неаутентифицированный `/integrations` показывает приглашение войти; неаутентифицированный `authorize` редиректит на `/login`; «не подключён» показывает кнопку подключения; «подключён» → клик «Отключить» → статус реально меняется на `disconnected` в БД и UI обновляется. `npx tsc --noEmit`, `npm run lint`, `npm run build` (все новые routes зарегистрированы), `npm test` (7/7, 11/11), `npm run test:e2e` (12/12, включая 4 новых) — без регрессий; dev-БД `olga` — 0 users/0 externalAccounts после прогонов. По ходу остановлен (с разрешения Olga) процесс `next dev --experimental-https`, оставшийся в её терминале после живой проверки Task 3.2/3.3 — блокировал порт 3000 для Playwright.

**Дополнение (13 августа 2026, по запросу Olga после первого отчёта)**: изначально `ExternalAccount` (Task 3.3) не хранил username — сознательно не стал расширять согласованную схему без разрешения, явно указал это в отчёте. Olga подтвердила, что это не просто удобство: у нее несколько связанных Instagram-аккаунтов, и экран без имени не даёт убедиться, что подключён именно нужный — попросила добавить. Добавлено: `prisma/schema.prisma` — `ExternalAccount.username` (nullable `String?` — платформа может не вернуть username, различие «не пришло» vs «пустая строка» важно, по аналогии с data-completeness принципом `08_METRICS_FRAMEWORK.md` §11); миграция `20260813153419_external_account_username`, применена и проверена на чистое переприменение полной истории (4 миграции) на одноразовой базе. `callback/route.ts` теперь сохраняет `identity.username` при create/update. `/integrations` показывает `Аккаунт: @username` (или явное «username недоступен», если Instagram его не вернул — не молчаливый пропуск). Тесты обновлены: `tests/integration/external-account-data-flow.smoke.test.ts` проверяет round-trip поля; `tests/e2e/integrations-instagram.spec.ts` сеет `username` и проверяет, что он реально отображается на экране, не только что статус «подключён». `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (7/7, 11/11), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 0 users/0 externalAccounts после прогонов.

**Финальное подтверждение Olga (13 августа 2026)**: полный цикл пройден вручную через настоящий интерфейс (`https://localhost:3000/integrations`, `next dev --experimental-https`). Подключение через реальный Instagram consent screen показало правильный аккаунт — `@nikodine` — среди нескольких связанных у Olga аккаунтов, именно то, для чего добавлялся `username`. Отключение вернуло статус в «не подключён». **Task 3.4 полностью закрыта**, включая финальную ручную проверку, которая оставалась открытым пунктом в «Готово, когда».

---

## Phase 4 — Ingestion Pipeline (следующее после Phase 3)

### Task 4.1 — Разовая синхронная синхронизация Instagram → `Content`/`PerformanceMetric`
**Цель:** первый шаг ingestion pipeline (`26_DATA_PIPELINE.md`, диапазон §17–26, обозначенный Olga для Phase 4 — по факту наиболее прямо применимы §7–12 базовый цикл fetch→validate→normalize→store и §18–21 performance/snapshot/missing-data семантика) — забрать данные через уже готовый `IntegrationProvider`/`IntegrationService` (Task 3.1–3.2: `getAccountInsights` + `listRecentMedia`/`getMediaInsights`), провалидировать, нормализовать и сохранить в уже существующие `Content`/`PerformanceMetric` (Task 1.1). **Простая, синхронная, разовая** — без очереди/BullMQ/фоновых задач: у нас нет ни Redis, ни явной необходимости в фоновой обработке для одного пользователя. Решение о переходе на асинхронную обработку — отдельное, принимается позже при реальной необходимости, не сейчас.
**Спецификация:** `26_DATA_PIPELINE.md` §3–12 (DATA_SOURCES/RAW_DATA/INGESTION/INGESTION_RESULT/VALIDATION/INVALID_DATA/NORMALIZATION/PLATFORM_ADAPTER/DEDUPLICATION/DATA_STORAGE), §18–21 (PERFORMANCE_PIPELINE/SNAPSHOT_PRINCIPLE/METRIC_NORMALIZATION/MISSING_DATA); `08_METRICS_FRAMEWORK.md` §4 (Format-Specific Metrics — распознавать формат каждой публикации и интерпретировать метрики соответственно; конкретный пример в тексте §4 — не сравнивать Reel и carousel по одинаковым допущениям, не Reel и video), §11 (data completeness: «данных нет» ≠ «не запрашивалось»); `src/integrations/` (Task 3.1–3.2); `prisma/schema.prisma` (`Content`/`PerformanceMetric`, Task 1.1); `DECISIONS.md` D-0011 (сырые данные не сохраняются на этом этапе — см. ниже).
**Что входит:**
- Модуль пайплайна (вероятно новая папка `src/ingestion/`, по аналогии с `src/auth/`/`src/integrations/` — INGESTION не картируется один-в-один на семь слоёв `CLAUDE.md` §3, это не архитектурное решение уровня DECISIONS.md, просто согласованное размещение).
- Для каждой публикации из `listRecentMedia`: validate (обязательные поля — `externalId`, `publishedAt`; отсутствие — публикация пропускается и логируется, не падает весь sync) → normalize (`mediaType` → внутренний `contentType`; точные строковые значения `media_type`, которые реально возвращает API, не подтверждены эмпирически до этой задачи — этот шаг их наконец подтвердит) → upsert в `Content` по естественному ключу `[externalAccountId, externalContentId]` (уже есть unique constraint, Task 1.1), где `externalAccountId` — это `ExternalAccount.id` (наш внутренний id, не строка платформы) — довершает то, что было явно оставлено плейсхолдером в Task 3.3/3.4.
- Для каждой публикации — `getMediaInsights`, каждая метрика (включая явно перечисленные в `unavailableMetrics`, см. `08_METRICS_FRAMEWORK.md` §11) сохраняется как **новая** строка `PerformanceMetric` (не upsert — `PerformanceMetric` уже спроектирован как snapshot-таблица, повторный sync создаёт новую точку временного ряда, не перезаписывает старую, это и есть `SNAPSHOT_PRINCIPLE` §19 «даром», без дополнительного кода). `value: null` для метрик из `unavailableMetrics` — не молчаливый пропуск.
- `getAccountInsights` — забирается и нормализуется, но **не персистится** в этой задаче: `PerformanceMetric.contentId` обязателен (не nullable), а account-level метрика по определению не привязана к конкретной публикации — ни один из существующих 10 entities не подходит для account-level данных без схемного расширения. Показывается в ответе синхронизации (summary), не в БД. Это explicit scope-граница, найденная при формулировке, не тихий пропуск — постоянное хранилище для account-level метрик (`Baseline`/`PerformanceSnapshot`-подобная сущность) — отдельная будущая задача, уже предвиденная зарубкой в `DECISIONS.md` D-0008.
- Обработка ошибок токена: если `IntegrationAuthError` всплывает во время sync — `ExternalAccount.status → 'expired'` (не `'disconnected'` — разный смысл: `expired` значит платформа сама инвалидировала токен, `disconnected` — пользователь сам отключил). Наконец задействует terminal-state дизайн, заложенный в `IntegrationProvider` ещё в Task 3.1.
- Триггер: `POST /api/integrations/instagram/sync` (session-protected, синхронный, без очереди), находит подключённый `ExternalAccount`, гоняет пайплайн, обновляет `ExternalAccount.lastSyncedAt` (поле существует с Task 3.3, пока не использовалось), возвращает summary (сколько публикаций/метрик сохранено, account-level insights, предупреждения по невалидным записям). Минимальная кнопка «Синхронизировать» + отображение результата на `/integrations` — без синхронизации некому было бы её вызвать, кроме как через dev tools, что не «готовая» задача.
**Явно не входит:** очередь/BullMQ/scheduled sync (сознательно отложено — отдельное решение позже, не эта задача); персистентное хранилище account-level метрик (см. выше); raw payload storage (`DECISIONS.md` D-0011 — YELLOW-решение принято заранее, не оставлено неявным); `Content.externalAccountId`/`externalContentId` как настоящий `@relation` внешний ключ на уровне схемы (заполняются правильными значениями в этой задаче, но остаются простыми строковыми полями — полноценная схемная миграция на relation, если понадобится, отдельно); incremental/change detection логика (`26_DATA_PIPELINE.md` §13–14 — пока просто «взять последние N публикаций и синхронизировать заново», не различать «что изменилось»); content features/AI-derived данные (§15–17 того же документа — Analysis layer, не Ingestion).
**Готово, когда:** ручной вызов на реальном подключённом аккаунте Olga создаёт корректные `Content`/`PerformanceMetric` записи; повторный вызов не создаёт дублей `Content` (upsert по natural key), но создаёт новые `PerformanceMetric` снимки (проверяемо по возросшему количеству строк с разными `measuredAt`); `mediaType` нормализуется в внутренний формат, реальные строки от API задокументированы по факту; `unavailableMetrics` создают `value: null` записи, не пропускаются молча; истёкший/отозванный токен переводит `ExternalAccount.status` в `expired`, не падает необработанным исключением; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят; интеграционный тест на пайплайне validate/normalize без сети (unit-уровень на синтетических данных, по аналогии с уже проверенным на реальном API providers/instagram.ts — сам факт живого Instagram-запроса confirmed ещё в Task 3.2, здесь достаточно проверить логику трансформации).
**Статус:** Завершено, включая живую проверку против реального Instagram API (не только dev-аккаунта Olga вручную через UI, но и автоматизированным живым тестом — см. ниже). [`src/ingestion/normalize.ts`](src/ingestion/normalize.ts) — чистые `validateMediaItem`/`normalizeContentType`, без сети/БД. [`src/ingestion/instagramSync.ts`](src/ingestion/instagramSync.ts) — `syncInstagramAccount(userId)`: находит подключённый `ExternalAccount`, получает `getAccountInsights`(`reach`, не персистится — см. явную scope-границу ниже) + `listRecentMedia`, на каждый item — validate → normalize → upsert `Content` по `[externalAccountId, externalContentId]` (`externalAccountId` = `ExternalAccount.id`) → `getMediaInsights` → новые строки `PerformanceMetric` (включая `value: null` для `unavailableMetrics`). `IntegrationAuthError` — terminal state, `ExternalAccount.status → 'expired'`, прерывает sync (retry по остальным публикациям бессмыслен — токен одинаково не работает для всех). Прочие ошибки — per-item, не блокируют весь sync (partial-success, `26_DATA_PIPELINE.md` §6 INGESTION_RESULT).

**Найдено при реализации (не было известно на Task 3.2):** `media_type` сам по себе не отличает Reels от обычного видео — оба приходят как `"VIDEO"`. Добавлено поле `media_product_type` в запрос `listRecentMedia` (`IntegrationMediaSummary.mediaProductType`, аддитивно) — **подтверждено живым запросом**: реальный аккаунт Olga вернул `media_product_type: "REELS"` для части публикаций, `normalizeContentType` корректно развёл `reel`/`carousel` (её последние 25 публикаций оказались только каруселями и Reels — ни одной одиночной фото/видео-публикации, поэтому `image`/`video` категории не были эмпирически проверены живым запросом в этом прогоне, только логикой/unit-тестами на синтетических данных).

**Живая проверка полного pipeline** ([`tests/integration/instagram-sync-live.smoke.test.ts`](tests/integration/instagram-sync-live.smoke.test.ts), тот же паттерн skip-if-no-credentials, что Task 3.2): на реальном аккаунте Olga — **25 публикаций синхронизировано, 175 метрик записано, 0 предупреждений**. Повторный вызов в том же тесте: те же 25 `Content` (не дубли, id совпадают один-в-один с первым прогоном) + метрики выросли ровно вдвое (350) — подтверждает upsert-по-`Content`/insert-по-`PerformanceMetric` семантику не только логически, но и фактически на реальных данных. `account_insights` (`reach`) тоже пришли реальными числами, показаны в summary, не в БД, как и запланировано.

**Repository:** [`contentRepository.findByExternalId`](src/data/repositories/contentRepository.ts) добавлен — dedup-lookup по natural key, используя уже существующий unique constraint (Task 1.1).

**Trigger:** [`src/app/api/integrations/instagram/sync/route.ts`](src/app/api/integrations/instagram/sync/route.ts) (`POST`, session-protected) + [`SyncButton.tsx`](src/app/integrations/SyncButton.tsx) на `/integrations` — показывает счётчики (публикаций/пропущено/метрик) после клика, `ExternalAccount.lastSyncedAt` теперь отображается и реально обновляется (поле существовало с Task 3.3, не использовалось).

**Найденный и исправленный баг по ходу**: `syncInstagramAccount` изначально не регистрировал Instagram-провайдер сам (полагался на `import '@/integrations/bootstrap'` в вызывающих route'ах) — при прямом вызове из теста падал с «No integration provider registered». Исправлено: side-effect import перенесён в сам `instagramSync.ts`, так вызывающий код не должен помнить об этом сам.

**Тесты:** [`tests/unit/ingestion-normalize.test.ts`](tests/unit/ingestion-normalize.test.ts) (10 тестов, синтетические данные, без сети) + живой integration-тест выше. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован), `npm test` (9/9 файлов, 21/21 тестов, включая живой), `npm run test:e2e` (12/12, без регрессий на `/integrations`) — всё зелёное. Dev-БД `olga` после всех прогонов — 0 `Content`/`PerformanceMetric` от тестов (реальный аккаунт Olga из ручной проверки Task 3.4 остаётся нетронутым — не тестовые данные).

### Task 4.2 — Персистентное хранение account-level метрик (`AccountSnapshot`)
**Цель:** закрыть explicit scope-границу, оставленную Task 4.1 — `getAccountInsights` (реализован в Task 3.1–3.2) сейчас забирается и нормализуется, но не сохраняется, только показывается в summary синхронизации.
**Спецификация:** `08_METRICS_FRAMEWORK.md` §2 (Account-Level Metrics — отдельная категория от publication-level), §11 (data completeness); `25_DATABASE_SCHEMA.md` §13–14 (PERFORMANCE/PERFORMANCE_SNAPSHOTS — обе привязаны к `content_id`, не подходят), §16 (BASELINES — вычисленное значение, не сырое измерение, тоже не подходит); `26_DATA_PIPELINE.md` §19 SNAPSHOT_PRINCIPLE; `DECISIONS.md` D-0014 (новая сущность — YELLOW, обоснование ниже); `DOCUMENT_CROSS_REFERENCE.md` (пробел зафиксирован там же).
**Что входит:**
- Новая Prisma-модель `AccountSnapshot` — по аналогии с `PerformanceMetric`, но на уровне `ExternalAccount`: `id`/`externalAccountId`/`metricType`/`value` (nullable)/`capturedAt`/`source`/`createdAt`. Snapshot-семантика — каждый sync создаёт новые строки, не upsert.
- `src/data/repositories/accountSnapshotRepository.ts` — `create`/`findByExternalAccountId`, тот же минимальный паттерн, что у остальных репозиториев.
- `syncInstagramAccount` (`src/ingestion/instagramSync.ts`) — после `getAccountInsights` сохраняет каждую метрику как `AccountSnapshot`, включая `unavailableMetrics` как `value: null` (тот же принцип, что уже применён к `PerformanceMetric`).
**Явно не входит:** `Baseline`-вычисления поверх `AccountSnapshot` (Analysis layer, не Ingestion, будущая задача); scheduled/incremental sync (по-прежнему отдельное решение, не эта задача).
**Готово, когда:** повторный вызов `syncInstagramAccount` на реальном подключённом аккаунте создаёт новые строки `AccountSnapshot` (не upsert, проверяемо по возросшему количеству строк с разными `capturedAt`); `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят, включая живой smoke-тест.
**Статус:** Завершено, включая живую проверку. [`prisma/schema.prisma`](prisma/schema.prisma) — модель `AccountSnapshot` (миграция `20260813191441_account_snapshots`, применена на dev-базе `olga` и проверена на чистое переприменение полной истории — 5 миграций — на одноразовой базе). [`src/data/repositories/accountSnapshotRepository.ts`](src/data/repositories/accountSnapshotRepository.ts) добавлен и подключён в `src/data/repositories/index.ts`. [`src/ingestion/instagramSync.ts`](src/ingestion/instagramSync.ts) — после `getAccountInsights` сохраняет метрики + `unavailableMetrics` (`value: null`) как `AccountSnapshot`, ошибка здесь по-прежнему не блокирует синхронизацию публикаций (только warning), кроме `IntegrationAuthError` (пробрасывается дальше, как и раньше). [`tests/integration/instagram-sync-live.smoke.test.ts`](tests/integration/instagram-sync-live.smoke.test.ts) дополнен: проверяет, что `AccountSnapshot` создаются в первом sync и растут (не перезаписываются) во втором — подтверждено на реальном аккаунте Olga.
**Документационная находка:** `25_DATABASE_SCHEMA.md` не описывает ни одной сущности под account-level метрики — `PERFORMANCE`/`PERFORMANCE_SNAPSHOTS` (§13–14) привязаны к `content_id`, `BASELINES` (§16) — вычисленное значение с `confidence`/`sample_size`, не сырой снимок. Это пробел в самой документации, не только в объёме Task 4.1 — зафиксировано в [`DOCUMENT_CROSS_REFERENCE.md`](DOCUMENT_CROSS_REFERENCE.md) (новый файл, реестр найденных расхождений/пробелов в `/docs`, ранее не существовал), чтобы не переоткрывать при будущих задачах.
`npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (9/9 файлов, 21/21 тестов, включая живой) — без регрессий. Dev-БД `olga` после всех прогонов — 0 `Content`/`PerformanceMetric`/`AccountSnapshot` от тестов (реальный аккаунт Olga остаётся нетронутым).

### Task 4.3 — Устойчивость к сбоям для `syncInstagramAccount`
**Цель:** закрыть критерий готовности `42_IMPLEMENTATION_ROADMAP.md` §24 RETRIES (timeout, retry, exponential backoff, maximum attempts, failure state) для сетевых вызовов к Instagram API внутри `syncInstagramAccount`. Без очереди/Redis — реализуется прямо внутри существующей синхронной функции (прямое требование Olga).
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §24 (RETRIES — дословно: timeout; retry; exponential backoff; maximum attempts; failure state), §26 (PHASE 4 COMPLETION — «система способна стабильно получать и сохранять данные без постоянного ручного вмешательства»); уже существующий `IntegrationAuthError`/`IntegrationRateLimitError` (`IntegrationProvider.ts`, Task 3.1) — терминальное состояние отдельно от ретраибл.
**Что входит:**
- `src/ingestion/retry.ts` — `withRetry(fn, options)`: exponential backoff (`baseDelayMs * 2^attempt`, ограничен `maxDelayMs`), `maxAttempts` (по умолчанию 3). `IntegrationAuthError` пробрасывается немедленно, без ретраев (terminal state, как и раньше). Если у `IntegrationRateLimitError` есть `retryAfterSeconds` — используется вместо расчётного backoff (более точный сигнал платформы), включая корректную обработку `0` как валидного значения, не «не задано». После исчерпания попыток — `RetriesExhaustedError` (явное состояние failure: число попыток + исходная ошибка), не просто последнее сообщение без контекста.
- `src/integrations/providers/instagram.ts` — `fetchWithTimeout` (10с по умолчанию, `AbortController`), обёрнуты все 7 мест, где адаптер делает `fetch()`. Timeout — отдельное от retry требование §24: без него зависший запрос блокировал бы sync неограниченно (Node не применяет таймаут к `fetch()` по умолчанию).
- `src/ingestion/instagramSync.ts` — `withRetry` обёрнуты все три сетевых вызова к Instagram (`getAccountInsights`, `listRecentMedia`, `getMediaInsights`). `SyncWarning` дополнен опциональным `attempts` — если предупреждение вызвано исчерпанием ретраев, число попыток видно в структурированном виде, не только в тексте сообщения.
**Явно не входит:** retry для DB-вызовов (repositories) — только сетевые вызовы к Instagram API, как явно попросила Olga; retry для OAuth-эндпоинтов (`exchangeCodeForTokens`/`refreshTokens`) — вне scope «внутри sync», интерактивный browser-flow, не эта задача; очередь/BullMQ (по-прежнему сознательно отложено).
**Готово, когда:** транзиентная сетевая ошибка при sync ретраится с backoff и не проваливает sync с первой попытки; `IntegrationAuthError` по-прежнему не ретраится и сразу переводит `ExternalAccount.status` в `expired`; исчерпание попыток даёт понятное предупреждение с числом попыток, не тихий пропуск; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят, включая живой smoke-тест (реальные вызовы теперь идут через timeout+retry путь).
**Статус:** Завершено. [`src/ingestion/retry.ts`](src/ingestion/retry.ts) — новый модуль, `withRetry`/`RetriesExhaustedError`, покрыт [`tests/unit/ingestion-retry.test.ts`](tests/unit/ingestion-retry.test.ts) (7 тестов на синтетических ошибках: успех с первой попытки, успех после транзиентного сбоя, `IntegrationAuthError` не ретраится, исчерпание попыток даёт `RetriesExhaustedError` с верным числом попыток и `cause`, `retryAfterSeconds: 0` трактуется как валидная задержка «сразу» — не как «не задано», `maxAttempts: 1` = без ретраев вовсе). [`src/integrations/providers/instagram.ts`](src/integrations/providers/instagram.ts) — `fetchWithTimeout`, все 7 вызовов `fetch()` переведены на него. [`src/ingestion/instagramSync.ts`](src/ingestion/instagramSync.ts) — три сетевых вызова обёрнуты `withRetry`, `SyncWarning.attempts` добавлен. **Живая проверка**: `tests/integration/instagram-sync-live.smoke.test.ts` (существующий, без изменений в тесте — сам факт зелёного прогона подтверждает, что sync проходит через новый timeout+retry путь на реальном API без регрессий) прошёл на реальном аккаунте Olga. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (10/10 файлов, 28/28 тестов, включая живой), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 0 `Content`/`PerformanceMetric`/`AccountSnapshot` от тестов после прогонов.
**Замечено по ходу, не блокирует:** `42_IMPLEMENTATION_ROADMAP.md` §23 SYNC_JOBS рекомендует background jobs, «если операция может быть длительной» — ретраи с backoff увеличивают худший случай длительности синхронного HTTP-запроса `POST /sync` (при повсеместных транзиентных сбоях, `maxAttempts=3`/`baseDelayMs=500`/`maxDelayMs=5000`, на аккаунте из ~25 публикаций — до нескольких десятков секунд). Это тот же осознанный компромисс «без очереди», что и в Task 4.1, не новое расхождение — упомянуто для полноты, т.к. §24/§23 расположены рядом в одном документе.

---

## Phase 5 — Data Quality (42_IMPLEMENTATION_ROADMAP.md §27–30)

### Task 5.1 — Статус «состояние данных»: свести существующие сигналы freshness/gaps/sync status
**Цель:** не собирать новые данные — свести уже существующее (`ExternalAccount.lastSyncedAt`/`status`, `AccountSnapshot.capturedAt`, `PerformanceMetric.measuredAt`) в простую функцию/эндпоинт «насколько свежая последняя синхронизация, были ли недавние сбои, есть ли явные пробелы» (`42_IMPLEMENTATION_ROADMAP.md` §28 FRESHNESS, §29 DATA_HEALTH). Первый шаг Phase 5, не вся фаза — consistency/anomaly detection (остальные пункты §27) сюда не входят.
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §27 (PHASE 5, DATA QUALITY), §28 (FRESHNESS — «каждая relevant data record должна иметь timestamp или equivalent»), §29 (DATA_HEALTH — «когда данные последний раз обновлялись; есть ли missing data; есть ли sync failures; можно ли использовать dataset для reasoning»); `DECISIONS.md` D-0015 (`SyncWarning` не персистится — обоснование ниже).
**Что входит:**
- `src/dataQuality/dataQualityStatus.ts` — `getDataQualityStatus(userId)`: по каждому `ExternalAccount` пользователя (не хардкод на Instagram — `ExternalAccount` уже мультиплатформенный, §70 FUTURE_MULTI_PLATFORM) — `connectionStatus`, `hasRecentFailure`, `lastSyncedAt`, `freshness` (`fresh`/`stale`/`never_synced`, порог 24ч — заглушка, не измеренная величина, см. ниже), `lastContentMetricAt`, `lastAccountSnapshotAt`, `gaps` (`no_content_synced`/`no_account_snapshots`, только после хотя бы одной попытки sync).
- Новые repository-методы (только чтение, без новых таблиц/данных): `contentRepository.countByExternalAccountId`, `performanceMetricRepository.findLatestMeasuredAtByExternalAccountId`, `accountSnapshotRepository.findLatestCapturedAt`.
- `GET /api/data-quality` (session-protected, только чтение — не state-changing, поэтому `GET`, в отличие от `POST /sync`).
**Явно не входит:** UI-экран для отображения статуса (кандидат на будущую задачу, если понадобится); consistency/anomaly detection (§27, не §28/§29); персистентная история sync-попыток — `SyncWarning` (Task 4.3) нигде не сохраняется, `hasRecentFailure` поэтому основан только на `ExternalAccount.status === 'expired'` (D-0015).
**Найдено при формулировке (не было очевидно заранее):** Olga назвала `SyncWarning` как один из источников для консолидации, но он существует только как возвращаемое значение одного конкретного вызова `syncInstagramAccount` (ответ `POST /sync`, показывается в `SyncButton.tsx`) — нигде не персистится. Функция, читающая состояние независимо от момента последнего клика «Синхронизировать», физически не может увидеть прошлые warnings. Зафиксировано как YELLOW, [`DECISIONS.md` D-0015](DECISIONS.md) — не тихое сужение scope.
**Готово, когда:** `getDataQualityStatus` корректно определяет `never_synced`/`fresh`/`stale` по `lastSyncedAt`; явные пробелы (`no_content_synced`/`no_account_snapshots`) обнаруживаются только после хотя бы одной попытки sync, не для только что подключённого аккаунта; `status === 'expired'` отражается в `hasRecentFailure`; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят.
**Статус:** Завершено. [`src/dataQuality/dataQualityStatus.ts`](src/dataQuality/dataQualityStatus.ts), [`src/app/api/data-quality/route.ts`](src/app/api/data-quality/route.ts). Папка `src/dataQuality/` — прагматичное размещение по аналогии с `src/ingestion/` (Task 4.1): Data Quality — отдельная фаза `42_IMPLEMENTATION_ROADMAP.md`, не мапится один-в-один на 7 слоёв `CLAUDE.md` §3, не архитектурное решение уровня `DECISIONS.md`. [`tests/integration/data-quality-status.smoke.test.ts`](tests/integration/data-quality-status.smoke.test.ts) — 6 тестов против реальной БД (не мок): `never_synced` без пробелов, `fresh` без пробелов при наличии контента/снимков, `stale` при `lastSyncedAt` старше 24ч, оба пробела вместе при sync без сохранённых данных, `hasRecentFailure` при `status: 'expired'`, пустой массив для пользователя без подключённых аккаунтов. Не gated по Instagram-credentials — функция не делает сетевых вызовов, только чтение БД. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован), `npm test` — новый тест-файл 6/6, полный набор без учёта двух заранее известных внешних live-сбоев (см. ниже) — 32/34, `npm run test:e2e` (12/12) — без регрессий.
**Обнаружено при прогоне, не связано с Task 5.1:** оба live smoke-теста, использующих реальный Instagram API (`instagram-live.smoke.test.ts`, Task 3.2; `instagram-sync-live.smoke.test.ts`, Task 4.1/4.3), упали с `Error: API access blocked.` — ответ от самого Instagram Graph API, не код проекта (подтверждено: Task 5.1 не трогал `src/integrations/providers/instagram.ts`/`src/ingestion/instagramSync.ts`, при Task 4.3 те же тесты были зелёными). RED — похоже на доступ уровня Meta App/токена, не диагностируется и не устраняется без Meta App Dashboard Olga. Требует её внимания отдельно от Task 5.1.

### Task 5.2 — Completeness + anomaly detection поверх `getDataQualityStatus`
**Цель:** расширить `getDataQualityStatus` (Task 5.1) двумя показателями, полностью на уже сохранённых данных, без обращений к Instagram API: (1) completeness — какая доля публикаций имеет полный набор ожидаемых метрик, используя уже существующее различие `value: null` для недоступных метрик (Task 4.1); (2) простая проверка на аномалию объёма — заметно ли последняя синхронизация вернула меньше записей, чем обычно для этого аккаунта, сравнение с историей самого аккаунта, не абсолютный порог.
**Спецификация:** `26_DATA_PIPELINE.md` §21 (MISSING_DATA — различать 0/NULL/unavailable/not_yet_measured), §56 (DATA_QUALITY — категории complete/partial/suspicious/unavailable), §57 (QUALITY_IMPACT — «если из 20 публикаций у 8 отсутствует ключевая метрика, система не должна вести себя так, будто анализ основан на всех 20», прямая модель для completeness ratio); `DECISIONS.md` D-0016 (кластеризация sync run по разрыву времени — обоснование ниже).
**Уточнение цитаты при формулировке:** Olga сослалась на `26_DATA_PIPELINE.md` §35–36 (SKELETON_CRITERIA/SKELETON_UPDATE) как на источник для anomaly detection — при проверке эти секции оказались про Content Skeleton (Recommendation Engine, Phase 8), не про аномалии объёма синхронизации. Точной секции именно про «меньше записей, чем обычно» в документе нет — ближайшая по духу секция §56 (категория «suspicious»). Реализация — инженерная интерпретация этого общего принципа, не переложение конкретного пункта документа. Находка не меняет суть задачи (аномалия по-прежнему нужна и реализована), только уточняет обоснование.
**Что входит:**
- [`src/dataQuality/completeness.ts`](src/dataQuality/completeness.ts) — чистая `computeCompleteness(rows)`: для каждой публикации берёт только последний по времени batch метрик (snapshot-семантика `PerformanceMetric`, Task 4.1) — публикация «complete», если ни одна метрика в этом batch не `null`. Возвращает `completenessRatio` (доля complete/total) или `null`, если метрик ещё нет вовсе.
- [`src/dataQuality/anomalyDetection.ts`](src/dataQuality/anomalyDetection.ts) — чистая `detectSyncCountAnomaly(rows)`: кластеризует `measuredAt` по разрыву времени (`SYNC_RUN_GAP_MS`, 5 минут — заглушка) в приближённые «прогоны» (в схеме нет отдельной сущности sync run, см. D-0016), считает число различных `contentId` в последнем прогоне против среднего по предыдущим (`ANOMALY_RATIO_THRESHOLD`, 0.5 — относительный порог, не абсолютный). Статусы: `ok`/`anomaly`/`insufficient_history` (один прогон в истории — не с чем сравнивать, не ложный «ok»).
- `AccountDataQualityStatus` (Task 5.1) дополнен `completeness`/`syncCountAnomaly`; новые записи в `gaps` — `incomplete_metrics` (ratio < 1), `sync_count_anomaly` (status === 'anomaly').
- `performanceMetricRepository.findRowsByExternalAccountId` заменил `findLatestMeasuredAtByExternalAccountId` из Task 5.1 (был не нужен нигде больше) — один запрос сырых `(contentId, measuredAt, value)` строк обслуживает freshness (`lastContentMetricAt`, теперь вычисляется в коде из тех же строк), completeness и anomaly detection разом, вместо трёх отдельных обращений к БД.
**Явно не входит:** consistency-проверки (§27, отдельный пункт Phase 5, не эта задача); персистентная сущность «sync run» для точного (не приближённого) подсчёта записей на прогон — новые данные сверх уже сохранённых, вне рамок задачи (см. D-0016); UI-экран.
**Готово, когда:** `computeCompleteness`/`detectSyncCountAnomaly` корректно работают на синтетических данных (юнит-тесты) и на реалистичном масштабе через реальную БД (интеграционные тесты); `gaps` пополняется `incomplete_metrics`/`sync_count_anomaly` в соответствующих случаях; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев, см. Task 5.1).
**Статус:** Завершено. [`tests/unit/data-quality-completeness.test.ts`](tests/unit/data-quality-completeness.test.ts) — 6 тестов (нет данных → `null`; complete/incomplete по одной публикации; ровно 8-из-20 пример §57 масштабирован; учитывается только последний batch, не вся история; несколько публикаций независимо). [`tests/unit/data-quality-anomaly-detection.test.ts`](tests/unit/data-quality-anomaly-detection.test.ts) — 7 тестов (нет данных; один прогон → `insufficient_history`; `ok` при типичном разбросе; `anomaly` при явном провале; относительный, не абсолютный порог — маленький аккаунт с 2 из обычных 3 не флагуется; среднее считается по всей истории, не только по предыдущему прогону; считаются различные `contentId`, не строки). [`tests/integration/data-quality-status.smoke.test.ts`](tests/integration/data-quality-status.smoke.test.ts) дополнен 2 тестами end-to-end wiring через реальную БД: completeness на масштабе 25 публикаций (17 complete/8 incomplete, ratio 17/25, gap `incomplete_metrics`) и anomaly на трёх прогонах (10/10/2, gap `sync_count_anomaly`). **Находка**: реальный датасет Olga (25 публикаций/175 метрик), упомянутый в постановке, на момент реализации не сохранён персистентно в dev-БД — аккаунт сейчас `disconnected`, 0 `Content` (та живая проверка была временными данными throwaway-тестов Task 4.1/4.3, удалёнными в `afterAll`). Тесты сеют синтетические данные того же масштаба напрямую — ровно то, что Olga попросила («живой API не нужен»). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (13 юнит + 8 интеграционных новых/обновлённых тестов — все зелёные; полный набор — 47/49, два падения те же самые заранее известные live-сбои из Task 5.1, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 0 `Content`/`PerformanceMetric`/`AccountSnapshot` от тестов после прогонов.

### Task 5.3 — Consistency: последний пункт Phase 5
**Цель:** закрыть последний пункт `42_IMPLEMENTATION_ROADMAP.md` §27 (consistency) — проверка внутренней непротиворечивости уже сохранённых данных, полностью на существующих таблицах, без Instagram API.
**Спецификация:** **Явная оговорка источника, зафиксированная перед реализацией по прямому требованию Olga** — в отличие от Task 5.1/5.2, отдельного детального документного раздела про consistency нет: `42_IMPLEMENTATION_ROADMAP.md` §27 упоминает слово в списке без расшифровки, и независимая проверка (сделана перед реализацией, полный текстовый поиск по `/docs`) это подтвердила — см. [`DECISIONS.md` D-0017](DECISIONS.md) за полным списком проверенных и отклонённых кандидатов (`24_TECHNICAL_ARCHITECTURE.md` §49, `27_API_CONTRACTS.md` §62–63, `26_DATA_PIPELINE.md` §31, `39_BACKUP_DISASTER_RECOVERY.md` §45–46 — ни один не подходит по смыслу или контексту). Реализация — явное рабочее предложение Olga из постановки задачи, помечено ею самой как «не цитата».
**Что входит:**
- [`src/dataQuality/temporalConsistency.ts`](src/dataQuality/temporalConsistency.ts) — чистая `checkTemporalConsistency(rows)`: `PerformanceMetric.measuredAt` раньше `Content.publishedAt` у той же публикации — временна́я невозможность. Встроена в `AccountDataQualityStatus` (per-account, как completeness/anomaly, Task 5.2) — `temporalConsistency`, gap `temporal_inconsistency`.
- [`src/dataQuality/schemaInvariants.ts`](src/dataQuality/schemaInvariants.ts) — чистая `findUniquenessViolations(groups)` + `checkSchemaInvariants()`: нарушение уникальности `[platform, externalUserId]` (`ExternalAccount`) и `[externalAccountId, externalContentId]` (`Content`) — оба уже enforced Postgres `@@unique` (Task 1.1/3.3). **Не per-user** — системная проверка, отдельное поле `schemaInvariantViolations` на верхнем уровне `GET /api/data-quality`, не внутри `accounts[]`.
- Новые repository-методы (только чтение, счётчики групп без `having`-фильтрации — фильтрация вынесена в чистую `findUniquenessViolations` ради юнит-тестируемости): `externalAccountRepository.findPlatformExternalUserIdGroupCounts`, `contentRepository.findExternalContentIdGroupCounts`; `performanceMetricRepository.findRowsByExternalAccountId` дополнен `publishedAt` (join через `Content`).
**Явно не входит:** консолидация в новую персистентную сущность/таблицу — обе проверки полностью вычисляются на лету поверх уже существующих данных; UI-экран.
**Честная оговорка (`schemaInvariants.ts`):** обе уникальности уже гарантированы констрейнтами БД — вставить настоящий дубликат физически невозможно, `checkSchemaInvariants()` на живой БД всегда будет возвращать пустой результат. Ценность — defense in depth: если констрейнт случайно потеряется при будущей миграции, эта проверка это заметит явно, а не полагается молча на «constraint же есть». Ветка «находит нарушение» проверена юнит-тестом на синтетических счётчиках, не на реальной БД (там физически невозможно).
**Готово, когда:** `checkTemporalConsistency` корректно находит нарушение на синтетических данных и на реальной БД (интеграционный тест); `checkSchemaInvariants` реально выполняет запрос к живой БД и возвращает пустой результат (констрейнты держат инвариант); `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/data-quality-temporal-consistency.test.ts`](tests/unit/data-quality-temporal-consistency.test.ts) — 6 тестов (пусто; после публикации — не нарушение; ровно в момент публикации — не нарушение; до публикации — нарушение; `publishedAt: null` — не нарушение, не с чем сравнивать; смесь валидных/невалидных строк). [`tests/unit/data-quality-schema-invariants.test.ts`](tests/unit/data-quality-schema-invariants.test.ts) — 4 теста на `findUniquenessViolations` (все count=1 → пусто; пустой вход; одна нарушающая группа; несколько сразу). [`tests/integration/data-quality-status.smoke.test.ts`](tests/integration/data-quality-status.smoke.test.ts) дополнен 2 тестами (нарушение временно́й последовательности через реальную БД → gap; нормальный порядок → без gap). [`tests/integration/data-quality-schema-invariants.smoke.test.ts`](tests/integration/data-quality-schema-invariants.smoke.test.ts) — новый файл, 2 теста: реальная БД без нарушений возвращает пустой результат; попытка вставить настоящий дубликат `[platform, externalUserId]` отклоняется констрейнтом на уровне Postgres (демонстрирует, почему первая ветка `findUniquenessViolations` не тестируема против живой БД). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (10 новых юнит-тестов + 4 новых/интеграционных — все зелёные; полный набор — 61/63, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga, не тестовые данные), 0 `Content`/`PerformanceMetric` от тестов.

---

## Phase 6 — Analytics Foundation (42_IMPLEMENTATION_ROADMAP.md §31–34)

### Task 6.1 — Первая детерминированная аналитика: сумма/среднее/тренд по основным метрикам
**Цель:** первая реализация ANALYSIS layer (`src/analysis/`, до этой задачи — только README-заготовка с Task 0.2) — простые, проверяемые, не-AI вычисления (`42_IMPLEMENTATION_ROADMAP.md` §32: «если показатель можно надёжно вычислить обычным кодом, он должен вычисляться обычным кодом») над уже сохранёнными `Content`/`PerformanceMetric`: сумма/среднее за период + тренд (растёт/падает/стабильно). Полностью на существующих данных, без Instagram API.
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §31 (PHASE 6 — reproducible/testable/independent from AI/based on defined formulas), §32 («AI не должен считать всё»), §33 (ANALYTICS_OUTPUT — structured results: metric/period/value/trend); `22_DATA_MODEL.md` §10 CONTENT_PERFORMANCE (основные показатели: `reach`/`likes`/`saves`/`followers_gained`); `DECISIONS.md` D-0018 (три метрики из четырёх, обоснование ниже).
**Найдено при формулировке:** `followers_gained` из §10 нигде не собирается — не на уровне публикации (не применимо, account-level показатель), не на уровне аккаунта (`getAccountInsights` в `syncInstagramAccount` запрашивает только `reach`, Task 4.1). Добавлять сбор сейчас означало бы новое обращение к Instagram API — прямо против формулировки задачи. `CORE_METRICS` сознательно ограничен `reach`/`likes`/`saved` (`saved`, не `saves` — так называет метрику сама Instagram API, `PerformanceMetric.metricType` хранит это значение буквально). Зафиксировано как YELLOW, `DECISIONS.md` D-0018.
**Что входит:**
- [`src/analysis/metricsAnalytics.ts`](src/analysis/metricsAnalytics.ts) — чистые функции: `summarizeMetric`/`computeMetricsAnalytics`. На публикацию берётся только последнее известное (по `measuredAt`) значение метрики в периоде (snapshot-семантика `PerformanceMetric`, тот же принцип, что `computeCompleteness`, Task 5.2), период фильтруется по `Content.publishedAt`. Тренд — публикации периода делятся хронологически пополам, сравниваются средние двух половин, относительный порог 10% (`DECISIONS.md` D-0018) — `up`/`down`/`stable`/`insufficient_data`.
- [`src/analysis/accountAnalytics.ts`](src/analysis/accountAnalytics.ts) — тонкая оркестрация: `getUserAnalytics(userId, period)`, по каждому `ExternalAccount` пользователя (тот же паттерн, что `getDataQualityStatus`, Task 5.1).
- `performanceMetricRepository.findRowsByExternalAccountId` (Task 5.2/5.3) дополнен `metricType` — один запрос обслуживает data quality (Task 5.x) и analytics (Task 6.1) разом.
- `GET /api/analytics?days=N` (session-protected, только чтение, по умолчанию 30 дней).
**Явно не входит:** `followers_gained` (см. находку выше — будущая задача Phase 4/Ingestion); anomaly/confidence поля из ANALYTICS_OUTPUT §33 (не применимы к простой сумме/среднему на этом этапе — anomaly для sync count уже закрыт Task 5.2 отдельно, per-metric confidence — Pattern Detection, Phase 7); UI-экран; сравнение с предыдущим периодом (comparison из §33) — только тренд внутри одного периода, следующий шаг Phase 6, не эта задача.
**Готово, когда:** `summarizeMetric`/`computeMetricsAnalytics` корректно считают сумму/среднее/тренд на синтетических данных; используется только последний снимок метрики на публикацию, не сумма по всей истории; `GET /api/analytics` реально возвращает структурированный результат на реальной БД, включая масштаб 25 публикаций; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/metrics-analytics.test.ts`](tests/unit/metrics-analytics.test.ts) — 12 тестов (нет данных; сумма/среднее по нескольким публикациям; фильтр по `metricType`; только последний снимок, не сумма истории; публикации вне периода исключены; `value: null` игнорируется; `insufficient_data` при < 2 публикаций; `stable`/`up`/`down` при разных сценариях; все три core-метрики разом; `followers_gained` отсутствует). [`tests/integration/account-analytics.smoke.test.ts`](tests/integration/account-analytics.smoke.test.ts) — 2 теста через реальную БД: пустой аккаунт → нули/`insufficient_data`; масштаб 25 публикаций (12 с `reach=100`, 13 с `reach=500`) → корректная сумма, тренд `up`. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован), `npm test` (12 юнит + 2 интеграционных новых — все зелёные; полный набор — 75/77, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 `Content`/`PerformanceMetric` от тестов.

### Task 6.2 — Personal baseline + сравнение + confidence
**Цель:** для каждой из трёх собираемых метрик (`reach`/`likes`/`saved`) — простой personal baseline (среднее по всей истории публикаций пользователя) + сравнение вывода Task 6.1 с ним («текущий период выше/ниже/на уровне нормы») + простой confidence по объёму выборки, чтобы не выдавать уверенный вывод на 2–3 постах. Полностью на уже сохранённых данных.
**Спецификация:** `22_DATA_MODEL.md` §13 PERSONAL_BASELINE («для каждого пользователя система формирует персональную норму… для reach/likes/saves/followers»; «baseline является вычисляемым знанием, а не исходным фактом»), §14 BASELINE_VERSION (baseline должен иметь версию/дату расчёта); `25_DATABASE_SCHEMA.md` §16 BASELINES (поля персистентной сущности, `scope` global/format/topic/goal), §17 BASELINE_HISTORY; `DECISIONS.md` D-0019 (обоснование сужения scope ниже).
**Уточнение при формулировке (не цитата, замечено при проверке §16/§17):** полноценная реализация §16 — персистентная `Baseline`-модель с `scope`/историей версий (§17 — не уничтожать предыдущие) — заметно больше, чем «простой baseline, один шаг». Реализовано как вычисляемое на лету значение (тот же принцип, что весь Task 6.1 — ничего не персистится сверх `Content`/`PerformanceMetric`), только `scope: global`. Зафиксировано как YELLOW, `DECISIONS.md` D-0019 — не тихое сужение.
**Что входит:**
- [`src/analysis/metricRows.ts`](src/analysis/metricRows.ts) — общий `MetricRow`/`latestValuesByContent`/`average`, вынесены из `metricsAnalytics.ts`, чтобы `personalBaseline.ts` мог их переиспользовать без циклического импорта (Task 6.2 нужен `computeBaseline` внутри `summarizeMetric`, Task 6.1).
- [`src/analysis/personalBaseline.ts`](src/analysis/personalBaseline.ts) — `computeBaseline(rows, metricType)`: среднее по последнему известному значению метрики на публикацию, без ограничения периодом (вся история). `confidenceFromSampleSize`: `< 5` → `low`, `5–14` → `medium`, `15+` → `high` (заглушки, `DECISIONS.md` D-0019). `compareToBaseline(periodAverage, periodSampleSize, baseline)`: `above`/`below`/`at_baseline`/`insufficient_data`, относительный порог 10% (тот же принцип, что тренд); итоговый confidence — минимум из confidence baseline и confidence периода, не confidence одного baseline.
- `MetricSummary` (Task 6.1) дополнен `baseline`/`comparisonToBaseline`/`confidence` — доводит структуру результата почти до полного `42_IMPLEMENTATION_ROADMAP.md` §33 ANALYTICS_OUTPUT (metric/period/value/comparison/trend/confidence; `anomaly` — отдельное понятие, уже закрыто на уровне sync count в Task 5.2, не здесь).
**Явно не входит:** персистентная `Baseline`-сущность/история версий (§16/§17 — будущая задача, см. D-0019); non-global scope (по формату/теме/цели — естественное продолжение при Pattern Detection, Phase 7); UI-экран.
**Готово, когда:** `computeBaseline` считает среднее по всей истории, не по периоду; `compareToBaseline` корректно определяет `above`/`below`/`at_baseline`/`insufficient_data`; confidence не завышен, если мал ЛИБО baseline, ЛИБО текущий период (даже если другая сторона большая); `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/personal-baseline.test.ts`](tests/unit/personal-baseline.test.ts) — 16 тестов (пороги confidence; baseline без данных/по истории/только последний снимок/фильтр по metricType; сравнение — insufficient_data при пустом периоде или baseline, above/below/at_baseline, confidence не завышен ни при тонком периоде, ни при тонком baseline, high только когда обе стороны надёжны, нулевой baseline — валидная точка сравнения, не insufficient_data). [`tests/unit/metrics-analytics.test.ts`](tests/unit/metrics-analytics.test.ts) дополнен: пустой результат теперь также проверяет `comparisonToBaseline`/`confidence`; новый тест — baseline считается по всей истории, не ограничен периодом запроса. [`tests/integration/account-analytics.smoke.test.ts`](tests/integration/account-analytics.smoke.test.ts) дополнен: на масштабе 25 публикаций baseline/confidence корректны (`high`); отдельный тест на 2 публикациях — confidence `low`, буквально сценарий Olga «2–3 поста не должны давать уверенный вывод». `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (16 юнит + обновления — все зелёные; полный набор — 93/95, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 `Content`/`PerformanceMetric` от тестов.

**Phase 6 — Analytics Foundation считается завершённой** на объёме Task 6.1–6.2. Решение Olga (13 августа 2026), по итогам обсуждения оставшегося пункта `42_IMPLEMENTATION_ROADMAP.md` §33 ANALYTICS_OUTPUT — «anomaly». Критерий `§34 PHASE 6 COMPLETION` («для тестового пользователя система способна получить raw data → normalized data → deterministic analytics») выполнен и подтверждён живыми данными (Task 4.1) плюс полным тестовым покрытием (Task 6.1–6.2). Per-metric «anomaly» из §33 сознательно не реализован здесь — это не то же самое, что sync count anomaly из Task 5.2 (тот про здоровье ingestion, не про аномальность конкретного результата аналитики), и по смыслу описания (`26_DATA_PIPELINE.md` §29 POSITIVE_PATTERN/§30 NEGATIVE_PATTERN — градация `SLIGHTLY_BELOW_BASELINE`/`CLEARLY_WEAK`/`STRONG_FAILURE`, требующая повторяемости наблюдений во времени, не разовой проверки) — это работа Pattern Detection, Phase 7 (Knowledge Layer), не Analytics Foundation. **§29–31 зафиксированы как естественный дом для anomaly, когда начнётся Phase 7** — не расписывается заранее как отдельная задача, возвращается, когда до неё дойдёт очередь.

---

## Phase 7 — Knowledge Layer (42_IMPLEMENTATION_ROADMAP.md §35–39)

### Task 7.1 — Первое использование `Memory`: аналитика Task 6.x → персистентный knowledge item
**Цель:** первый шаг Knowledge Layer — превратить вычисляемый на лету вывод `getUserAnalytics` (Task 6.1/6.2) в персистентную запись, чтобы система могла ответить «что мы знаем об этом пользователе и почему» (`42_IMPLEMENTATION_ROADMAP.md` §39 PHASE 7 COMPLETION), не только вычислить это заново при каждом запросе.
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §35 PHASE 7, KNOWLEDGE LAYER (минимум: knowledge item schema/evidence references/timestamps/source/confidence/outcome/relevance), §39 PHASE 7 COMPLETION; `25_DATABASE_SCHEMA.md` §36 MEMORY (поля: memory_id/user_id/memory_type/content/confidence/source/created_at/updated_at/status — уже реализовано как есть в Task 1.1, впервые реально используется здесь), §37 MEMORY_EVIDENCE (не реализовано, см. находку ниже); `DECISIONS.md` D-0022 (обоснование частичного покрытия §35 ниже).
**Что входит:**
- [`src/knowledge/analyticsMemory.ts`](src/knowledge/analyticsMemory.ts) — `captureAnalyticsMemory(userId, period)`: для каждого `ExternalAccount` пользователя и каждой метрики (`reach`/`likes`/`saved`) из `getUserAnalytics` — если `comparisonToBaseline !== 'insufficient_data'` (есть что запомнить), создаёт `Memory` (`memoryType: 'fact'`, `source: 'analytics'`, `confidence` — числовая проекция категориальной шкалы `low`/`medium`/`high` → `0.2`/`0.5`/`0.8`, `DECISIONS.md` D-0022). `content` — читаемая строка с периодом/значением/нормой/сравнением/трендом (неформальный суррогат evidence references, не формальная связь). Snapshot-семантика — каждый вызов создаёт новые строки, не upsert (тот же принцип, что `PerformanceMetric`/`AccountSnapshot`).
- `memoryRepository.findByUserId` — первый метод чтения по пользователю (раньше был только `findById`).
- `POST /api/knowledge/capture?days=N` (session-protected, state-changing — пишет новые строки, поэтому `POST`, не `GET`, в отличие от `/api/analytics`).
**Найдено при формулировке:** `Memory`/`Pattern` существуют в схеме с Task 1.1 (10 core entities, D-0008), но ни разу не использовались ни одной задачей — Task 7.1 первый реальный writer. `42_IMPLEMENTATION_ROADMAP.md` §35 требует 7 полей у knowledge item; `Memory` (`25_DATABASE_SCHEMA.md` §36) покрывает 4 (schema/timestamps/source/confidence) — evidence references (`25_DATABASE_SCHEMA.md` §37 MEMORY_EVIDENCE, отдельная join-таблица, не входила в Task 1.1 — не путать с §37 в `42_IMPLEMENTATION_ROADMAP.md`, там это другой раздел, HISTORICAL DATA), outcome и relevance не покрыты вообще (нет таких полей на модели). Не тихий пропуск — зафиксировано как YELLOW, `DECISIONS.md` D-0022.
**Явно не входит:** §36 KNOWLEDGE_WEIGHTING (freshness/evidence strength/relevance/user-specific history), §37 HISTORICAL_DATA (decay весов старых данных), §38 USER_MEMORY (устойчивые preferences/decisions/patterns) — остальные пункты Phase 7, не эта задача; формальные evidence references (`MemoryEvidence`) — будущая задача, когда понадобится программная, не текстовая, связь `Memory` → `Content`/`Pattern`/`Recommendation`; UI-экран.
**Готово, когда:** `captureAnalyticsMemory` создаёт `Memory`-записи только для метрик с осмысленным сравнением (`insufficient_data` — пропуск, не пустая запись); повторный вызов создаёт новые строки, не перезаписывает; `confidence`/`source`/`memoryType` корректно проставлены; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/analytics-memory.test.ts`](tests/unit/analytics-memory.test.ts) — 2 теста на чистой `formatFactContent` (содержит все ключевые поля; разные метрики дают разные строки). [`tests/integration/analytics-memory.smoke.test.ts`](tests/integration/analytics-memory.smoke.test.ts) — 3 теста через реальную БД: пустой аккаунт → 0 `Memory`, 3 skipped; масштаб 20 публикаций → 3 `Memory` (по одной на метрику), корректные `memoryType`/`source`/`confidence` (`0.8` при `high`); повторный вызов создаёт новые строки (snapshot, не upsert — 2 вызова → 2 строки). `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован), `npm test` (5 новых тестов — все зелёные; полный набор — 98/100, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 `Content`/`PerformanceMetric`/`Memory` от тестов.

### Task 7.2 — Pattern Detection: первый шаг
**Цель:** первое использование `Pattern` (модель с Task 1.1, ни разу не использовалась) — простейшая содержательная закономерность: метрика конкретной публикации систематически выше/ниже личной нормы (`26_DATA_PIPELINE.md` §28 PATTERN_DETECTION/§29 POSITIVE_PATTERN/§30 NEGATIVE_PATTERN/§31 PATTERN_CONFIDENCE). Один шаг Pattern Detection, не вся тема.
**Спецификация:** `26_DATA_PIPELINE.md` §28 (пять видов закономерностей — реализованы два: повторяющиеся сильные/устойчивые слабые результаты), §29 (репрезентативность — один пост не создаёт вывод «всегда работает»), §30 (для отрицательных результатов — тот же принцип; градация `SLIGHTLY_BELOW_BASELINE`/`CLEARLY_WEAK`/`STRONG_FAILURE` не входит), §31 (confidence: sample size/consistency/effect size/recency/data quality); `22_DATA_MODEL.md` §15 PATTERN (поля), §16 PATTERN_EVIDENCE (не реализован), §17 PATTERN_STATUS/§18 PATTERN_DECAY/§19 PATTERN_REACTIVATION (жизненный цикл — не входит); `DECISIONS.md` D-0023 (полное обоснование сужений ниже).
**Что входит:**
- [`src/knowledge/patternDetection.ts`](src/knowledge/patternDetection.ts) — `detectMetricPattern(rows, metricType)` (чистая функция): для каждой публикации (не периода — гранулярность §29 буквально про «один пост») сравнение с personal baseline (Task 6.2) по тому же относительному порогу 10%, что и весь остальной код. Направление (`positive`/`negative`) определяется большинством при консистентности ≥60% (заглушка) и минимум 5 публикациях (тот же порядок, что D-0019). `confidence` = consistency × шкала объёма выборки (переиспользует `CONFIDENCE_SCORE` из Task 7.1). `strength` = средняя относительная величина отклонения среди подтверждающих публикаций.
- `detectPatterns(userId)` — оркестрация по `CORE_METRICS` (`reach`/`likes`/`saved`), **upsert** по `(userId, patternType)` — не snapshot, единственная эволюционирующая запись на закономерность (прямое следствие §18 PATTERN_DECAY — «старая информация не удаляется» внутри одной записи). `lastConfirmedAt` обновляется при каждом подтверждении, включая первое создание.
- `performanceMetricRepository.findRowsByUserId` — новый метод: Pattern не привязан к `ExternalAccount` (нет такого поля на модели, закономерность формулируется на уровне пользователя), join напрямую через `Content.userId`, не через объединение по всем аккаунтам.
- `patternRepository.findByUserId` — первый метод чтения по пользователю.
- `POST /api/knowledge/patterns` (session-protected, state-changing).
**Найдено при формулировке:** гранулярность Task 6.2 (personalBaseline) — период vs весь baseline; но §29 явно говорит про отдельный **пост**, не период — Pattern Detection не может просто переиспользовать период-усреднённое сравнение Task 6.2/7.1 один-в-один, нужна своя, более мелкая гранулярность. `22_DATA_MODEL.md` §15 PATTERN документирует поля «связанные признаки»/«связанные цели», которых на модели `Pattern` нет (не входили в Task 1.1) — тот же класс пробела, что у `Memory`/§35 в Task 7.1.
**Явно не входит:** жизненный цикл `status` (§17–19 — остаётся Prisma-дефолтом `hypothesis`, не эволюционирует, confidence не угасает при отсутствии подтверждения); комбинации признаков (нужен `ContentFeature`, не задействован); градация отрицательных результатов §30; формальные evidence references §16 (`description` — текстовый суррогат, не связь с `Content`); UI-экран.
**Готово, когда:** `detectMetricPattern` корректно определяет направление/пропускает при недостаточной выборке или консистентности (юнит-тесты на синтетических данных); `detectPatterns` создаёт `Pattern` при первом обнаружении и **обновляет ту же запись**, не создаёт вторую, при повторном прогоне (интеграционный тест на реальной БД); `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/pattern-detection.test.ts`](tests/unit/pattern-detection.test.ts) — 7 тестов (нет данных; ниже минимума выборки даже при идеальной консистентности — прямая проверка §29; чёткое большинство → positive/negative с ожидаемыми `matchingCount`/`consistency`/`strength`/`confidence`; ровно 50/50 → нет направления, ниже порога консистентности; больший объём выборки → выше confidence при сравнимой консистентности; фильтрация по `metricType`). [`tests/integration/pattern-detection.smoke.test.ts`](tests/integration/pattern-detection.smoke.test.ts) — 3 теста через реальную БД: пустой аккаунт → 0 `Pattern`; явное большинство (5 из 7) → `Pattern` создан, `direction: positive`, `status: hypothesis` (Prisma-дефолт, не тронут), `lastConfirmedAt` заполнен; повторный вызов **обновляет ту же запись** (тот же `id`, `lastConfirmedAt` не раньше первого), не создаёт вторую — прямая проверка upsert-семантики, отличной от Task 4.1/4.2/7.1. **Найденный и исправленный по ходу баг**: изначально `lastConfirmedAt` не устанавливался при первом создании `Pattern` (только при `update`) — интеграционный тест поймал `null` там, где ожидалась дата; исправлено — первое обнаружение тоже подтверждение, не только повторные. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован), `npm test` (10 новых тестов — все зелёные; полный набор — 108/110, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 `Content`/`PerformanceMetric`/`Pattern` от тестов.

### Task 7.3 — Единый ответ на «что мы знаем и почему» (Phase 7 completion)
**Цель:** закрыть буквальный критерий `42_IMPLEMENTATION_ROADMAP.md` §39 PHASE 7 COMPLETION — «система может ответить: что мы знаем об этом пользователе и почему мы это знаем». Найдено при оценке готовности фазы: `Memory` (Task 7.1) и `Pattern` (Task 7.2) существовали только за write-триггерами (`POST /api/knowledge/capture`/`POST /api/knowledge/patterns`), которые возвращают счётчики, не сами записи — не было ни одного способа реально спросить систему «что ты знаешь», кроме прямого обращения к БД в обход приложения.
**Спецификация:** `42_IMPLEMENTATION_ROADMAP.md` §39 PHASE 7 COMPLETION.
**Что входит:**
- [`src/knowledge/userKnowledge.ts`](src/knowledge/userKnowledge.ts) — `getUserKnowledge(userId)`: читает `Memory` + `Pattern` пользователя (`memoryRepository`/`patternRepository.findByUserId`, Task 7.1/7.2). Memory отфильтрован по `status: 'active'`, отсортирован по свежести (новые первыми). Pattern исключает `status: 'inactive'` (`22_DATA_MODEL.md` §17 PATTERN_STATUS — закономерность, помеченная неактивной, не должна выдаваться как текущее знание), отсортирован по `confidence` (увереннее — первыми).
- [`GET /api/knowledge`](src/app/api/knowledge/route.ts) (session-protected, read-only — в отличие от двух существующих `POST`-триггеров в том же неймспейсе).
**Явно не входит:** формальные evidence references (§16/§37 MEMORY_EVIDENCE, D-0022/D-0023 — «почему» здесь отвечается уже существующими `content`/`description`, не новой связью); AI-генерируемое резюме поверх списков (`42_IMPLEMENTATION_ROADMAP.md` §32 — не AI-слой, обычный код); UI-экран.
**Готово, когда:** `GET /api/knowledge` реально возвращает объединённый список `Memory`+`Pattern` пользователя; `inactive` Pattern и неактивный Memory не попадают в ответ; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/integration/user-knowledge.smoke.test.ts`](tests/integration/user-knowledge.smoke.test.ts) — 3 теста через реальную БД: пустой пользователь → пустые списки; несколько `Memory`/`Pattern` → верная сортировка (свежее/увереннее первыми) и верный набор (`inactive` Pattern исключён); архивный `Memory` (`status ≠ 'active'`) исключён. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован, конфликта с `/api/knowledge/capture`/`/api/knowledge/patterns` нет), `npm test` (3 новых теста — все зелёные; полный набор — 111/113, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 `Memory`/`Pattern` от тестов.

**Phase 7 — Knowledge Layer считается завершённой** на объёме Task 7.1–7.3. Критерий `§39 PHASE 7 COMPLETION` подтверждён напрямую — `GET /api/knowledge` реально отвечает на «что мы знаем об этом пользователе и почему». §36 KNOWLEDGE_WEIGHTING (freshness/evidence strength/relevance/user-specific history), §37 HISTORICAL_DATA (decay весов старых данных), §38 USER_MEMORY (устойчивые preferences/decisions), жизненный цикл Pattern (§17–19 из `22_DATA_MODEL.md`), формальные evidence references (§16/§37 MEMORY_EVIDENCE) — документированные, но сознательно не реализованные части темы (см. D-0022/D-0023), не входят в буквальный критерий §39 и не расписываются заранее как отдельные задачи — возвращаются, когда до них дойдёт очередь (естественно — при реализации Recommendation Engine, Phase 8, которому понадобится более точная работа со знанием, чем «прочитать список»).

---

## Phase 8 — Recommendation Engine (21_DECISION_LOGIC.md §9–19, 23_SYSTEM_ARCHITECTURE.md §23–29)

### Task 8.1 — CANDIDATE_GENERATOR + CANDIDATE_SCORER + RANKING_ENGINE, узкий первый шаг
**Цель:** первая реализация DECISION layer (`src/decision/`, до этой задачи — только README-заготовка с Task 0.2). Список кандидатов-форматов из реально существующих `Content.contentType` (не захардкожен); простой scorer поверх уже готовых Task 6.2 (baseline/comparison) и Task 7.2 (Pattern) — честно только то, для чего есть расчёт; ranking — упорядочить кандидатов по оценке. Не пишет в `Recommendation` — reasons/context/decision_version — следующий шаг.
**Спецификация:** `21_DECISION_LOGIC.md` §9 CANDIDATE_GENERATION/§10 CANDIDATE_RELEVANCE, §39 DECISION_SCORE/§40 NO FALSE PRECISION/§41 RANKING; `23_SYSTEM_ARCHITECTURE.md` §25 CANDIDATE_GENERATOR/§26 CANDIDATE_SCORER (9 факторов: goal fit/recent performance/historical performance/pattern strength/confidence/freshness/repetition/risk/opportunity)/§27 RANKING_ENGINE; `DECISIONS.md` D-0024 (обоснование частичного покрытия факторов ниже).
**Уточнение цитаты при формулировке:** Olga сослалась на «§36 требует reasons/context/decision_version» для будущей Recommendation-таблицы — при проверке эти поля оказались в `25_DATABASE_SCHEMA.md` §26 RECOMMENDATIONS (`decision_version`)/§28 RECOMMENDATION_REASONS (`reasons`)/§30 RECOMMENDATION_CONTEXT (`context`); §36 в этом документе — MEMORY (уже обсуждалось в D-0022/D-0023). Похоже на перенос номера из соседнего обсуждения — содержательный вывод (не писать в Recommendation сейчас) остаётся верным, поправлен только номер, см. `DECISIONS.md` D-0024.
**Что входит:**
- [`src/decision/candidateGenerator.ts`](src/decision/candidateGenerator.ts) — `generateCandidateFormats(content)`: distinct `Content.contentType`, не универсальный список форматов Instagram (тот же принцип, что `CORE_METRICS`/D-0018).
- [`src/decision/candidateScorer.ts`](src/decision/candidateScorer.ts) — `scoreAndRankCandidates`: для каждой из трёх метрик (`CORE_METRICS`) — среднее конкретного кандидата (формата) сравнивается с личной нормой (Task 6.2, `computeBaseline`/`compareToBaseline`, `scope: global`, не меняется — D-0019) тем же порогом 10%. Pattern (Task 7.2) — контекстная пометка в `label`, только когда направление кандидата совпадает с направлением уже подтверждённого паттерна, никогда не влияет на сортировку (Pattern глобален по метрике, не по формату — не может честно объяснять разницу между кандидатами). Только 3 из 9 факторов §26 реализованы (historical performance/сравнение с нормой/confidence) — остальные (goal fit/freshness/repetition/risk/opportunity) не посчитаны, нет данных. Результат — качественный `label` (например «above baseline, medium confidence»), не число (§40).
- [`src/decision/recommendationCandidates.ts`](src/decision/recommendationCandidates.ts) — `getRankedCandidates(userId)`, оркестрация, read-only.
- `contentRepository.findByUserId` — новый метод.
- `GET /api/decision/candidates`.
**Явно не входит:** запись в `Recommendation` (reasons/context/decision_version — `25_DATABASE_SCHEMA.md` §26/§28/§30, следующий шаг); goal fit/freshness/repetition/risk/opportunity (нет данных для расчёта); единый ranking across all metrics (без goal fit — придуманный вес, см. D-0024); UI-экран.
**Готово, когда:** кандидаты берутся только из реально существующих форматов пользователя; каждый кандидат сравнивается с личной нормой по каждой метрике отдельно, не одним общим числом; вывод — качественная метка, не число; `Recommendation`-таблица остаётся пустой после вызова; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/candidate-generator.test.ts`](tests/unit/candidate-generator.test.ts) — 3 теста (пусто; distinct-форматы; несуществующий формат не появляется). [`tests/unit/candidate-scorer.test.ts`](tests/unit/candidate-scorer.test.ts) — 7 тестов (пустые кандидаты; явное ранжирование above/below; кандидат без данных метрики — `insufficient_data`, в конце списка; label качественный, не число; пометка о паттерне только при совпадении направления; без пометки на `at_baseline`; метрики ранжируются независимо друг от друга — разные лидеры для reach/likes на одних и тех же кандидатах). [`tests/integration/recommendation-candidates.smoke.test.ts`](tests/integration/recommendation-candidates.smoke.test.ts) — 3 теста через реальную БД: пустой пользователь → пустые ranking; reel (высокий reach) ранжируется выше carousel (низкий reach), никогда не публиковавшийся формат не появляется; `Recommendation`-таблица остаётся пустой после вызова. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован), `npm test` (13 новых тестов — все зелёные; полный набор — 124/126, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 `Content`/`PerformanceMetric`/`Recommendation` от тестов.

### Task 8.2 — GOAL_FIRST/GOAL_PRIORITY: подключить реальные цели пользователя к скорингу
**Цель:** закрыть единственный фактор, который в Task 8.1 (D-0024) не давал свести per-metric рейтинги в одну честную рекомендацию — goal fit. Реальный приоритет целей пользователя (`Goal`, Task 1.1, до сих пор не использовалась ни одним кодом как список для решения) определяет, какая из уже посчитанных per-metric рейтингов первична. Явно, не молчаливым предположением, обработать случай отсутствия целей — не только для тестового пользователя.
**Спецификация:** `21_DECISION_LOGIC.md` §6 GOAL_FIRST (цель — первый фильтр), §7 GOAL_PRIORITY (относительный приоритет целей — пример: 1. followers, 2. reach, 3. saves, 4. likes; главная цель получает больший вес, остальные продолжают участвовать), §8 FOUR_RECOMMENDATION_DIRECTIONS (четыре направления likes/followers/saves/reach; «система определяет, какое из них сейчас наиболее актуально относительно целей»); `DECISIONS.md` D-0025 (обоснование ниже).
**Что входит:**
- [`src/decision/goalFit.ts`](src/decision/goalFit.ts) — `determineGoalFit(goals, trackedMetrics)`: сортирует активные цели по приоритету (меньшее число — выше важность, YELLOW-решение — направление поля не было установлено ни одним кодом ранее, D-0025), сопоставляет `goalType` с трекаемой метрикой через явную таблицу `GOAL_TYPE_TO_METRIC` (не совпадение строк напрямую — `saves`(документ)↔`saved`(Instagram API, D-0018) та же метрика, разное словоупотребление), выбирает первую (по приоритету) цель, которая сопоставляется с реально трекаемой метрикой (`CORE_METRICS`). **Выбор, не смешивание** — прямое продолжение D-0024: goal fit определяет, КАКОЙ из существующих Task 8.1 рейтингов первичен, не порождает новое смешанное число.
- Явные состояния: `no_goals_defined` (целей нет вовсе), `no_tracked_goals` (цели есть, но ни одна не сопоставляется с трекаемой метрикой — типичный случай `goalType: 'followers'`, followers_gained не собирается, D-0018), `goal_matched`. `untrackedGoals` — явный список проигнорированных целей, не молчаливый пропуск. Fallthrough по приоритету — нетрекаемая цель с высшим приоритетом не блокирует более низкоприоритетную трекаемую.
- `goalRepository.findByUserId` — новый метод.
- [`src/decision/recommendationCandidates.ts`](src/decision/recommendationCandidates.ts) — `getRankedCandidates` заменена на `getDecisionCandidates(userId)`: `{ goalFit, rankings, primaryRanking }`. `primaryRanking` — `null`, если goal fit не смог выбрать метрику; `rankings` (Task 8.1) в любом случае возвращаются целиком.
- `GET /api/decision/candidates` — ответ дополнен `goalFit`/`primaryRanking`.
**Уточнение цитаты при формулировке:** пересчитал `FOUR_RECOMMENDATION_DIRECTIONS` — `saves`/`likes`/`followers`/`reach` — против `CORE_METRICS` (`reach`/`likes`/`saved`, D-0018): `followers` не имеет соответствия вообще (followers_gained не собирается), `saves`≠`saved` буквально по строке (то же понятие, разное словоупотребление). Оба случая обработаны явно (`GOAL_TYPE_TO_METRIC`, `no_tracked_goals`), не тихим совпадением строк, которое просто не нашло бы `saves`.
**Явно не входит:** числовое смешивание метрик по весам приоритета (только выбор одной первичной — реальное смешивание создало бы новый риск false precision, §40, отдельное решение в будущем, не тихое расширение); freshness/repetition/risk/opportunity (по-прежнему нет данных, D-0024); запись в `Recommendation`; UI-экран.
**Готово, когда:** отсутствие целей и цели без трекаемой метрики дают явные, разные, не спутанные с «успехом» состояния; цель `saves` находит метрику `saved`; при нескольких целях выбирается по приоритету (меньшее число — важнее), с fallthrough на трекаемую цель при нетрекаемой более приоритетной; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/goal-fit.test.ts`](tests/unit/goal-fit.test.ts) — 9 тестов (нет целей; только неактивные цели → тоже `no_goals_defined`; одна цель совпадает; `saves`→`saved` алиас; меньший приоритет побеждает; `followers` → `no_tracked_goals` явно; fallthrough на трекаемую цель при нетрекаемой приоритетнее; несколько нетрекаемых целей — все перечислены; неактивные цели игнорируются даже при наличии активной трекаемой). [`tests/integration/recommendation-candidates.smoke.test.ts`](tests/integration/recommendation-candidates.smoke.test.ts) дополнен 4 тестами через реальную БД: цель `reach` → `primaryRanking` с реальным ranking'ом; цель `followers` → `no_tracked_goals`, per-metric рейтинги всё равно возвращены; fallthrough `followers`(приоритет 1, нетрекаема) → `reach`(приоритет 2, трекается). `npx tsc --noEmit`, `npm run lint`, `npm run build` (route обновлён), `npm test` (13 новых тестов + обновлённые Task 8.1-тесты — все зелёные; полный набор — 136/138, те же два заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 `Goal`/`Content`/`PerformanceMetric`/`Recommendation` от тестов.

### Task 8.3 — персистентность решения: запись Recommendation (reasons/context/decision_version)

**Цель:** закрыть цикл «решение → можно объяснить и найти позже» (прямая формулировка Olga) на уже готовых кандидатах Task 8.1/8.2 — без добора оставшихся факторов скоринга, это сознательно вне scope этого шага.
**Спецификация:** `25_DATABASE_SCHEMA.md` §26 RECOMMENDATIONS/§28 RECOMMENDATION_REASONS/§30 RECOMMENDATION_CONTEXT; `CLAUDE.md` §3 — цепочка объяснимости `Recommendation → Reason → Pattern → Evidence → Content → Performance`; `DECISIONS.md` D-0026 (обоснование ниже).
**Что входит:**
- `prisma/schema.prisma` — новая модель `RecommendationReason` (§28: id/recommendationId/reasonType/description/weight?/confidence?, `reasonType` — String, не enum, тот же принцип, что goalType/patternType); `Recommendation.context` (`Json?`, §30 — JSON-снимок, не отдельная таблица, т.к. документ не даёт список колонок и описывает единственный снимок на recommendation). Миграция `20260815001732_recommendation_reasons_and_context`, проверена полным реприменением истории на одноразовой БД `project_bootstrap_migration_check`.
- [`src/decision/candidateScorer.ts`](src/decision/candidateScorer.ts) — `CandidateResult.matchesPattern`/`MetricRanking.pattern` экспортированы как структурные поля (раньше совпадение с паттерном было видно только текстом в `label`) — нужны персистентности для отдельной RECOMMENDATION_REASON('pattern').
- [`src/decision/goalFit.ts`](src/decision/goalFit.ts) — `GoalLike`/`matchedGoal` дополнены `id` (нужен реальный `Goal.id` для обязательного `Recommendation.goalId`).
- [`src/decision/recommendationPersistence.ts`](src/decision/recommendationPersistence.ts) — `createRecommendation(userId)`: пишет `Recommendation` только при `goalFit.reason === 'goal_matched'` и реальном top-кандидате (не `insufficient_data`); честные `{ created: false, reason }` во всех остальных случаях (`no_goals_defined`/`no_tracked_goals`/`no_candidates`/`insufficient_data`) — не запись-заглушка. `DECISION_VERSION = 'decision-v1'` — первое реальное значение `decisionVersion`. Reasons: `goal` (описание выбора цели, confidence не заполняется — детерминированный выбор, не вероятностная оценка) + `baseline` (текст `label` из Task 8.1, confidence через уже существующий `CONFIDENCE_SCORE`, Task 7.1) + `pattern` (только если направление подтверждённого паттерна совпало, confidence — реальный `Pattern.confidence`). `weight` не заполняется нигде (§40 NO FALSE PRECISION — нет расчёта). `context` — снимок `{goal, primaryMetric, ranking, untrackedGoals, decisionVersion, skeletonVersion: null}`; `skeletonVersion` явно `null` — не имеет референта в кодовой базе (AI-слой не участвует). Append-only — каждый вызов создаёт новую строку.
- `recommendationRepository.ts` — `createWithReasons` (вложенный create с `include: {reasons: true}`), `findByUserId` (read-back, новейшие первыми) — "найти позже".
- `POST /api/decision/recommendations` (state-changing, session-protected, 200 и при `created: false` — честное состояние, не ошибка) + `GET /api/decision/recommendations` (read-only, session-protected).
**Явно не входит:** `RECOMMENDATION_CANDIDATES` (§27 — все кандидаты, участвовавшие в ranking, не только выбранный) и `RECOMMENDATION_EVIDENCE` (§29 — формальные ссылки на content/performance/pattern/baseline) — Olga процитировала только §26/§28/§30, scope сужен явно; оставшиеся 6 из 9 факторов CANDIDATE_SCORER (freshness/repetition/risk/opportunity + возможное числовое смешивание по весам приоритета) — по-прежнему нет расчёта, D-0024/D-0025.
**Готово, когда:** `Recommendation` создаётся только когда goal fit реально выбрал метрику и есть реальный кандидат с данными; во всех остальных честных состояниях — явный некреированный результат, не запись с придуманными полями; `RecommendationReason` содержит хотя бы `goal`+`baseline`, `pattern` — когда направление совпало; `context` читаем как самостоятельный JSON-снимок; повторный вызов создаёт новую строку, не перезаписывает; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев); полная история миграций переприменяется на одноразовой БД.
**Статус:** Завершено. [`tests/integration/recommendation-persistence.smoke.test.ts`](tests/integration/recommendation-persistence.smoke.test.ts) — 6 тестов через реальную БД (нет целей → не создано; только нетрекаемая цель → не создано; цель есть, контента нет → `no_candidates`; реальная цель+данные → `Recommendation`+`goal`/`baseline` reasons+читаемый `context`; подтверждённый совпадающий Pattern → отдельная `pattern`-причина с реальным confidence; повторный вызов → вторая строка, не апдейт). [`tests/unit/candidate-scorer.test.ts`](tests/unit/candidate-scorer.test.ts) дополнен проверкой `matchesPattern`/`pattern` структурно (не только через текст `label`). [`tests/unit/goal-fit.test.ts`](tests/unit/goal-fit.test.ts) — все 9 литералов дополнены `id`, добавлена проверка `matchedGoal.id`. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован) — без ошибок; `npm test` — 142/144 (6 новых тестов зелёные; те же два заранее известных live-сбоя, не регрессия); `npm run test:e2e` (12/12) — без регрессий; миграция переприменена на одноразовой `project_bootstrap_migration_check`, база удалена после проверки; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 тестовых строк в любой таблице после прогонов.

### Task 8.4 — CANDIDATE_SCORER: freshness + repetition

**Цель:** улучшить качество уже работающего цикла (Task 8.1–8.3) двумя из оставшихся факторов CANDIDATE_SCORER, а не расширять citation scope на §27/§29 — прямой выбор Olga. Один-два фактора за шаг, не все четыре сразу.
**Спецификация:** `21_DECISION_LOGIC.md` §12 FRESHNESS_WEIGHT/§13 RECENCY_OVERRIDE/§14 REPETITION_CONTROL; `23_SYSTEM_ARCHITECTURE.md` §26 CANDIDATE_SCORER; `DECISIONS.md` D-0027 (обоснование ниже).
**Что входит:**
- [`src/decision/candidateGenerator.ts`](src/decision/candidateGenerator.ts) — `mostRecentContentType(content)`: формат самой недавней публикации по ВСЕМ форматам разом (факт о публикационном поведении, не per-metric).
- [`src/decision/candidateScorer.ts`](src/decision/candidateScorer.ts) — `CandidateResult.freshness: 'recent'|'aging'|'stale'|null` — буквальные пороги §12 (0–3/3–6/6+ месяцев) от самой свежей публикации, вносящей значение в кандидата; `null`, если у кандидата вообще нет данных по метрике (не путать со 'stale'). `CandidateResult.repetitionNote: string|null` — заполняется только на топ-кандидате ranking'а, только когда он совпадает с `mostRecentContentType` И есть конкурентная альтернатива (следующий по рангу кандидат не хуже `at_baseline`) — §13/§14 реализованы как ОДИН механизм (один и тот же пример в обоих разделах документа). Ни freshness, ни repetition НЕ меняют порядок `ranking` — качественные сигналы, не скрытый вес (§40, продолжение D-0024/D-0025).
- [`src/decision/recommendationCandidates.ts`](src/decision/recommendationCandidates.ts) — прокидывает `mostRecentContentType(content)` в `scoreAndRankCandidates`.
- [`src/decision/recommendationPersistence.ts`](src/decision/recommendationPersistence.ts) — новая `RecommendationReason(reasonType: 'freshness')`, когда у top-кандидата есть freshness-метка (реальный пункт словаря §28, confidence `null` — категориальный бакет, не вероятность). `repetitionNote` НЕ становится причиной — «repetition» отсутствует в словаре `reasonType`, который даёт сам документ.
**Явно не входит:** decay-переоценка самого `computeBaseline` (Task 6.2/D-0019) по возрасту данных — §12 буквально об этом, но это отдельное, более крупное решение, затрагивающее ANALYSIS layer, не только DECISION; risk/opportunity (§26 RISK/§27 OPPORTUNITY — нужен «experimental candidate»/hypothesis status, которого нет ни в схеме, ни в текущем CANDIDATE_GENERATOR); `RECOMMENDATION_CANDIDATES`/`RECOMMENDATION_EVIDENCE` (§27/§29 из `25_DATABASE_SCHEMA.md`) — Olga явно выбрала не эти факторы; числовое смешивание метрик.
**Готово, когда:** freshness-метка соответствует буквальным порогам §12 и не путает «нет данных» со «старые данные»; repetitionNote появляется только на топ-кандидате, только при совпадении с последним опубликованным форматом и наличии конкурентной альтернативы, и никогда не переставляет `ranking`; persisted `Recommendation` получает `freshness`-причину, когда она есть; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/candidate-generator.test.ts`](tests/unit/candidate-generator.test.ts) дополнен 4 тестами на `mostRecentContentType` (пусто; нет publishedAt нигде; выбор самого свежего по всем форматам; смешанные null/не-null). [`tests/unit/candidate-scorer.test.ts`](tests/unit/candidate-scorer.test.ts) дополнен 9 тестами (freshness: recent/aging/stale по порогам §12, самый свежий контрибьютор — не самый старый, `null` при отсутствии данных; repetition: пометка при конкурентной альтернативе, отсутствие пометки без альтернативы, отсутствие пометки при несовпадении с последним форматом, дефолт без `mostRecentContentType`). [`tests/integration/recommendation-persistence.smoke.test.ts`](tests/integration/recommendation-persistence.smoke.test.ts) обновлён — реальный сценарий теперь содержит 3 reasons (`goal`/`baseline`/`freshness`), добавлена явная проверка содержимого freshness-причины. `npx tsc --noEmit`, `npm run lint`, `npm run build` — без ошибок; `npm test` — 155/157 (13 новых тестов зелёные; те же два заранее известных live-сбоя, не регрессия); `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 тестовых строк в любой таблице после прогонов.

**Phase 8 — Recommendation Engine считается завершённой** на объёме Task 8.1–8.4. Критерий `42_IMPLEMENTATION_ROADMAP.md` §44 PHASE 8 COMPLETION («для тестового пользователя система способна создать: data → insight → recommendation, без необходимости AI») подтверждён напрямую и end-to-end, реальным интеграционным тестом (`recommendation-persistence.smoke.test.ts`): реальные `Content`/`PerformanceMetric` (data) → `computeBaseline` (Task 6.2)/`Pattern` (Task 7.2) как insight → персистентный `Recommendation`+`RecommendationReason` (Task 8.3) как recommendation — полностью детерминированно, ни один шаг цепочки не обращается к AI (`AI_SERVICE`/`AIProvider` не импортируются нигде в `src/decision/`). Проверено перед закрытием: **найден честный пробел** — `Memory` (Task 7.1, Knowledge Layer) физически не читается Decision Engine (`recommendationCandidates.ts` использует `patternRepository`, но нигде не импортирует `memoryRepository`) — единственная часть §41 RECOMMENDATION_INPUT «knowledge», которая не дошла до реального decision-пайплайна, хотя другие виды insight (Baseline/Pattern) дошли. Это не блокирует буквальный критерий §44 (там сказано «insight», не «Memory» персонально), но зафиксировано явно, не обнаружено молча — естественный кандидат на будущее, когда Decision Engine станет богаче входными данными. Остальное, документированное, но сознательно не реализованное на этом объёме: §41 RECOMMENDATION_INPUT (recent/historical context, previous decisions/`UserDecision` не консультируется), §42 RECOMMENDATION_OUTPUT (`expected effect`/`risks`/формальные `source references` — RECOMMENDATION_EVIDENCE §29 дало бы это честно), `RECOMMENDATION_CANDIDATES`/`RECOMMENDATION_EVIDENCE` (§27/§29, D-0026), risk/opportunity и FRESHNESS_WEIGHT-decay для `computeBaseline` (D-0027), числовое смешивание метрик по приоритету (D-0025). Решение Olga (15 августа 2026) — перейти к Phase 9, AI Layer; более тонкие доработки Recommendation Engine (risk/opportunity и подобные) естественно продолжатся уже в контексте объяснений от AI, не как отдельные технические доработки самого движка.

---

## Phase 9 — AI Layer (`29_AI_LAYER.md`, `42_IMPLEMENTATION_ROADMAP.md` §45–51)

### Task 9.1 — AI_SERVICE → AIProvider: decision explanation

**Цель:** первая реальная задача AI Layer — не строить AI-инфраструктуру саму по себе (§73 КРИТИЧЕСКОЕ ПРАВИЛО), а сразу привязать `AIService`/`AIProvider` (существуют с Task 0.2, ни разу не вызывались ни одним domain-кодом) к одной конкретной полезной задаче: генерация объяснения для уже сохранённой (Task 8.3) `Recommendation`.
**Спецификация:** `21_DECISION_LOGIC.md` §50 DECISION EXPLANATION (структура Recommendation → Why now → Evidence → Expected benefit → Uncertainty); `29_AI_LAYER.md` §11 STRUCTURED OUTPUT/§18-20 AI CONTEXT/CONTEXT_BUILDER/EVIDENCE-FIRST/§24 AI RUN/§26 AI OUTPUT VALIDATION/§34-35 AI FOR EXPLANATIONS/EXPLANATION MUST NOT INVENT/§67-68 AI DOES NOT CONTROL DATABASE/DOMAIN STATE/§73-76 КРИТИЧЕСКИЕ ПРАВИЛА 1-4; `DECISIONS.md` D-0028 (обоснование ниже).
**Уточнение цитаты при формулировке:** Olga сослалась на «§50 DECISION_EXPLANATION» в контексте `29_AI_LAYER.md` (главный документ Phase 9) — при проверке §50 в этом документе оказался PROVIDER FAILURE, другой раздел. Точная структура и пример нашлись в `21_DECISION_LOGIC.md` §50 DECISION EXPLANATION — тот же номер, другой документ, вероятно перенос номера из соседнего обсуждения. Содержательно формулировка Olga точная, поправлен только документ, см. `DECISIONS.md` D-0028.
**Что входит:**
- `prisma/schema.prisma` — новая модель `AiRun` (§24 AI RUN/§76: provider/model/promptVersion/status/output/inputTokens/outputTokens/latencyMs/createdAt) + enum `AiRunStatus` (закрытый набор из контракта `AIProvider`, не документа); `aiRuns` relation на `Recommendation`. Миграция `20260815092612_ai_runs`, проверена полным реприменением истории (7 миграций) на одноразовой БД.
- [`src/ai/decisionExplanationPrompt.ts`](src/ai/decisionExplanationPrompt.ts) — `buildExplanationPrompt`: собирает промпт из уже сохранённых `RecommendationReason` (evidence, §20 EVIDENCE-FIRST — конкретные данные, не пересказ) и `context.ranking` (Task 8.3) — качественная метка confidence (`low`/`medium`/`high`), не голое число `Recommendation.confidence` (риск псевдо-статистики, §35). Системный промпт прямо требует: не придумывать факты, не утверждать больше уверенности, чем даёт confidence, ровно 4 строковых JSON-поля на выходе.
- [`src/ai/decisionExplanationValidation.ts`](src/ai/decisionExplanationValidation.ts) — `parseExplanation`: структурная валидация (§11/§26) — 4 обязательных непустых строковых поля, снимает возможный ```json code fence. Не проверяет честность содержания текста — это ответственность промпта.
- [`src/ai/decisionExplanation.ts`](src/ai/decisionExplanation.ts) — `generateDecisionExplanation(recommendationId, userId, generate = AIService.generate)`: читает `Recommendation`+`reasons` (никогда не пишет в неё — §67/§68), вызывает `AIService.generate`, валидирует, пишет `AiRun` при ЛЮБОМ исходе (`completed`/`failed`/`timeout`/`provider_unavailable`/`validation_failed`). Чужая/несуществующая рекомендация → `not_found`, AI не вызывается вовсе (тот же принцип, что Task 2.3 Goal ownership). `generate` — DI-параметр, не мок модуля — реальный `AIService` недетерминирован и стоит денег.
- `recommendationRepository.findByIdWithReasons`, `aiRunRepository.create` — новые методы.
- `POST /api/decision/recommendations/[id]/explain` — state-changing (создаёт `AiRun`, реальный AI-запрос), session-protected; честные `failed`/`validation_failed`/`provider_unavailable` — 200 с описанием, не HTTP-ошибка; `not_found` — 404 (чужая/несуществующая рекомендация неразличимы).
**Явно не входит:** отдельный Prompt Registry (§21 — один промпт на одну задачу пока достаточно); отдельный AI_CONTEXT builder модуль (§19 — сборка контекста пока часть `buildExplanationPrompt`); SDK-специфичный structured-output/tool-use режим Anthropic (JSON-через-промпт остаётся provider-agnostic, §7/§75); любые другие AI-задачи (content analysis §31, pattern interpretation §32) — не входят, отдельные будущие шаги; изменение `AIProvider`/`AIService`/`providers/anthropic.ts` — контракт Task 0.2 уже подошёл без изменений.
**Готово, когда:** объяснение генерируется по структуре §50 (Why now/Evidence/Expected benefit/Uncertainty) на основе реальных `RecommendationReason`, не выдуманных фактов; confidence в промпте качественный, не выдуманное число; `AiRun` создаётся при любом исходе (успех и неуспех), с provider/model/promptVersion/timestamp; невалидный AI output не попадает в `Recommendation`/бизнес-логику как настоящее объяснение; `Recommendation`/`RecommendationReason`/`Goal`/`Pattern` не изменяются этим кодом ни при каком исходе; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` проходят (не считая двух заранее известных live-сбоев); полная история миграций переприменяется на одноразовой БД.
**Статус:** Завершено. [`tests/unit/decision-explanation-prompt.test.ts`](tests/unit/decision-explanation-prompt.test.ts) — 6 тестов (реальные данные в промпте; качественный, не числовой confidence; fallback на "unknown"; устойчивость к отсутствующему/битому context; плейсхолдер при пустых reasons; явный запрет выдумывать факты/переоценивать уверенность в системном промпте). [`tests/unit/decision-explanation-validation.test.ts`](tests/unit/decision-explanation-validation.test.ts) — 11 тестов (валидный JSON; code fence с/без "json"; не-JSON текст; пропущенное поле; пустая строка; неверный тип; массив вместо объекта; null; обрезка пробелов; лишние поля не мешают). [`tests/integration/decision-explanation.smoke.test.ts`](tests/integration/decision-explanation.smoke.test.ts) — 5 тестов против реальной БД с фиктивным `generate` (несуществующая рекомендация → `not_found`, AI не вызван; чужая рекомендация → `not_found`, 0 `AiRun`; успешная генерация + `Recommendation` доказуемо не изменилась до/после; невалидный JSON → `validation_failed` с сырым текстом; сбой provider → персистентный статус, объяснение не выдумывается). [`tests/integration/decision-explanation-live.smoke.test.ts`](tests/integration/decision-explanation-live.smoke.test.ts) — живой прогон против настоящего Anthropic API, `describe.skipIf(!ANTHROPIC_API_KEY)`, тот же принцип, что Instagram (Task 3.2); пропущен в этом окружении (ключ не задан локально). `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован) — без ошибок; `npm test` — 177/180 (22 новых теста зелёные, 1 живой тест пропущен; те же два заранее известных live-сбоя, не регрессия); `npm run test:e2e` (12/12) — без регрессий; миграция переприменена на одноразовой `project_bootstrap_migration_check`, база удалена после проверки; dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 тестовых строк в любой таблице после прогонов.

### Task 9.2 — экран отображения AI-объяснения рекомендации

**Цель:** простой UI-экран для результата Task 9.1 — вызывает уже существующий `POST /api/decision/recommendations/[id]/explain`, показывает результат по структуре §50 (Why now/Evidence/Expected benefit/Uncertainty). Честно показывает состояние, когда `ANTHROPIC_API_KEY` не настроен (прямое требование Olga), не притворяется, что всё работает.
**Спецификация:** `21_DECISION_LOGIC.md` §50 DECISION EXPLANATION; тот же UI-паттерн, что `/integrations` (Task 3.4) — server component читает данные напрямую через repository, клиентская кнопка вызывает существующий API route.
**Что входит:**
- [`src/app/recommendations/[id]/page.tsx`](src/app/recommendations/[id]/page.tsx) — server component: `auth()` → «Войдите» для неаутентифицированных (без раскрытия существования рекомендации); `recommendationRepository.findByIdWithReasons(id)` напрямую (без нового `GET`-эндпоинта — тот же паттерн, что `/integrations`); чужая/несуществующая рекомендация → одно и то же «Рекомендация не найдена.» (тот же принцип, что Task 2.3/Task 9.1 API).
- [`src/app/recommendations/[id]/ExplainButton.tsx`](src/app/recommendations/[id]/ExplainButton.tsx) — client component (`'use client'`), тот же паттерн, что `SyncButton`/`DisconnectButton` (Task 3.4/4.1): по клику `POST` на существующий `explain`-route, рендерит результат. `status: 'completed'` → 4 секции по структуре §50. Любой другой статус (`failed`/`timeout`/`provider_unavailable`/`validation_failed`) → честное сообщение конкретно под этот исход, не общий «что-то пошло не так».
- [`src/app/recommendations/[id]/explainStatusMessages.ts`](src/app/recommendations/[id]/explainStatusMessages.ts) — чистая `formatExplainStatusMessage(status)`, вынесена из компонента для юнит-тестируемости (тот же принцип, что весь проект: логика форматирования тестируется без рендеринга React). `provider_unavailable` — практически гарантированный исход в этом окружении (нет `ANTHROPIC_API_KEY` ни локально, ни в CI) — сообщение прямо называет вероятную причину, не выдаёт непонятную ошибку.
- `.claude/launch.json` — dev-сервер конфигурация для браузерной проверки (появилась впервые в этой задаче, полезна для будущей UI-работы).
**Явно не входит:** кнопка НЕ нажимается в e2e-тестах — реальный внешний AI-вызов, тот же принцип, что кнопка «Синхронизировать» (`integrations-instagram.spec.ts`, Task 3.4) никогда не нажимается в e2e; список всех рекомендаций пользователя (экран открывается по конкретному id, `GET /api/decision/recommendations` для списка уже есть с Task 8.3, но UI для списка — не в этом шаге); отображение сырого `Recommendation.confidence` (числовое приближение, §40 NO FALSE PRECISION — тот же принцип, что весь проект, не показывается пользователю напрямую).
**Готово, когда:** неаутентифицированный посетитель видит приглашение войти, не данные рекомендации; чужая и несуществующая рекомендация дают одно и то же сообщение; владелец видит формат и кнопку; при отсутствии `ANTHROPIC_API_KEY` клик по кнопке показывает конкретное честное сообщение об этом, не тихую ошибку и не выдуманный успех — **подтверждено вручную в реальном браузере** (не только тестами): зарегистрирован тестовый пользователь, реально засеяна `Recommendation`, кнопка нажата, на экране появилось «AI-провайдер сейчас недоступен — скорее всего, не настроен ключ API. Объяснение не сгенерировано.»; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test`/`npm run test:e2e` проходят (не считая двух заранее известных live-сбоев).
**Статус:** Завершено. [`tests/unit/explain-status-messages.test.ts`](tests/unit/explain-status-messages.test.ts) — 3 теста (честная формулировка для `provider_unavailable`; у каждого статуса своё сообщение, не общий; `validation_failed` не звучит как успех). [`tests/e2e/recommendation-explanation.spec.ts`](tests/e2e/recommendation-explanation.spec.ts) — 4 теста через реальный браузер и реальную БД (неаутентифицированный → «Войдите»; несуществующий id → «не найдена»; чужая рекомендация → то же «не найдена», два независимых `browser.newContext()`; владелец видит формат и кнопку — кнопка не нажимается). Ручная проверка в браузере (см. «Готово, когда») дополнительно подтвердила весь путь клика по кнопке, включая честное сообщение при отсутствующем ключе — то, что e2e намеренно не проверяет. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новая страница `/recommendations/[id]` зарегистрирована) — без ошибок; `npm test` — 180/183 (3 новых юнит-теста зелёные; 1 живой AI-тест пропущен без ключа, те же два заранее известных live-сбоя Meta API — не регрессия); `npm run test:e2e` — 16/16 (4 новых, без регрессий); dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 тестовых/ручных строк в любой таблице после прогонов и после ручной проверки.

### Task 9.3 — `ContentKnowledge`: внешняя база приёмов создания контента (первые записи)

**Цель:** «вторая AI-задача» после Task 9.1 — не хардкодить приёмы создания контента в промпт/код, а спроектировать таблицу для их хранения отдельными записями, которые AI при генерации текста читает из базы. Загрузить содержимое `Полное руководство.pdf` и `Полное руководство дополнительно.pdf` (личные файлы Olga, не в репозитории) как первые записи. Цель: будущее пополнение базы — дешёвая операция (добавить запись), не переписывание кода.
**Спецификация:** прямое указание Olga; `29_AI_LAYER.md` §33 AI FOR RECOMMENDATIONS («генерации контента» как легитимное применение AI) — единственный существующий якорь в документации, отдельного раздела про базу приёмов создания контента нет ни в одном документе `/docs`; `DECISIONS.md` D-0030 (обоснование ниже).
**Что входит:**
- `prisma/schema.prisma` — новая модель `ContentKnowledge` (id/category/title/content/source/sourceSection/status/createdAt/updatedAt), НЕ привязана к `userId` (внешнее ремесленное знание, не производное от данных конкретного пользователя, в отличие от `Memory`/`Pattern`). `category` — `String`, не enum (открытый список, тот же принцип, что `goalType`/`contentType`). Миграция `20260815105642_content_knowledge`, проверена полным реприменением истории (8 миграций) на одноразовой БД.
- [`src/data/repositories/contentKnowledgeRepository.ts`](src/data/repositories/contentKnowledgeRepository.ts) — `create`/`findById`/`findActive(category?)` (то, что AI при генерации текста будет читать вместо захардкоженного текста)/`findBySourceAndTitle` (для идемпотентного seed).
- [`prisma/contentKnowledgeSeedData.ts`](prisma/contentKnowledgeSeedData.ts) — чистые данные (15 записей, без побочных эффектов) + [`prisma/seedContentKnowledge.ts`](prisma/seedContentKnowledge.ts) — идемпотентный раннер (`npm run seed:content-knowledge`, проверка по `source`+`title` перед вставкой). `tsx` добавлен как dev-зависимость для запуска standalone TS-скриптов.
- 15 загруженных записей: 1 `content_strategy` (фундамент стратегии) + 7 `headline_rule` (все 7 правил заголовков из "доп. файла") + 6 `hook_template` (Понедельник–Четверг, Суббота, Воскресенье — недельная система хуков из Раздела 1 "Полное руководство.pdf") + 1 `posting_schedule` (Пятница — день без публикаций).
**Явно не входит:** Раздел 2 (тренды монтажа), Раздел 3 (стратегия платформ) и весь остаток "Полное руководство.pdf" (130 страниц — загружен только Раздел 1) — сознательно частичная загрузка, честно зафиксирована, не тихо пропущена, см. `DECISIONS.md` D-0030; Раздел 2 "доп. файла" (сезонные идеи "на примере июля") — привязан к месяцу, не переиспользуемый приём; сам AI-код, который читает `ContentKnowledge` при генерации текста (промпт/эндпоинт/structured output/AiRun-логирование по аналогии с Task 9.1) — не реализован этим шагом, только механизм чтения (`findActive`), которым будущая задача сможет воспользоваться (§73 КРИТИЧЕСКОЕ ПРАВИЛО — не строить AI-инфраструктуру без реальной задачи, которая её использует прямо сейчас, тот же принцип, что D-0028); retrieval/ранжирование релевантности записей — не реализовано, простой `findActive`/`findActive(category)`.
**Готово, когда:** таблица спроектирована и обоснована (YELLOW, D-0030); первые записи реально загружены из обоих файлов и читаемы через repository; повторный запуск seed-скрипта не создаёт дублей; текст приёмов не зашит в код/промпт — только в БД; `npx tsc --noEmit`/`npm run lint`/`npm run build`/`npm test`/`npm run test:e2e` проходят; полная история миграций переприменяется на одноразовой БД.
**Статус:** Завершено. [`tests/unit/content-knowledge-seed-data.test.ts`](tests/unit/content-knowledge-seed-data.test.ts) — 6 тестов на чистых данных, без БД (≥15 записей; все поля непустые; нет дублей `source`+`title`; записи из обоих файлов; все 7 правил заголовков; все 7 дней недельной системы хуков). [`tests/integration/content-knowledge.smoke.test.ts`](tests/integration/content-knowledge.smoke.test.ts) — 4 теста против реальной БД на синтетических данных (создание+чтение по id; `findActive` исключает `archived`; `findActive(category)` фильтрует; `findBySourceAndTitle` для идемпотентности). Seed реально запущен на dev-БД `olga`, дважды подряд (идемпотентность подтверждена: второй запуск — 0 создано/15 пропущено), проверено `psql` по категориям (content_strategy: 1, headline_rule: 7, hook_template: 6, posting_schedule: 1). `npx tsc --noEmit`, `npm run lint`, `npm run build` — без ошибок; `npm test` — 192/193 (10 новых тестов зелёные; 1 живой AI-тест пропущен без ключа, **оба ранее заблокированных live Instagram-теста прошли реально** — см. «Заблокировано», не регрессия и не относится к Task 9.3 напрямую); `npm run test:e2e` — 16/16; миграция переприменена на одноразовой `project_bootstrap_migration_check`, база удалена после проверки; dev-БД `olga` после прогонов — 1 user/1 externalAccount (реальный аккаунт Olga), 15 реальных `content_knowledge` (не тестовые — намеренно оставлены), 0 прочих тестовых строк.

---

## Backlog

### Консолидация пересекающихся документов
**Причина отсрочки:** не блокирует реализацию, противоречий не найдено, только избыточность изложения (см. `DECISIONS.md`, D-0002).
- Объединить `35_TESTING_QUALITY_CONTROL.md` + `43_TESTING_AND_QUALITY_ASSURANCE.md`
- Объединить `31_INFRASTRUCTURE_DEPLOYMENT.md` + `37_DEPLOYMENT_RELEASE_STRATEGY.md`
- Объединить `20_USER_JOURNEY.md` + `33_UX_USER_FLOWS.md`
- `42_IMPLEMENTATION_ROADMAP.md` §6 использует обобщённую терминологию (RawData/NormalizedData/Insight/KnowledgeItem), не совпадающую дословно с 18/22_DATA_MODEL.md — вероятно, разный уровень абстракции одного и того же, не отдельная схема; свериться при необходимости, не блокирует.

### First.md
**Причина отсрочки:** требует решения Olga — наполнить или удалить (удаление файла из репозитория — не техническое решение по умолчанию).

### Реальная staging-инфраструктура (Vercel/Neon/Upstash/R2)
**Причина отсрочки:** продуктовое решение Olga, не техническое (см. `DECISIONS.md`, D-0007). Локальная разработка Phase 1 не требует внешних облачных аккаунтов. Вернуться к этому пункту перед первым реальным демо, внешним доступом или интеграцией, требующей публичного URL/webhook (например, Instagram OAuth, Phase 3).
- Создать проект Vercel, ветки/базы Neon, инстанс Upstash, bucket R2 для staging и production по стратегии в `ENVIRONMENTS.md`
- Настроить branch protection для `main` в GitHub Settings (открытый пункт с Task 0.4)
