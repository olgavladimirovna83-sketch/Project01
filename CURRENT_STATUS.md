# CURRENT_STATUS.md

**Обновлено:** 15 августа 2026 (Task 9.12)

## Текущая фаза
**Phase 0 — Project Foundation: завершена** (Task 0.1–0.6). **Phase 1 — Database Foundation: завершена** (Task 1.1–1.2), кроме критерия «connected account» (сознательно отложен до Phase 3/Task 3.0). Критерий Task 0.6 «deploy в staging» сознательно отложен — продуктовое решение Olga (`DECISIONS.md`, D-0007), не блокирует Phase 1. **Phase 2 — Authentication: завершена** (Task 2.1–2.3) — критерий `42_IMPLEMENTATION_ROADMAP.md` §11 подтверждён e2e-тестами через реальный браузер, включая ownership-check за пределами `User`. Account recovery и role model сознательно вне MVP-scope, решение Olga от 12 августа 2026 (см. `TASKS.md`). **Phase 3 — предварительное условие: Task 3.0 завершена** (research-only, [`INSTAGRAM_API_REVIEW.md`](INSTAGRAM_API_REVIEW.md)). **Task 3.1 завершена** — контракт `IntegrationProvider`/`IntegrationService` спроектирован (`src/integrations/`). **Task 3.2 полностью завершена, включая реальный browser OAuth round trip** — `getAuthorizationUrl` → консент-экран → `code` → `exchangeCodeForTokens` прошёл чисто с первой попытки через настоящий Instagram, токен живёт ~60 дней; `getAccountInsights` дважды подтверждён живыми данными аккаунта Olga. **Task 3.3 завершена** — `ExternalAccount` (data layer) сохраняет результат подключения, callback route теперь реально пишет в БД, не только показывает. **Task 3.4 полностью завершена, включая финальную ручную проверку** — экран `/integrations` (подключить/статус/отключить); реальный consent screen показал верный аккаунт (`@nikodine`, один из нескольких связанных у Olga), отключение вернуло статус в «не подключён». **Phase 4 — Ingestion Pipeline: Task 4.1 завершена, включая живую проверку** — синхронизация Instagram → `Content`/`PerformanceMetric` реально прогнана на аккаунте Olga: 25 публикаций, 175 метрик, 0 предупреждений; повторный прогон подтвердил upsert без дублей + новые snapshot-метрики. **Task 4.2 завершена, включая живую проверку** — account-level метрики (`getAccountInsights`) теперь персистятся в новую сущность `AccountSnapshot` (`DECISIONS.md` D-0014), а не только показываются в summary; найденный по ходу пробел в документированной схеме (`25_DATABASE_SCHEMA.md` не описывает account-level raw-хранилище) зафиксирован в новом `DOCUMENT_CROSS_REFERENCE.md`. **Task 4.3 завершена** — устойчивость к сбоям для `syncInstagramAccount` по критерию `42_IMPLEMENTATION_ROADMAP.md` §24 RETRIES: timeout на HTTP-запросах (`fetchWithTimeout`), retry с exponential backoff и maximum attempts (`src/ingestion/retry.ts`) на все три сетевых вызова к Instagram внутри sync, явное состояние failure (`SyncWarning.attempts`) при исчерпании попыток — без очереди, прямо внутри существующей синхронной функции. **Phase 4 — Ingestion Pipeline: полностью завершена** (Task 4.1–4.3). **Phase 5 — Data Quality: полностью завершена** (Task 5.1–5.3) — `getDataQualityStatus`/`GET /api/data-quality` (`42_IMPLEMENTATION_ROADMAP.md` §28–29, §56–57) сводят `ExternalAccount.status`/`lastSyncedAt`, `AccountSnapshot`/`PerformanceMetric` timestamps в freshness/gaps/failure статус (Task 5.1, `DECISIONS.md` D-0015), completeness ratio и sync count anomaly detection (Task 5.2, `DECISIONS.md` D-0016), temporal consistency + schema invariants (Task 5.3, `DECISIONS.md` D-0017 — «consistency» из §27 нигде не расшифрована в `/docs`, реализация по явному рабочему предложению Olga, не по цитате) — весь Phase 5 без сбора новых данных и без обращений к Instagram API. **Phase 6 — Analytics Foundation: Task 6.1 завершена** — первая реализация ANALYSIS layer (`src/analysis/`), детерминированная (не AI) аналитика по трём основным метрикам (`reach`/`likes`/`saved`) поверх уже сохранённых `Content`/`PerformanceMetric`: сумма/среднее/тренд за период (`42_IMPLEMENTATION_ROADMAP.md` §31–34, `DECISIONS.md` D-0018). **Task 6.2 завершена** — personal baseline (среднее по всей истории, `scope: global`, вычисляется на лету, не персистится) + сравнение периода с нормой (`above`/`below`/`at_baseline`) + confidence по объёму выборки, минимум из confidence baseline и confidence периода (`22_DATA_MODEL.md` §13–14, `25_DATABASE_SCHEMA.md` §16, `DECISIONS.md` D-0019). **Phase 6 — Analytics Foundation закрыта на объёме Task 6.1–6.2** (решение Olga, 13 августа 2026) — критерий §34 выполнен, per-metric «anomaly» из §33 сознательно не реализован (не то же самое, что sync count anomaly Task 5.2 — это Pattern Detection, §29–31 зафиксированы как её будущий дом, Phase 7). **Phase 7 — Knowledge Layer: Task 7.1 завершена** — первое реальное использование `Memory` (модель с Task 1.1, не использовалась ни разу): `captureAnalyticsMemory` превращает вывод Task 6.1/6.2 в персистентные `fact`-записи (`42_IMPLEMENTATION_ROADMAP.md` §35, `DECISIONS.md` D-0022 — покрыты 4 из 7 полей §35, evidence references/outcome/relevance не покрыты). **Task 7.2 завершена** — первое использование `Pattern`: `detectPatterns` находит простую частотную закономерность (метрика конкретной публикации систематически выше/ниже личной нормы) для каждой из трёх метрик, upsert по `(userId, patternType)` — единственная эволюционирующая запись, не snapshot (`26_DATA_PIPELINE.md` §28–31, `DECISIONS.md` D-0023). **Task 7.3 завершена — Phase 7 закрыта.** `GET /api/knowledge` — первый read-only доступ к `Memory`+`Pattern` разом; до этого обе сущности существовали только за write-триггерами, возвращающими счётчики, не записи — критерий `42_IMPLEMENTATION_ROADMAP.md` §39 («система может ответить: что мы знаем и почему») не выполнялся буквально, пока не появился способ реально прочитать знание через приложение. **Phase 8 — Recommendation Engine: Task 8.1 завершена** — первая реализация DECISION layer (`src/decision/`): `CANDIDATE_GENERATOR` берёт форматы из реально существующих `Content.contentType`, `CANDIDATE_SCORER` сравнивает каждый кандидат с личной нормой (Task 6.2) по каждой метрике отдельно, использует Pattern (Task 7.2) как контекст, не как фактор ранжирования — только 3 из 9 документированных факторов реализованы (`23_SYSTEM_ARCHITECTURE.md` §25–27, `21_DECISION_LOGIC.md` §9/§39–41, `DECISIONS.md` D-0024). Read-only — `Recommendation` не заполняется. **Task 8.2 завершена** — goal fit (`21_DECISION_LOGIC.md` §6–8): `determineGoalFit` выбирает, какой из per-metric рейтингов первичен для целей пользователя (`Goal.priority`, первое реальное использование поля), выбор, не смешивание метрик (продолжение D-0024). Явные состояния `no_goals_defined`/`no_tracked_goals`, когда цели нет или она не сопоставляется с трекаемой метрикой (например `followers` — не собирается, D-0018) — не молчаливое предположение (`DECISIONS.md` D-0025). **Task 8.3 завершена** — персистентность решения (`25_DATABASE_SCHEMA.md` §26/§28/§30): `createRecommendation` закрывает цикл «решение → можно объяснить и найти позже» на уже готовых Task 8.1/8.2 кандидатах, без добора оставшихся факторов скоринга (прямое требование Olga). Пишет `Recommendation`+`RecommendationReason` (новая таблица) только когда goal fit реально выбрал метрику и есть реальный кандидат с данными; во всех остальных честных состояниях — явный некреированный результат, не запись-заглушка (`DECISIONS.md` D-0026). **Task 8.4 завершена** — freshness + repetition (`21_DECISION_LOGIC.md` §12–14): качественные сигналы на `CandidateResult` (`freshness`: recent/aging/stale по буквальным порогам §12; `repetitionNote`: только на топ-кандидате, только при совпадении с последним опубликованным форматом и наличии конкурентной альтернативы), ни один не меняет порядок `ranking` (§40, продолжение D-0024/D-0025). `freshness` дополняет persisted `RecommendationReason` (реальный пункт словаря §28); `repetitionNote` намеренно не становится причиной — «repetition» отсутствует в словаре документа (`DECISIONS.md` D-0027). **Phase 8 — Recommendation Engine закрыта на объёме Task 8.1–8.4** (решение Olga, 15 августа 2026) — критерий `42_IMPLEMENTATION_ROADMAP.md` §44 («data → insight → recommendation, без необходимости AI») подтверждён end-to-end реальным интеграционным тестом, полностью детерминированно. Проверка перед закрытием нашла честный пробел: `Memory` (Task 7.1) физически не читается Decision Engine — единственная часть §41 «knowledge»-входа, не дошедшая до реального pipeline'а (Baseline/Pattern дошли); не блокирует §44, зафиксировано на будущее. **Phase 9 — AI Layer начата — Task 9.1 завершена** (`29_AI_LAYER.md`, `21_DECISION_LOGIC.md` §50 DECISION EXPLANATION): первое реальное использование `AIService`/`AIProvider` (Task 0.2, ни разу не вызывались) — `generateDecisionExplanation` генерирует объяснение уже сохранённой (Task 8.3) `Recommendation` по структуре Recommendation → Why now → Evidence → Expected benefit → Uncertainty, полностью на реальных `RecommendationReason`, без выдуманных фактов. Обязательные ограничения выполнены с самого начала: AI только читает `Recommendation`, пишет исключительно в новую append-only `AiRun` (§67/§68); структурированный JSON-output с валидацией до персистентности (§11/§26); confidence в промпте — качественная метка из уже сохранённого `context.ranking`, не голое число (§35 EXPLANATION MUST NOT INVENT); `AiRun` логируется при любом исходе, не только успехе (§24/§76). `AIProvider`/`AIService`/`providers/anthropic.ts` не изменены — контракт Task 0.2 подошёл без правок. `DECISIONS.md` D-0028. **Task 9.2 завершена** — экран `/recommendations/[id]`: отображает результат Task 9.1 по структуре §50, тот же UI-паттерн, что `/integrations` (Task 3.4) — server component читает `Recommendation` напрямую через repository, клиентская кнопка вызывает уже существующий `explain`-route. Честное сообщение под каждый нестандартный исход (`provider_unavailable`/`failed`/`timeout`/`validation_failed`), не общий «что-то пошло не так» — **подтверждено вручную в реальном браузере**: реально засеянная `Recommendation`, реальный клик, реально отобразилось «AI-провайдер сейчас недоступен — скорее всего, не настроен ключ API» (в этом окружении `ANTHROPIC_API_KEY` действительно не настроен на тот момент). **Повторно подтверждено 15 августа 2026** после активации кредитов Anthropic — тот же экран, реально засеянная `Recommendation`, реальный клик, все 4 секции (`whyNow`/`evidence`/`expectedBenefit`/`uncertainty`) реально отобразились с настоящим сгенерированным текстом, не сообщением об ошибке; временный тестовый пользователь и все связанные данные удалены после проверки. **Task 9.3 завершена** — `ContentKnowledge` (`29_AI_LAYER.md` §33 AI FOR RECOMMENDATIONS — «генерации контента» как применение AI): новая, НЕ привязанная к пользователю таблица для внешних приёмов создания контента (заголовки/хуки/расписание публикаций), загружаемых отдельными записями, а не зашитых в промпт/код (прямое требование Olga). 15 первых записей загружены идемпотентным скриптом (`npm run seed:content-knowledge`) из двух личных файлов Olga: все 7 правил заголовков + фундамент стратегии из «доп. файла», недельная система хуков (Раздел 1, 7 дней) из «Полное руководство.pdf» (130 страниц — сознательно частичная загрузка, честно зафиксирована, D-0030). Сам AI-код чтения `ContentKnowledge` при генерации текста — не реализован этим шагом, только механизм (`findActive`), которым будущая задача воспользуется (§73, тот же принцип, что D-0028). **Побочная находка при полном прогоне тестов**: оба ранее заблокированных live Instagram-теста (Task 3.2/4.1) впервые за 14 задокументированных прогонов прошли реально — блокировка на стороне Meta разрешилась сама, не связана с кодом проекта (см. «Заблокировано» ниже). **Task 9.4 завершена** — генерация текста на основе `ContentKnowledge` (вторая AI-задача, зеркально Task 9.1: промпт+эндпоинт+валидация+AiRun-логирование). `AiRun` обобщена: `recommendationId` стал опциональным, добавлены `userId`/`inputContext` (§24 сам по себе общий, не Recommendation-специфичный — `DECISIONS.md` D-0031). `generateContentSuggestions` использует ТОЛЬКО реально загруженные (Task 9.3) `ContentKnowledge` (`hook_template`+`headline_rule`), честные состояния `invalid_topic`/`no_knowledge_available` без AI-вызова. **Подтверждено вживую через реально запущенный dev-сервер** (не только автотестами) — реальная сессия, реальный HTTP-запрос, реальный `ContentKnowledge`, реальный `AIService.generate`, честный `provider_unavailable` в ответе и в персистентной `AiRun`. Честная оговорка: сама генерация реального текста нуждается в `ANTHROPIC_API_KEY`, не настроенном в этом окружении. **Task 9.5 завершена** — экран `/content-suggestions`: отображает форму для Task 9.4, тот же UI-паттерн, что `/recommendations/[id]` (Task 9.2). Форма «тема поста» → `POST /api/ai/content-suggestions`, список вариантов при успехе, честное сообщение под каждый из 6 нестандартных исходов (включая `invalid_topic`/`no_knowledge_available`) при неуспехе. **Подтверждено вручную в реальном браузере**: реально зарегистрирован пользователь, реальная отправка формы с темой показала «AI-провайдер сейчас недоступен…», пустая тема показала «Тема не может быть пустой…». **Повторно подтверждено 15 августа 2026** после активации кредитов Anthropic — та же форма, реальная тема поста, все 6 вариантов реально отобразились с настоящим сгенерированным текстом на русском и корректной атрибуцией `basedOn` к загруженным записям `ContentKnowledge`, не сообщением об ошибке; временный тестовый пользователь удалён после проверки. **Task 9.6 завершена** — пополнение `ContentKnowledge`: Раздел 2 «Тренды монтажа» «Полное руководство.pdf» загружен целиком (3 записи, новая категория `editing_style`), из Раздела 3 «Стратегия диверсификации контента по платформам» загружена только буквально одноимённая часть — стр. 8–11 (5 записей, новая категория `platform_strategy`); честная находка зафиксирована в `DECISIONS.md` D-0032 — заголовок «Раздел 3» не меняется до стр. 130 документа, но содержание после стр. 11 расходится на множество разных отдельных видео-уроков, не одну тему, стр. 12–130 сознательно не загружены. Итого 23 записи, 6 категорий; `npm run seed:content-knowledge` подтверждён идемпотентным вторым прогоном. **Task 9.7 завершена** — прежде чем продолжать загрузку, Olga попросила измерить реальную стоимость нефильтрованной передачи `ContentKnowledge` в промпт против фильтрации по category: через бесплатный `count_tokens` — нефильтрованно 5 824 токена/вызов (~$0.016), прогноз после полной загрузки 130 страниц ~51–52К токенов (~$0.11–0.16), продакшн-фильтр 3 299 токенов (~$0.011) — разница на порядок при любой интенсивности использования, решение: остаться на фильтрации по category. Чтобы снять неудобство «запись честно про несколько тем сразу», `category` (String) стала `categories` (String[], `DECISIONS.md` D-0033) — миграция без потери данных, проверена полным реприменением истории (10 миграций) на одноразовой БД. Беглый пересмотр 23 записей нашёл одну с честным вторым тегом (воскресный хук — одновременно `hook_template` и `headline_rule`). Загружено ещё 3 записи со стр. 11–15 «Полное руководство.pdf» (новая категория `vlog_strategy`), банки нишевых идей (Comfort Creator по нишам, vlog storylines) честно не загружены — не переиспользуемый приём, тот же принцип D-0030 п. 6. Итого 26 записей, 7 категорий. **Task 9.8 завершена** — продолжение загрузки, стр. 16–35 «Полное руководство.pdf»: 15 новых записей (функция Series, 3 функции заголовка, 4 тренда MID YEAR TREND SPOTTING, система 4 кварталов дня, обновление каруселей, шаблоны карусели Hierarchy/Formula, Trial Reels, 4 новых хука, организация телефона — 2 новые категории `workflow_system`/`content_format_template`). Честно исключены нишевые банки идей И отдельно — дубликаты уже загруженного (I-Centric→We-Centric дублирует Правило 1, тренд Micro-Series дублирует формат Task 9.7, хук Reverse Psychology дублирует хук среды, Digital Diet дублирует «контентные шоры»), а также контент вне области ContentKnowledge (бизнес-автоматизация Gusto). Итого 41 запись, 9 категорий. **Task 9.9 завершена** — продолжение загрузки, стр. 36–62 «Полное руководство.pdf»: 14 новых записей (Instagram Instants, 4 типа контент-опор по функции, Content Anchor+Supporting Pillars, продвинутое недельное расписание, долгосрочный фундамент vs. постановочный инфлюенсинг, архетип платформы, шрифтовая система, текстовые оверлеи Canva, оформление Stories, кросс-платформенный цикл роста TikTok↔Instagram, новый хук Unexpected Visual Hook, реаранжировка рутины под серию, оптимизация био — новая категория `bio_strategy`, выход из «View Jail»). Впервые целенаправленно отфильтрованы не только нишевые/сезонные банки идей, но и прямые дубликаты уже загруженного (5 из 6 дней «Unique Hook Schedule» дублировали уже загруженные хуки, включая буквальное совпадение шаблона с Bold Claim Hook) — по итогам похвалы Olga за находку скрытого дубликата в Task 9.8. Итого 55 записей, 10 категорий. **Task 9.10 завершена** — продолжение загрузки, стр. 63–82 «Полное руководство.pdf»: 13 новых записей (Repetitive Signature Hook, формула Episode X of/Day X of, разогрев алгоритма вокруг публикации, квадрант каруселей Personal/Valuable/Relatable/Promotional + технические правила карусели, лимиты приложений как триггер, настройки камеры iPhone, тренд «усталости от монтажа» и визуальный отпечаток, матрица назначения форматов, Trial Reels для не-подписчиков, определение фирменного стиля из 5 компонентов, система организации идей Brainstorm→Organize→Execute, различие «контент для подписчиков» vs «только на просмотры»). Систематическая проверка на дубликаты снова сработала: список микро-визуальных хуков в «2026 Editing Styles» дублировал уже загруженный Unexpected Visual Hook, виджеты в «Niche Phone Settings» дублировали организацию телефона, одна из двух каруселных таксономий пересекалась с другой. Итого 68 записей, 10 категорий. **Task 9.11 завершена** — продолжение загрузки, стр. 83–102 «Полное руководство.pdf»: 15 новых записей (Reverse Trend, FaceTime Effect vs Community Repellent, 3 новых хука — Community Driven/Confessional/Inclusive Signature, горизонтальные видео, эскапистский контент, статичное фото с музыкой, анатомия закадрового голоса, хуки для КОНЦА видео — cliffhanger/takeaway/CTA, адаптация структуры тренда под нишу, диагностика застоя роста, цикл негативной обратной связи инфлюенс-индустрии, недельный ритм производства контента, разделение зон контроля творец/делегируемое). Систематическая проверка на дубликаты сработала третий шаг подряд (Repetitive/Episodic Signature Hooks, анти-ИИ часть Escapism, Carousels as Micro Vlogs, Reverse Psychology/Visual/Engagement хуки — все дубликаты уже загруженного). Итого 83 записи, 10 категорий. **Task 9.12 завершена — «Полное руководство.pdf» (130 страниц) загружено полностью.** Последний шаг, стр. 103–130: 18 новых записей (Signature Series Matrix, техническая структура дня батчинга, messy-контент как «нарушитель паттерна», стратегия первых трёх постов нового аккаунта, Google Search Profile, сдвиг алгоритма Instagram на удержание, недельный календарь Stories, выбор 2-3 форматов, Freebie вместо продаж, 5 способов «удвоить» контент, нехватка идей как проблема обучения, 5 структур серии, UGC для брендовых сделок (практика+портфолио/питчинг), монтаж вирусных voiceover-видео, три принципа личного бренда, Binge-Watching Blueprint, «личность как усилитель идеи»). Итого 101 запись, 10 категорий — весь документ пройден узкими честными шагами Task 9.3/9.6–9.12, с двойной систематической проверкой на каждом шаге (нишевая/сезонная привязка И прямой дубликат уже загруженного).

## Завершено
- Полная архитектурная и продуктовая спецификация (45 документов в `/docs`)
- Определена модель разработки (Olga / ChatGPT / Claude Code) и процесс Green/Yellow/Red
- Полный документальный аудит: все файлы прочитаны целиком, проверены на дубли и противоречия (`DECISIONS.md`, D-0002)
- Технологический стек MVP выбран и обоснован по 10-пунктному чек-листу (`DECISIONS.md`, D-0001)
- Независимый review от ChatGPT получен и принят: architecture/stack/DB/async/deployment/AI abstraction/documentation — все GREEN. Instagram/Auth boundary — YELLOW, требует отдельной проверки перед Phase 3 (`DECISIONS.md`, D-0003)
- Обязательные архитектурные границы из review закреплены в `CLAUDE.md` §4.1
- Task 0.1: git-репозиторий создан (local + remote GitHub, `origin/main`, история запушена), базовая структура папок на месте — `/docs`, `/src`, `/tests`
- Task 0.2: Next.js + TypeScript + Prisma scaffolding создан и верифицирован. Структура `/src` физически отражает слои (`data/analysis/knowledge/decision/ai/storage/integrations/learning` + `app/` как UI/API layer). Обязательные абстракции реализованы: `AIProvider`/`AIService` (единственный импорт `@anthropic-ai/sdk` — в `src/ai/providers/anthropic.ts`), `ObjectStorageService` (единственный импорт AWS S3 SDK — в `src/storage/providers/r2.ts`), Prisma Client singleton в `src/data/`, `ExternalIntegration` — пустая заготовка до Task 3.0. Верификация: `npm install` (373 пакета), `npx prisma generate`, `npx tsc --noEmit` (без ошибок), `npm run dev` (Ready in 2.6s, HTTP 200 на `/`) — все прошли успешно
- Task 0.3 (repo-часть): `ENVIRONMENTS.md` — стратегия трёх окружений (dev/staging/production), правило изоляции credentials/БД/Redis/storage по окружениям. `.env.example` расширен до полного набора переменных стека (Postgres, Redis, Auth.js, Anthropic, R2, Sentry) с комментариями по назначению и по окружению. `.gitignore` подтверждён (`.env`/`.env*.local` исключены)
- Task 0.4: `.github/workflows/ci.yml` — GitHub Actions pipeline на `pull_request`/`push` в `main`: `npm ci` → `npx prisma generate` → `npm run lint` → `npx tsc --noEmit` → `npm run build`. Все шаги проверены локально перед коммитом (lint — 0 warnings, build — успешен). Required status checks / branch protection на GitHub НЕ настроены — нет `gh` CLI и доступа к репозиторию в этом окружении, нужна ручная настройка Olga (см. «В работе»)
- D-0006: репозиторий перенесён на новый GitHub-аккаунт `olgavladimirovna83-sketch`, проект `Project01` (RED-действие, выполнено лично Olga; старый репозиторий `Olgavladimirovna83-create/Application` сохранён как резервный, не удалён)
- Task 0.5: тестовый фреймворк инициализирован — Vitest (`vitest.config.ts`, `tests/unit/`) и Playwright (`playwright.config.ts`, `tests/e2e/`), по одному smoke-тесту на каждый. `package.json`: `test`/`test:watch`/`test:e2e`. CI (`.github/workflows/ci.yml`) расширен шагами `npm test` и `npx playwright install --with-deps chromium` + `npm run test:e2e`. Проверено локально и подтверждено зелёным в GitHub Actions
- Task 0.6: Phase 0 закрыта по всем локально проверяемым критериям (`42_IMPLEMENTATION_ROADMAP.md` §4) — запуск, тесты, сборка, безопасная конфигурация. Критерий «deploy в staging» отложен продуктовым решением Olga (`DECISIONS.md`, D-0007), перенесён в Backlog `TASKS.md`
- Task 1.1: `prisma/schema.prisma` — 10 core-моделей (User, Goal, Content, ContentFeature, PerformanceMetric, Pattern, Memory, Recommendation, UserDecision, Experiment) + 5 enum (см. `DECISIONS.md`, D-0008 по scope и enum/String правилу). Postgres.app установлен и инициализирован Olga локально (порт 5432, trust auth); подключение — через Unix socket `/tmp` (TCP/localhost упирается в permission-dialog Postgres.app). `DATABASE_URL` записан в реальный `.env` (не в git). Миграция `20260812142954_init_core_entities` применена на dev-базе `olga`; чистый rollback/reapply подтверждён на отдельной одноразовой базе (создана и удалена только для проверки — база `olga` не тронута)
- Task 1.2: repository-слой в `src/data/repositories/` — по одному репозитиву (`create`/`findById`/`update`) на каждую из 10 сущностей. Интеграционный smoke-тест `tests/integration/user-data-flow.smoke.test.ts` — реально проходит по критерию Phase 1 completion (`42_IMPLEMENTATION_ROADMAP.md` §8): создать user → сохранить данные (goal) → получить обратно; чистит за собой созданные строки. CI получил `postgres:16` service + `prisma migrate deploy`, чтобы интеграционный тест реально гонялся и там, не только локально. `npm test` (2/2), `npm run test:e2e` (1/1), `npx tsc --noEmit`, `npm run lint`, `npm run build` — без ошибок. Критерий «connected account» из Phase 1 completion сознательно отложен до Phase 3/Task 3.0 (см. `TASKS.md`) — не блокирует закрытие Phase 1. Phase 1 — Database Foundation считается завершённой
- Task 2.1: Auth.js — техническая основа. `prisma/schema.prisma` расширен под Auth.js (`Account`/`Session`/`VerificationToken` + auth-поля на `User`), миграция `20260812163141_auth_js_foundation` применена и проверена на чистое применение полной истории (аналогично Task 1.1). Механизм — Credentials (email+password), выбран специально чтобы не создавать RED-зависимость от внешнего email-сервиса, по прямому ограничению Olga (`DECISIONS.md`, D-0009). `src/auth/` — `config.ts`/`credentials.ts`/`password.ts`/`types.d.ts`, route handler `src/app/api/auth/[...nextauth]/route.ts`. Integration smoke-тест проверяет `authenticateWithCredentials` против живой БД (верный/неверный пароль, неизвестный email). Полный HTTP sign-in flow (cookie-based JWT-сессия через браузер) — не проверялся, за пределами scope Task 2.1. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (3/3), `npm run test:e2e` (1/1) — все прошли; CI дополнен `AUTH_SECRET` (не реальный секрет, CI-only)
- Task 2.2: Registration/Login/Logout (UI + API) поверх Task 2.1. `src/auth/register.ts` (`registerUser` — валидация email/пароля, проверка занятого email, хеширование, создание пользователя), `src/app/api/register/route.ts` (201/400/409), `src/app/api/me/route.ts` (единственный protected route — 401 без сессии, иначе перечитывает пользователя из БД по `session.user.id`, не по claims), `src/app/login/page.tsx`, `src/app/register/page.tsx`, `src/app/_components/LogoutButton.tsx`, обновлённый `src/app/page.tsx` (async server component, logged-in/logged-out состояние через `auth()`). **Пробел из Task 2.1 закрыт**: `tests/e2e/auth-flow.spec.ts` — 4 сценария через реальный браузер (Playwright): register → session → `/api/me` 200 → logout → `/api/me` 401; повторный login с верным паролем; login с неверным паролем не создаёт сессию; два независимо залогиненных пользователя видят через `/api/me` только свои данные (user isolation, два `browser.newContext()`). `tests/integration/auth-registration.smoke.test.ts` — `registerUser` против живой БД. Rate limiting на auth endpoints сознательно не реализован (блокер — Redis не поднят, `DECISIONS.md` D-0007), явно зафиксировано, не тихий пропуск. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (4 файла/7 тестов), `npm run test:e2e` (5/5) — все прошли; dev-БД `olga` — 0 тестовых пользователей после прогонов
- Task 2.3: Authorization foundation. `src/auth/session.ts` — `requireSessionUserId()`, единая точка получения user id из сессии; `/api/me` отрефакторен на неё же (без изменения поведения). `src/app/api/goals/route.ts` (`POST` — создаёт `Goal` с `userId` строго из сессии) и `src/app/api/goals/[id]/route.ts` (`GET` — `404` единообразно для несуществующего И чужого goal, не палит существование). `tests/e2e/helpers/auth.ts` — общий `registerAndLogin`/`uniqueEmail` helper, вынесен из Task 2.2 для переиспользования между спеками. `tests/e2e/goal-authorization.spec.ts` (3 теста, реальный браузер) доказывает: ownership-check обобщается за пределы `/api/me` на произвольный ресурс — неаутентифицированный запрос → 401; владелец создаёт/читает свой goal; второй пользователь получает 404 и на чужой goal, и на несуществующий id (коды неотличимы). Role model (owner/admin/editor/viewer), service permissions, password reset — сознательно не в этой задаче (`30_SECURITY_PRIVACY.md` §11 явно оставляет role model вне MVP-scope). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (4 файла/7 тестов), `npm run test:e2e` (8/8) — все прошли; dev-БД `olga` — 0 users/0 goals после прогонов
- Phase 2 — Authentication закрыта продуктовым решением Olga (12 августа 2026) на объёме Task 2.1–2.3 — критерий `42_IMPLEMENTATION_ROADMAP.md` §11 («создать account → войти → получить доступ только к своим данным») полностью подтверждён тестами. Account recovery/password reset и role model сознательно остаются вне MVP-scope — не забыты, а не входят в критерий готовности фазы (см. `TASKS.md`)
- Task 3.0: техническая проверка Instagram/Meta Graph API (research-only, без реализации и без создания Meta developer-аккаунта — RED). Резюме — [`INSTAGRAM_API_REVIEW.md`](INSTAGRAM_API_REVIEW.md). Ключевое: Business Login (без Facebook Page) — рекомендуемый путь; token lifecycle 1ч → 60д → refresh; метрика `impressions` deprecated с апреля 2025 (заменена на `views`) — влияет на маппинг метрик из `08_METRICS_FRAMEWORK.md` при будущей реализации Data layer; история метрик хранится Instagram 90 дней (подтверждает цель «~3 месяца» из документа, не превышает её); rate limiting per-account, не глобальный; webhooks не покрывают performance-метрики — polling/incremental sync (уже в `26_DATA_PIPELINE.md`) остаётся верной архитектурой; реальный запуск для сторонних пользователей потребует Meta App Review + Business Verification — организационная, не только техническая зависимость
- Уточнение по Task 3.0 (12 августа 2026): Olga лично прошла реальную авторизацию Instagram и подтвердила фактический набор permissions — исправило исходный вывод резюме. Разрешены: `instagram_business_basic` (обязательный) + отдельное insights-разрешение (не часть `instagram_business_basic`, как предполагалось изначально). Осознанно отключены: comments/messages/content-publish. `INSTAGRAM_API_REVIEW.md` обновлён (§3, §8, §9) — первичный источник (наблюдение Olga) выше документации по иерархии `CLAUDE.md` §2
- Task 3.1: Integration Service — контракт `IntegrationProvider`/`IntegrationService` (design). [`src/integrations/IntegrationProvider.ts`](src/integrations/IntegrationProvider.ts) — общий контракт платформенных адаптеров (authorize URL → code exchange → refresh → account/media insights → best-effort disconnect), спроектирован на основе подтверждённых Task 3.0 деталей: `IntegrationAuthError` (терминальное состояние, не ретраится) отдельно от `IntegrationRateLimitError` (ретраится с backoff); `unavailableMetrics` в результате insights — прямая реализация data-completeness правила из `08_METRICS_FRAMEWORK.md` §11. [`src/integrations/IntegrationService.ts`](src/integrations/IntegrationService.ts) — фасад на registry (`registerIntegrationProvider`), не hardcoded provider, т.к. платформ будет больше одной (Instagram сейчас, TikTok и другие позже). Старый placeholder `ExternalIntegration.ts` удалён (не использовался в коде нигде), заменён — терминология `CLAUDE.md` §4.1 не переписана, только код-реализация названа по аналогии с `AIProvider`/`AIService`
- Task 3.2: Instagram-адаптер `providers/instagram.ts` + живая проверка через реальный API. `getAccountInsights({ metrics: ['reach'], period: 'day' })` дважды реально прочитал данные с настоящего Instagram-аккаунта Olga. Найдено по ходу: (1) большинство account-level метрик, кроме `reach`, требуют `metric_type=total_value` — добавлено опциональное поле `metricType` в `IntegrationAccountInsightsParams`; (2) `vitest.config.ts` не грузил `.env` в `process.env` вообще — `DATABASE_URL` у прежних Prisma-тестов «работал» только случайно, как побочный эффект того, что `@prisma/client` сам грузит `.env` при импорте; исправлено явной загрузкой через `loadEnv` из `vite` — общий фикс, не костыль под Instagram
- Session key invalid — расследовано и закрыто (13 августа 2026): полный raw JSON от endpoint'а обмена показал `type: OAuthException`, `code: 452` — независимо подтверждено, что это легаси-код `API_EC_SESSION_INVALID` из дореформенного Facebook REST API, как и подозревала Olga по формулировке. Вместо дальнейших гипотез по `error_subcode` — прямая проверка реальным browser OAuth round trip: добавлены `src/integrations/bootstrap.ts` (регистрирует Instagram-провайдер в `IntegrationService`) и диагностический `src/app/api/integrations/instagram/callback/route.ts`. Redirect URI потребовал `https://` (Meta отклонила `http://localhost`) — решено через встроенный `next dev --experimental-https` (mkcert, локально доверенный сертификат), не через ngrok (тот требует создания нового внешнего аккаунта — тот же RED, что Meta developer account, отклонён). Olga лично прошла flow — успех с первой попытки, токен на ~60 дней, как документировано. Вывод: `Session key invalid` — не баг реализации, а следствие того, что токены из ручной кнопки "Generate Token" в Meta Dashboard не совместимы с `ig_exchange_token` — реальные пользователи всегда идут через настоящий OAuth flow, в продакшене эта ошибка не встретится. `INSTAGRAM_API_REVIEW.md` дополнен §4.1, закрывающим этот вопрос как подтверждённый факт, не гипотезу
- Task 3.3: `ExternalAccount` — data layer для хранения подключения Instagram (`25_DATABASE_SCHEMA.md` §7). [`prisma/schema.prisma`](prisma/schema.prisma) — модель с полями §7 (`userId`/`platform`/`externalUserId`/`status`/`connectedAt`/`lastSyncedAt`) + `accessToken`/`tokenExpiresAt` сверх §7 (практически необходимы). `status` — закрытый enum `ExternalAccountStatus` (`connected`/`expired`/`disconnected`, `30_SECURITY_PRIVACY.md` §25). `platform` — `String`, не enum (открытая категория, §70 FUTURE_MULTI_PLATFORM). Unique `[platform, externalUserId]` (§53). Миграция `20260813082115_external_accounts` применена и проверена на чистое переприменение полной истории. **Security-gap зафиксирован явно в [`DECISIONS.md` D-0010](DECISIONS.md)**: `accessToken` хранится без encryption at rest (`30_SECURITY_PRIVACY.md` §23 не выполнен) — сознательно, secret manager отложен вместе с cloud-стеком (D-0007), явная зарубка «закрыть до production». [`externalAccountRepository`](src/data/repositories/externalAccountRepository.ts) — `create`/`findByUserId`/`update`. Найдено по ходу: `IntegrationProvider` не возвращал id аккаунта платформы — добавлен аддитивный метод `getAccountIdentity`. Callback route переписан: требует сессию приложения (401 без неё), сохраняет/обновляет `ExternalAccount` через repository вместо простого показа JSON, не создаёт дубли при реконнекте. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (7/7 файлов, 11/11 тестов) — без регрессий; dev-БД `olga` — 0 users/0 externalAccounts после прогонов
- Task 3.4: UI подключения/отключения Instagram. [`src/integrations/config.ts`](src/integrations/config.ts) — общие `INSTAGRAM_REDIRECT_URI`/`INSTAGRAM_OAUTH_STATE_COOKIE`. [`authorize/route.ts`](src/app/api/integrations/instagram/authorize/route.ts) — реальная инициация (заменяет throwaway-скрипт Task 3.2): без сессии редиректит на `/login`; генерирует `state`, кладёт в short-lived httpOnly cookie, редиректит на authorize URL. `callback/route.ts` дополнен CSRF-проверкой `state` (сверяется с cookie, одноразовая) и больше не отдаёт raw JSON — редиректит на `/integrations` с исходом в query (`connected=1`/`error=<code>`) — это и есть «реальный product-flow», не диагностика. [`disconnect/route.ts`](src/app/api/integrations/instagram/disconnect/route.ts) — `POST`, переводит запись в `disconnected` (не удаляет), best-effort `IntegrationService.disconnect`. [`src/app/integrations/page.tsx`](src/app/integrations/page.tsx) + `DisconnectButton.tsx` — экран статуса, ссылка добавлена на главную. [`tests/e2e/integrations-instagram.spec.ts`](tests/e2e/integrations-instagram.spec.ts) (реальный браузер+БД, «подключённое» состояние сеется напрямую через Prisma — реальный Instagram round trip технически невозможен автоматизировать, та же причина, что Task 3.2/3.3) покрывает: неаутентифицированный доступ, «не подключён» → ссылка, «подключён» → «Отключить» → статус реально меняется в БД и UI. По ходу с разрешения Olga остановлен процесс `next dev --experimental-https`, занимавший порт 3000 в её терминале после Task 3.2/3.3 (можно перезапустить той же командой при следующей необходимости HTTPS)
- Дополнение к Task 3.4 (13 августа 2026): добавлено поле `username` в `ExternalAccount` по запросу Olga — не просто удобство, у неё несколько связанных Instagram-аккаунтов, экран без имени не даёт убедиться, что подключён именно нужный. Изначально сознательно не добавлял (согласованная на Task 3.3 схема не включала это поле) — Olga подтвердила и попросила добавить. `prisma/schema.prisma` — `ExternalAccount.username` (nullable), миграция `20260813153419_external_account_username`, применена и проверена на чистое переприменение полной истории (4 миграции) на одноразовой базе. `callback/route.ts` сохраняет `identity.username`; `/integrations` показывает `Аккаунт: @username` (или явное «username недоступен», если платформа его не вернула). Тесты обновлены — `external-account-data-flow.smoke.test.ts` проверяет round-trip поля, `integrations-instagram.spec.ts` сеет и проверяет реальное отображение на экране. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (7/7, 11/11), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 0 users/0 externalAccounts после прогонов
- Финальное подтверждение Task 3.4 (13 августа 2026): Olga прошла полный цикл вручную через настоящий интерфейс (`https://localhost:3000/integrations`, `next dev --experimental-https`). Подключение через реальный Instagram consent screen показало правильный аккаунт — `@nikodine` — среди нескольких связанных, именно то, для чего добавлялся `username`. Отключение вернуло статус в «не подключён». Task 3.4 полностью закрыта — открытых пунктов не осталось
- **Task 4.1: разовая синхронная синхронизация Instagram → `Content`/`PerformanceMetric` (первый шаг Phase 4). [`src/ingestion/normalize.ts`](src/ingestion/normalize.ts) — чистые `validateMediaItem`/`normalizeContentType` (unit-тесты, синтетические данные). [`src/ingestion/instagramSync.ts`](src/ingestion/instagramSync.ts) — `syncInstagramAccount(userId)`: `getAccountInsights`+`listRecentMedia`→validate→normalize→upsert `Content`→`getMediaInsights`→новые строки `PerformanceMetric` (snapshot-семантика, не upsert; `value: null` для `unavailableMetrics`, не тихий пропуск). `IntegrationAuthError` → `ExternalAccount.status = 'expired'`, прерывает sync — задействует terminal-state дизайн из Task 3.1. **Найдено при реализации**: `media_type` не отличает Reels от обычного видео — добавлено поле `media_product_type` в `listRecentMedia` (аддитивно), **подтверждено живым запросом**: реальный аккаунт вернул `REELS` для части публикаций, нормализация развела `reel`/`carousel` корректно. **Живая проверка полного pipeline** (`tests/integration/instagram-sync-live.smoke.test.ts`, skip-if-no-credentials, как Task 3.2): на реальном аккаунте Olga — 25 публикаций, 175 метрик, 0 предупреждений; повторный вызов — те же 25 `Content` (id идентичны, не дубли), метрики выросли ровно вдвое (350) — upsert/snapshot семантика подтверждена не только логически, но и на реальных данных. `getAccountInsights` (`reach`) получен, но сознательно не персистится — нет подходящей сущности без схемного расширения (явная scope-граница из формулировки задачи). `contentRepository.findByExternalId` добавлен для dedup-lookup. Trigger — `POST /api/integrations/instagram/sync` + кнопка «Синхронизировать» на `/integrations`, `lastSyncedAt` теперь реально используется (поле существовало с Task 3.3). Найденный и исправленный по ходу баг: `syncInstagramAccount` не регистрировал Instagram-провайдер сам — исправлено переносом side-effect import в сам модуль. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (9/9 файлов, 21/21, включая живой), `npm run test:e2e` (12/12) — без регрессий; dev-БД `olga` — 0 `Content`/`PerformanceMetric` от тестов после прогонов**
- **Task 4.2: персистентное хранение account-level метрик.** Новая Prisma-модель [`AccountSnapshot`](prisma/schema.prisma) (по аналогии с `PerformanceMetric`, но на уровне `ExternalAccount`) — `DECISIONS.md` [D-0014](DECISIONS.md), YELLOW: ни `PerformanceMetric` (жёстко привязан к `content_id`), ни `Baseline`/`25_DATABASE_SCHEMA.md` §16 (вычисленное значение, не сырое измерение) для этого не подошли — реальный, ранее не описанный пробел в документированной схеме, зафиксирован в новом [`DOCUMENT_CROSS_REFERENCE.md`](DOCUMENT_CROSS_REFERENCE.md) («Известные ограничения»). Миграция `20260813191441_account_snapshots`, применена и проверена на чистое переприменение полной истории (5 миграций) на одноразовой базе. [`accountSnapshotRepository`](src/data/repositories/accountSnapshotRepository.ts) — `create`/`findByExternalAccountId`. `syncInstagramAccount` теперь сохраняет `getAccountInsights` как `AccountSnapshot` (включая `unavailableMetrics` → `value: null`, тот же принцип, что `PerformanceMetric`) сразу после Task 4.1-пайплайна, снимок не upsert. **Живая проверка**: `tests/integration/instagram-sync-live.smoke.test.ts` дополнен — подтверждено на реальном аккаунте Olga, что снимки создаются при первом sync и растут (не перезаписываются) при повторном. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (9/9, 21/21, включая живой) — без регрессий.
- **Task 4.3: устойчивость к сбоям для `syncInstagramAccount`** (`42_IMPLEMENTATION_ROADMAP.md` §24 RETRIES — timeout/retry/exponential backoff/maximum attempts/failure state). [`src/ingestion/retry.ts`](src/ingestion/retry.ts) — `withRetry`/`RetriesExhaustedError`: `IntegrationAuthError` по-прежнему не ретраится (terminal state), `IntegrationRateLimitError.retryAfterSeconds` используется как задержка, если платформа его вернула (включая корректную обработку `0`), иначе — exponential backoff с `maxAttempts` (по умолчанию 3). [`src/integrations/providers/instagram.ts`](src/integrations/providers/instagram.ts) — `fetchWithTimeout` (10с, `AbortController`), все 7 мест с `fetch()` переведены на него. [`src/ingestion/instagramSync.ts`](src/ingestion/instagramSync.ts) — все три сетевых вызова к Instagram обёрнуты `withRetry`; `SyncWarning.attempts` — явное состояние failure при исчерпании попыток, не только текст сообщения. Без очереди — реализовано прямо внутри существующей синхронной функции (прямое требование Olga). **Тесты**: [`tests/unit/ingestion-retry.test.ts`](tests/unit/ingestion-retry.test.ts) — 7 тестов на синтетических ошибках (успех сразу/после сбоя, `IntegrationAuthError` не ретраится, исчерпание попыток даёт `RetriesExhaustedError` с верными `attempts`/`cause`, `retryAfterSeconds: 0` не путается с «не задано», `maxAttempts: 1`). **Живая проверка**: существующий `instagram-sync-live.smoke.test.ts` прошёл без изменений в самом тесте — зелёный прогон подтверждает, что реальный sync теперь идёт через timeout+retry путь без регрессий. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (10/10 файлов, 28/28, включая живой), `npm run test:e2e` (12/12) — без регрессий.
- **Task 5.1: статус «состояние данных»** (`42_IMPLEMENTATION_ROADMAP.md` §28 FRESHNESS/§29 DATA_HEALTH), первый шаг Phase 5. [`src/dataQuality/dataQualityStatus.ts`](src/dataQuality/dataQualityStatus.ts) — `getDataQualityStatus(userId)`: по каждому `ExternalAccount` — `connectionStatus`/`hasRecentFailure`/`lastSyncedAt`/`freshness` (`fresh`/`stale`/`never_synced`, порог 24ч)/`lastContentMetricAt`/`lastAccountSnapshotAt`/`gaps` (`no_content_synced`/`no_account_snapshots`). Не собирает новые данные — только читает уже существующее через новые read-only методы репозиториев (`countByExternalAccountId`, `findLatestMeasuredAtByExternalAccountId`, `findLatestCapturedAt`). [`GET /api/data-quality`](src/app/api/data-quality/route.ts) (session-protected). **Находка**: `SyncWarning` (Task 4.3), названный Olga как один из источников, нигде не персистится — существует только в ответе одного конкретного вызова `POST /sync`. `hasRecentFailure` поэтому основан только на `ExternalAccount.status === 'expired'`, зафиксировано как [`DECISIONS.md` D-0015](DECISIONS.md) (YELLOW, не тихое сужение). [`tests/integration/data-quality-status.smoke.test.ts`](tests/integration/data-quality-status.smoke.test.ts) — 6 тестов против реальной БД, не gated по Instagram-credentials (нет сетевых вызовов). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (новый файл 6/6; без учёта двух независимо упавших live-тестов — 32/34, см. «Заблокировано»), `npm run test:e2e` (12/12) — без регрессий.
- **Task 5.2: completeness + anomaly detection поверх `getDataQualityStatus`** (`26_DATA_PIPELINE.md` §21 MISSING_DATA, §56 DATA_QUALITY, §57 QUALITY_IMPACT). [`src/dataQuality/completeness.ts`](src/dataQuality/completeness.ts) — чистая `computeCompleteness`: доля публикаций, у которых последний batch метрик не содержит `value: null` (использует существующее различие Task 4.1). [`src/dataQuality/anomalyDetection.ts`](src/dataQuality/anomalyDetection.ts) — чистая `detectSyncCountAnomaly`: кластеризует `measuredAt` по разрыву времени в приближённые sync-прогоны (в схеме нет отдельной сущности sync run — `DECISIONS.md` [D-0016](DECISIONS.md)), сравнивает последний прогон со средним по истории аккаунта, относительный порог (не абсолютный, прямое требование Olga). `AccountDataQualityStatus` дополнен `completeness`/`syncCountAnomaly`, `gaps` — `incomplete_metrics`/`sync_count_anomaly`. `performanceMetricRepository.findRowsByExternalAccountId` заменил более узкий `findLatestMeasuredAtByExternalAccountId` из Task 5.1 — один запрос вместо трёх. **Уточнение цитаты**: Olga сослалась на §35–36 (SKELETON_CRITERIA/SKELETON_UPDATE) для anomaly detection — при проверке это оказалось про Content Skeleton (Recommendation Engine), не про аномалии объёма sync; исправлено на §56/§57. **Находка**: реальный датасет Olga (25 постов/175 метрик) не сохранён персистентно в dev-БД (аккаунт `disconnected`, 0 `Content`) — тесты сеют синтетические данные того же масштаба напрямую, живой API не требовался, как и просила Olga. 13 юнит-тестов (без БД) + 2 новых интеграционных теста (реальная БД, масштаб 25/175). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (47/49 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Task 5.3: consistency, последний пункт Phase 5** (`42_IMPLEMENTATION_ROADMAP.md` §27). **Проверено перед реализацией**: отдельного детального документного раздела про consistency нет нигде в `/docs` — независимая проверка подтвердила находку Olga (полный список отклонённых кандидатов — `24_TECHNICAL_ARCHITECTURE.md` §49, `27_API_CONTRACTS.md` §62–63, `26_DATA_PIPELINE.md` §31, `39_BACKUP_DISASTER_RECOVERY.md` §45–46 — в [`DECISIONS.md` D-0017](DECISIONS.md)). Реализация — явное рабочее предложение Olga, не цитата. [`src/dataQuality/temporalConsistency.ts`](src/dataQuality/temporalConsistency.ts) — чистая `checkTemporalConsistency`: `PerformanceMetric.measuredAt` раньше `Content.publishedAt` — временна́я невозможность, встроена в `AccountDataQualityStatus` (gap `temporal_inconsistency`). [`src/dataQuality/schemaInvariants.ts`](src/dataQuality/schemaInvariants.ts) — чистая `findUniquenessViolations` + `checkSchemaInvariants`: нарушение уникальности `[platform, externalUserId]`/`[externalAccountId, externalContentId]`, уже enforced Postgres `@@unique` — defense in depth, системная проверка (не per-user), отдельное поле `schemaInvariantViolations` в `GET /api/data-quality`, не внутри `accounts[]`. **Честная оговорка**: обе уникальности физически не могут быть нарушены на живой БД, пока держится констрейнт — ветка "находит нарушение" проверена юнит-тестом на синтетических данных, реальный дубликат создать нельзя (подтверждено отдельным тестом — попытка вставки отклоняется Postgres). 10 юнит-тестов + 4 интеграционных (2 новых файла/дополнения). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (61/63 — те же 2 заранее известных live-сбоя), `npm run test:e2e` (12/12) — без регрессий. **Phase 5 — Data Quality полностью завершена** (Task 5.1–5.3).
- **Task 6.1: первая детерминированная аналитика** (`42_IMPLEMENTATION_ROADMAP.md` §31 PHASE 6/§32 «AI не должен считать всё»/§33 ANALYTICS_OUTPUT), первый шаг Phase 6 и первая реальная реализация ANALYSIS layer (`src/analysis/`, до этого — только README-заготовка). [`src/analysis/metricsAnalytics.ts`](src/analysis/metricsAnalytics.ts) — чистые `summarizeMetric`/`computeMetricsAnalytics`: сумма/среднее по последнему известному снимку метрики на публикацию (snapshot-семантика, тот же принцип, что Task 5.2) за период, тренд (`up`/`down`/`stable`/`insufficient_data`) — деление периода пополам хронологически, относительный порог 10%. [`src/analysis/accountAnalytics.ts`](src/analysis/accountAnalytics.ts) — `getUserAnalytics(userId, period)`, тот же паттерн, что `getDataQualityStatus`. **Находка**: `22_DATA_MODEL.md` §10 называет 4 основных показателя (`reach`/`likes`/`saves`/`followers_gained`), но `followers_gained` нигде не собирается ingestion'ом — добавлять сейчас означало бы новый вызов Instagram API, против формулировки задачи. `CORE_METRICS` ограничен тремя реально существующими метриками (`reach`/`likes`/`saved`), зафиксировано в [`DECISIONS.md` D-0018](DECISIONS.md). `performanceMetricRepository.findRowsByExternalAccountId` дополнен `metricType`. [`GET /api/analytics?days=N`](src/app/api/analytics/route.ts) (session-protected, по умолчанию 30 дней). 12 юнит-тестов + 2 интеграционных (масштаб 25 публикаций, тренд `up` подтверждён на реальной БД). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (75/77 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Task 6.2: personal baseline + сравнение + confidence** (`22_DATA_MODEL.md` §13 PERSONAL_BASELINE/§14 BASELINE_VERSION, `25_DATABASE_SCHEMA.md` §16 BASELINES). [`src/analysis/personalBaseline.ts`](src/analysis/personalBaseline.ts) — `computeBaseline`: среднее по последнему известному значению метрики на публикацию за ВСЮ историю (не ограничено периодом Task 6.1). `confidenceFromSampleSize`: `<5` → `low`, `5–14` → `medium`, `15+` → `high`. `compareToBaseline`: `above`/`below`/`at_baseline`/`insufficient_data`, итоговый confidence — минимум из confidence baseline и confidence текущего периода (не только baseline — надёжный baseline не спасает сравнение с периодом из 2 постов). **Scope сознательно сужен**: `25_DATABASE_SCHEMA.md` §16 описывает персистентную сущность со `scope` (global/format/topic/goal) и §17 требует историю версий — реализовано как вычисляемое на лету значение, только `scope: global`, ничего не персистится (тот же принцип, что весь Task 6.1), зафиксировано в [`DECISIONS.md` D-0019](DECISIONS.md). `metricRows.ts` — общие хелперы, вынесены из `metricsAnalytics.ts`, чтобы избежать циклического импорта с `personalBaseline.ts`. `MetricSummary` дополнен `baseline`/`comparisonToBaseline`/`confidence` — вывод теперь почти полностью соответствует `42_IMPLEMENTATION_ROADMAP.md` §33 ANALYTICS_OUTPUT. 16 новых юнит-тестов + обновления существующих + 1 новый интеграционный тест, буквально подтверждающий требование Olga «2–3 поста не должны давать уверенный вывод» (confidence `low` на 2 публикациях с реальной БД). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (93/95 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Внешний архитектурный review (ChatGPT, 13 августа 2026)** — подтвердил здоровое состояние проекта, без architectural drift. Два быстрых фикса: [`src/integrations/providers/instagram.ts`](src/integrations/providers/instagram.ts) — комментарий про «имя insights scope не проверено живым браузером» устарел (реальный OAuth round trip давно подтверждён, Task 3.2/3.4, `INSTAGRAM_API_REVIEW.md` §8 gap 2 закрыт), обновлён на актуальное состояние; `DECISIONS.md` — счётчик «46 документов» (3 места) исправлен на 45 (`First.md` был только в Project Knowledge, не в `/docs`, удалён до начала работы, никогда не входил в реальный аудит). Два forward-looking флажка, не блокируют текущую работу: [`DECISIONS.md` D-0020](DECISIONS.md) — тренд в `metricsAnalytics.ts` делит публикации по количеству, не по календарному времени, пересмотреть перед Phase 8; [`DECISIONS.md` D-0021](DECISIONS.md) — `getUserAnalytics`/`getDataQualityStatus` делают по одному запросу на внешний аккаунт, пересмотреть при реальном multi-account. Остальное из review — уже известные, отслеженные пункты (D-0010 encryption at rest, branch protection, `Content` без настоящего `@relation`), новых действий не потребовалось.
- **Task 7.1: первое использование `Memory`** (`42_IMPLEMENTATION_ROADMAP.md` §35 PHASE 7/§39 PHASE 7 COMPLETION), первый шаг Phase 6→7. [`src/knowledge/analyticsMemory.ts`](src/knowledge/analyticsMemory.ts) — `captureAnalyticsMemory(userId, period)`: для каждой метрики из `getUserAnalytics` (Task 6.1/6.2) с осмысленным сравнением (не `insufficient_data`) создаёт `Memory` (`memoryType: 'fact'`, `source: 'analytics'`, `confidence` — числовая проекция `low`/`medium`/`high` → `0.2`/`0.5`/`0.8`). Snapshot-семантика — новые строки на каждый вызов, не upsert. `POST /api/knowledge/capture?days=N` (state-changing). **Находка**: `Memory` покрывает только 4 из 7 пунктов `42_IMPLEMENTATION_ROADMAP.md` §35 (schema/timestamps/source/confidence) — evidence references (`25_DATABASE_SCHEMA.md` §37 MEMORY_EVIDENCE — другой документ, не путать с §37 в `42_IMPLEMENTATION_ROADMAP.md`, там это HISTORICAL DATA), outcome, relevance не реализованы (нет join-таблицы/полей), зафиксировано в [`DECISIONS.md` D-0022](DECISIONS.md) как честное частичное покрытие, не тихий пропуск. 5 новых тестов (2 юнит на чистом форматировании + 3 интеграционных: пустой аккаунт → 0 записей, масштаб 20 публикаций → 3 записи с верными полями, повторный вызов → новые строки). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (98/100 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Task 7.2: Pattern Detection, первый шаг** (`26_DATA_PIPELINE.md` §28 PATTERN_DETECTION/§29 POSITIVE_PATTERN/§30 NEGATIVE_PATTERN/§31 PATTERN_CONFIDENCE). [`src/knowledge/patternDetection.ts`](src/knowledge/patternDetection.ts) — `detectMetricPattern`: гранулярность на уровне ОТДЕЛЬНОЙ публикации (не периода — прямое следствие §29 «один пост не создаёт закономерность»), сравнение с personal baseline (Task 6.2) по тому же порогу 10%, направление при консистентности ≥60% и минимум 5 публикациях. `detectPatterns(userId)` — **upsert** по `(userId, patternType)`, не snapshot: единственная эволюционирующая запись на закономерность (`22_DATA_MODEL.md` §18 PATTERN_DECAY — «старая информация не удаляется» внутри одной записи). `POST /api/knowledge/patterns`. **Scope сознательно сужен**: только 2 из 5 видов закономерностей §28 (частотная, не комбинации признаков/изменения поведения); жизненный цикл `status` (§17–19) не реализован — остаётся Prisma-дефолтом `hypothesis`; формальные evidence references (§16 PATTERN_EVIDENCE) — текстовый суррогат в `description`, тот же класс пробела, что `Memory`/D-0022. Зафиксировано в [`DECISIONS.md` D-0023](DECISIONS.md). **Найденный и исправленный баг**: `lastConfirmedAt` изначально не устанавливался при первом создании `Pattern` — только при повторном `update`; интеграционный тест поймал `null` там, где ожидалась дата. 10 новых тестов (7 юнит + 3 интеграционных на реальной БД, включая явную проверку upsert — повторный вызов обновляет ту же запись, не создаёт вторую). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (108/110 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Task 7.3: единый ответ «что мы знаем и почему»** (`42_IMPLEMENTATION_ROADMAP.md` §39 PHASE 7 COMPLETION). **Оценка перед реализацией**: Task 7.1/7.2 создали `Memory`/`Pattern`, но обе сущности были доступны только за `POST`-триггерами, возвращающими счётчики (created/skipped, detected/skipped), не сами записи — не было способа реально спросить систему «что ты знаешь» через приложение, буквальный критерий §39 не выполнялся, хотя данные уже существовали. [`src/knowledge/userKnowledge.ts`](src/knowledge/userKnowledge.ts) — `getUserKnowledge(userId)`: объединяет `Memory` (только `status: 'active'`, свежие первыми) и `Pattern` (без `inactive`, увереннее — первыми). [`GET /api/knowledge`](src/app/api/knowledge/route.ts) — первый read-only доступ к обеим сущностям разом. «Почему» отвечается уже существующими `content`/`description` — новых evidence references не добавлено (тот же пробел, что D-0022/D-0023, не устранён, просто не мешает ответить на §39 в его буквальной формулировке). 3 новых интеграционных теста. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (111/113 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий. **Phase 7 — Knowledge Layer полностью завершена** (Task 7.1–7.3).
- **Task 8.1: CANDIDATE_GENERATOR + CANDIDATE_SCORER + RANKING_ENGINE, первый шаг** (`23_SYSTEM_ARCHITECTURE.md` §25–27, `21_DECISION_LOGIC.md` §9/§39–41), первая реализация DECISION layer (`src/decision/`, до этого — только README-заготовка). [`src/decision/candidateGenerator.ts`](src/decision/candidateGenerator.ts) — кандидаты из реально существующих `Content.contentType`, не захардкожены. [`src/decision/candidateScorer.ts`](src/decision/candidateScorer.ts) — для каждой метрики отдельно среднее конкретного кандидата (формата) сравнивается с личной нормой (Task 6.2, `scope: global`, не меняется), тем же порогом 10%; Pattern (Task 7.2) — контекстная пометка в `label`, никогда не влияет на сортировку (глобален по метрике, не по формату — не может честно объяснять разницу между кандидатами). **Реализованы 3 из 9 документированных факторов CANDIDATE_SCORER** (historical performance/сравнение с нормой/confidence) — goal fit/freshness/repetition/risk/opportunity не посчитаны, нет данных, честно не выданы за реализованные. Ranking — раздельно по каждой метрике, не единый список across all metrics (без goal fit — придуманный вес). Вывод — качественная метка («above baseline, medium confidence»), не число (§40 NO FALSE PRECISION). [`GET /api/decision/candidates`](src/app/api/decision/candidates/route.ts) — read-only, `Recommendation` не заполняется. **Уточнение цитаты**: Olga сослалась на «§36» для reasons/context/decision_version — оказалось `25_DATABASE_SCHEMA.md` §26/§28/§30 (§36 в этом документе — MEMORY, уже обсуждалось в D-0022/D-0023); похоже на перенос номера из соседнего обсуждения, содержательный вывод не изменился, см. [`DECISIONS.md` D-0024](DECISIONS.md). 13 новых тестов (10 юнит + 3 интеграционных на реальной БД, включая явную проверку, что `Recommendation` остаётся пустой). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (124/126 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Task 8.2: GOAL_FIRST/GOAL_PRIORITY** (`21_DECISION_LOGIC.md` §6–8). [`src/decision/goalFit.ts`](src/decision/goalFit.ts) — `determineGoalFit`: сортирует активные цели по приоритету (меньшее число — выше важность, первое реальное использование `Goal.priority`), сопоставляет `goalType` с трекаемой метрикой через явную таблицу (`saves`↔`saved` — то же понятие, разное словоупотребление документа/Instagram API, D-0018), выбирает первую (по приоритету) цель с трекаемой метрикой как первичную. **Выбор, не смешивание** — прямое продолжение D-0024. Честные состояния: `no_goals_defined` (целей нет), `no_tracked_goals` (например только `followers` — не собирается, D-0018) — per-metric рейтинги (Task 8.1) всё равно возвращаются целиком, не прячутся. Fallthrough на трекаемую цель при более приоритетной, но нетрекаемой. `getRankedCandidates` заменена на `getDecisionCandidates(userId)` — `{ goalFit, rankings, primaryRanking }`. [`GET /api/decision/candidates`](src/app/api/decision/candidates/route.ts) дополнен. 13 новых тестов (9 юнит + 4 интеграционных, включая fallthrough и `no_tracked_goals` на реальной БД). `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (136/138 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Task 8.3: персистентность решения** (`25_DATABASE_SCHEMA.md` §26 RECOMMENDATIONS/§28 RECOMMENDATION_REASONS/§30 RECOMMENDATION_CONTEXT — не §27/§29, Olga явно сузила scope этим шагом). Новая модель `RecommendationReason` (§28 даёт список колонок — отдельная таблица) + `Recommendation.context` (`Json?`, §30 — единственный снимок на recommendation, не список строк, поэтому JSON, не таблица); миграция `20260815001732_recommendation_reasons_and_context`, переприменение полной истории (6 миграций) проверено на одноразовой БД. [`src/decision/recommendationPersistence.ts`](src/decision/recommendationPersistence.ts) — `createRecommendation(userId)`: пишет `Recommendation` только при `goalFit.reason === 'goal_matched'` (обязательный `goalId` — не выбирается «по умолчанию») и реальном top-кандидате (не `insufficient_data`); честные `{ created: false, reason }` во всех остальных случаях. `DECISION_VERSION = 'decision-v1'` — первое реальное значение `decisionVersion`. Reasons: `goal` (confidence не заполняется — детерминированный выбор) + `baseline` (переиспользует `CONFIDENCE_SCORE`, Task 7.1) + `pattern` (только при совпадении направления, реальный `Pattern.confidence`) — `weight` нигде не заполняется (§40 NO FALSE PRECISION, нет расчёта). `context` — снимок с явным `skeletonVersion: null` (не имеет референта в кодовой базе). Append-only, как `Memory` (не upsert, как `Pattern`). `candidateScorer.ts`/`goalFit.ts` дополнены структурными полями (`matchesPattern`/`pattern`/`matchedGoal.id`) для персистентности, без изменения поведения ranking/goal fit. `POST`/`GET /api/decision/recommendations` — запись и чтение уже сохранённых («найти позже»). 6 новых интеграционных тестов (нет целей/нетрекаемая цель/нет контента → не создано; реальная запись с reasons+context; отдельная pattern-причина; append-only на повторном вызове). `DECISIONS.md` D-0026. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован), `npm test` (142/144 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Task 8.4: freshness + repetition** (`21_DECISION_LOGIC.md` §12 FRESHNESS_WEIGHT/§13 RECENCY_OVERRIDE/§14 REPETITION_CONTROL). Выбор Olga из списка кандидатов после Task 8.3 — не расширение citation scope (§27/§29), а факторы, напрямую улучшающие качество уже работающего цикла. [`src/decision/candidateGenerator.ts`](src/decision/candidateGenerator.ts) — `mostRecentContentType(content)`: последний опубликованный формат по всем форматам разом. [`src/decision/candidateScorer.ts`](src/decision/candidateScorer.ts) — `CandidateResult.freshness` (буквальные пороги §12 от самой свежей публикации кандидата, `null` при отсутствии данных, не путать со `'stale'`) и `CandidateResult.repetitionNote` (только на топ-кандидате, только при совпадении с `mostRecentContentType` и наличии конкурентной альтернативы — §13/§14 реализованы как один механизм, один и тот же пример в обоих разделах документа). **Ни один сигнал не меняет порядок `ranking`** — качественная пометка, не скрытый вес (§40, прямое продолжение D-0024/D-0025). FRESHNESS_WEIGHT как decay-переоценка самого `computeBaseline` (Task 6.2/D-0019) сознательно НЕ реализован — затронул бы ANALYSIS layer, отдельная будущая задача. [`src/decision/recommendationPersistence.ts`](src/decision/recommendationPersistence.ts) — новая `RecommendationReason(reasonType: 'freshness')` (реальный пункт словаря §28); `repetitionNote` НЕ становится причиной — «repetition» отсутствует в словаре, который даёт сам документ. Risk/opportunity — явно вне scope, нужен «experimental candidate»/hypothesis status, которого нет ни в схеме, ни в текущем CANDIDATE_GENERATOR (`Experiment`-модель Task 1.1 по-прежнему не используется). 13 новых тестов (4 юнит на `mostRecentContentType` + 9 юнит на freshness/repetition в `candidateScorer`) + обновление `recommendation-persistence.smoke.test.ts` (реальный сценарий теперь содержит `freshness`-причину). `DECISIONS.md` D-0027. `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (155/157 — те же 2 заранее известных live-сбоя, не регрессия), `npm run test:e2e` (12/12) — без регрессий.
- **Phase 8 — Recommendation Engine закрыта** (решение Olga, 15 августа 2026) на объёме Task 8.1–8.4 — критерий `42_IMPLEMENTATION_ROADMAP.md` §44 PHASE 8 COMPLETION («для тестового пользователя система способна создать: data → insight → recommendation, без необходимости AI») проверен и подтверждён напрямую: реальные `Content`/`PerformanceMetric` → `computeBaseline`/`Pattern` как insight → персистентный `Recommendation`+`RecommendationReason` как recommendation, полностью детерминированно (`recommendation-persistence.smoke.test.ts` доказывает всю цепочку на реальной БД), `AI_SERVICE`/`AIProvider` не импортируются нигде в `src/decision/`. **Найденный при проверке честный пробел**: `Memory` (Task 7.1) физически не читается Decision Engine (`recommendationCandidates.ts` использует `patternRepository`, но не `memoryRepository`) — единственная часть §41 RECOMMENDATION_INPUT «knowledge», не дошедшая до реального pipeline'а; не блокирует буквальный критерий §44 (Baseline/Pattern — тоже legitimate insight), зафиксировано как задел на будущее. §41 (recent/historical context, previous decisions), §42 RECOMMENDATION_OUTPUT (expected effect/risks/формальные source references), `RECOMMENDATION_CANDIDATES`/`RECOMMENDATION_EVIDENCE` (§27/§29, D-0026), risk/opportunity и FRESHNESS_WEIGHT-decay (D-0027), числовое смешивание метрик (D-0025) — документированные, но сознательно не реализованные части темы, не входят в критерий §44, см. `TASKS.md`
- **Task 9.1: AI_SERVICE → AIProvider, decision explanation** (`21_DECISION_LOGIC.md` §50 DECISION EXPLANATION — не `29_AI_LAYER.md`, уточнение цитаты; `29_AI_LAYER.md` §11/§18-20/§24/§26/§34-35/§67-68/§73-76). Первое реальное использование `AIService`/`AIProvider` (Task 0.2, ни разу не вызывались ни одним domain-кодом) — привязано сразу к одной полезной задаче, не к инфраструктуре саму по себе (§73). [`src/ai/decisionExplanationPrompt.ts`](src/ai/decisionExplanationPrompt.ts) — `buildExplanationPrompt`: собирает промпт из реальных `RecommendationReason` (Task 8.3) и качественной confidence-метки из `context.ranking` (не голое число — §35, риск псевдо-статистики), явный запрет придумывать факты/переоценивать уверенность. [`src/ai/decisionExplanationValidation.ts`](src/ai/decisionExplanationValidation.ts) — `parseExplanation`: 4 обязательных строковых поля, валидация до персистентности (§11/§26). [`src/ai/decisionExplanation.ts`](src/ai/decisionExplanation.ts) — `generateDecisionExplanation(recommendationId, userId, generate)`: только читает `Recommendation`, пишет исключительно в новую `AiRun` (§67/§68), при ЛЮБОМ исходе — не только успехе (§24/§76 воспроизводимость). `generate` — DI-параметр (не мок модуля) для детерминированных тестов без реальных вызовов Anthropic API. `AIProvider`/`AIService`/`providers/anthropic.ts` (Task 0.2) не изменены — контракт подошёл без правок. `POST /api/decision/recommendations/[id]/explain`. 22 новых теста (6 юнит на промпт + 11 юнит на валидацию + 5 интеграционных с фиктивным `generate`, включая явную проверку, что `Recommendation` не меняется — §67/§68) + 1 живой smoke-тест (`describe.skipIf(!ANTHROPIC_API_KEY)`, пропущен в этом окружении — ключ не задан локально). `DECISIONS.md` D-0028. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован) — без ошибок; `npm test` — 177/180 (те же 2 заранее известных live-сбоя, не регрессия; 1 живой AI-тест пропущен), `npm run test:e2e` (12/12) — без регрессий.
- **Task 9.2: экран отображения AI-объяснения рекомендации** (`21_DECISION_LOGIC.md` §50). [`src/app/recommendations/[id]/page.tsx`](src/app/recommendations/%5Bid%5D/page.tsx) — server component, тот же UI-паттерн, что `/integrations` (Task 3.4): читает `Recommendation` напрямую через `recommendationRepository.findByIdWithReasons`, не через свой же API; неаутентифицированный → «Войдите»; чужая/несуществующая рекомендация → одно и то же «Рекомендация не найдена.» (тот же принцип, что Task 2.3/Task 9.1). [`ExplainButton.tsx`](src/app/recommendations/%5Bid%5D/ExplainButton.tsx) — client component, тот же паттерн, что `SyncButton`/`DisconnectButton` (Task 3.4/4.1): вызывает существующий `POST .../explain`, рендерит 4 секции по структуре §50 при успехе, честное сообщение под каждый нестандартный исход при неуспехе (не общее «что-то пошло не так»). [`explainStatusMessages.ts`](src/app/recommendations/%5Bid%5D/explainStatusMessages.ts) — чистая, юнит-тестируемая функция форматирования, вынесена из React-компонента. Кнопка НЕ нажимается в e2e (реальный внешний AI-вызов — тот же принцип, что `SyncButton` никогда не нажимается в `integrations-instagram.spec.ts`); честное поведение при отсутствующем `ANTHROPIC_API_KEY` вместо этого **подтверждено вручную в реальном браузере**: зарегистрирован тестовый пользователь, реально засеяна `Recommendation` (через `createRecommendation`), кнопка нажата — на экране появилось «AI-провайдер сейчас недоступен — скорее всего, не настроен ключ API. Объяснение не сгенерировано.», ровно как задумано. 7 новых тестов (3 юнит на форматирование + 4 e2e через реальный браузер/БД). `.claude/launch.json` — dev-сервер конфигурация для браузерной проверки, впервые появилась в этой задаче. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новая страница зарегистрирована) — без ошибок; `npm test` — 180/183 (3 новых юнит-теста зелёные; 1 живой AI-тест пропущен без ключа, те же два заранее известных live-сбоя Meta API — не регрессия); `npm run test:e2e` — 16/16 (4 новых, без регрессий); dev-БД `olga` — 1 user/1 externalAccount (реальный аккаунт Olga), 0 тестовых и вручную созданных строк после прогонов и ручной проверки.
- **Task 9.3: `ContentKnowledge` — первые записи внешней базы приёмов создания контента** (прямое требование Olga, «вторая AI-задача», `29_AI_LAYER.md` §33). [`prisma/schema.prisma`](prisma/schema.prisma) — новая модель `ContentKnowledge` (id/category/title/content/source/sourceSection/status), сознательно НЕ привязана к `userId` — внешнее ремесленное знание, не производное от данных конкретного пользователя (в отличие от `Memory`/`Pattern`). `category` — `String`, не enum (та же логика, что `goalType`/`contentType`). [`contentKnowledgeRepository.ts`](src/data/repositories/contentKnowledgeRepository.ts) — `findActive(category?)`, то, что AI при генерации текста будет читать вместо захардкоженного текста. [`prisma/contentKnowledgeSeedData.ts`](prisma/contentKnowledgeSeedData.ts) (чистые данные) + [`prisma/seedContentKnowledge.ts`](prisma/seedContentKnowledge.ts) (идемпотентный раннер, `npm run seed:content-knowledge`, `tsx` добавлен в devDependencies) — «дешёвое пополнение базы»: добавить запись в массив + одна команда, без миграции. 15 первых записей: 1 фундамент стратегии + 7 правил заголовков (всё из «Полное руководство дополнительно.pdf») + 6 хуков по дням недели + 1 правило про пятницу (из Раздела 1 «Полное руководство.pdf», 130 страниц — загружен только этот раздел, сознательно частичная загрузка, честно зафиксирована). Сам AI-код чтения `ContentKnowledge` при генерации — не реализован этим шагом, только читаемый механизм (§73 КРИТИЧЕСКОЕ ПРАВИЛО, тот же принцип, что D-0028). `DECISIONS.md` D-0030. 10 новых тестов (6 юнит на чистых данных без БД + 4 интеграционных на синтетических данных против реальной БД). Seed реально применён на dev-БД, идемпотентность подтверждена повторным запуском (0 создано/15 пропущено). **Побочная находка**: полный `npm test` в ходе этой задачи впервые за 14 прогонов показал оба live Instagram-теста зелёными на реальных данных — блокировка Meta разрешилась сама, см. «Заблокировано». `npx tsc --noEmit`, `npm run lint`, `npm run build` — без ошибок; `npm test` — 192/193 (10 новых тестов зелёные, 1 живой AI-тест пропущен без ключа — Instagram live-тесты больше не в числе известных сбоев); `npm run test:e2e` — 16/16; миграция переприменена на одноразовой БД; dev-БД `olga` после прогонов — 1 user/1 externalAccount/15 `content_knowledge` (реальные, оставлены намеренно), 0 прочих тестовых строк.
- **Task 9.4: генерация текста на основе `ContentKnowledge`** (зеркально Task 9.1, прямой приоритет Olga перед дальнейшей загрузкой руководства). `prisma/schema.prisma` — `AiRun` обобщена: `recommendationId` стал опциональным (§24 AI RUN сам по себе общий, ничего Recommendation-специфичного не требует — сужение было решением Task 9.1/D-0028), добавлены `userId` (иначе AI-run для генерации текста был бы анонимным) и `inputContext` (буквальный пункт §24 "input context reference"). Миграция `20260815112339_ai_run_generalize`. [`contentSuggestionPrompt.ts`](src/ai/contentSuggestionPrompt.ts) — генерирует хук/заголовок-варианты по теме, используя ТОЛЬКО реально загруженные (Task 9.3) `ContentKnowledge` (по умолчанию `hook_template`+`headline_rule`), прямой запрет придумывать приёмы сверх переданных. [`contentSuggestionValidation.ts`](src/ai/contentSuggestionValidation.ts) — структурная валидация до персистентности. [`contentSuggestion.ts`](src/ai/contentSuggestion.ts) — `generateContentSuggestions(topic, userId, category?, generate)`: честные состояния `invalid_topic`/`no_knowledge_available` без AI-вызова и без `AiRun`. `POST /api/ai/content-suggestions`. `DECISIONS.md` D-0031. 17 новых тестов (12 юнит без БД + 5 интеграционных с фиктивным `generate`) + живой skip-if-no-credentials тест. **Подтверждено вживую через реально запущенный dev-сервер**: реальная регистрация+сессия (`/api/register`+`/api/auth/callback/credentials`), реальный `curl` на эндпоинт — `invalid_topic`/`no_knowledge_available`/401 unauthorized все честно подтверждены; вызов с реальным `hook_template` дошёл до настоящего `AIService.generate`, вернул честный `provider_unavailable`, реальная `AiRun`-запись в БД с верными `user_id`/`input_context`. Честная оговорка: сама генерация текста с реальным содержимым 15 записей не может быть продемонстрирована без `ANTHROPIC_API_KEY`, не настроенного в этом окружении — механизм подтверждён живьём вплоть до самого AI-вызова. `npx tsc --noEmit`, `npm run lint`, `npm run build` (новый route зарегистрирован) — без ошибок; `npm test` — 210/212 (17 новых тестов зелёные, 2 живых AI-теста пропущены без ключа); `npm run test:e2e` — 16/16; миграция переприменена на одноразовой БД; dev-БД `olga` после прогонов и ручной проверки — 1 user/1 externalAccount/15 content_knowledge, 0 прочих строк.
- **Task 9.5: экран интерфейса для генерации текста** (зеркально Task 9.2, прямое требование Olga — увидеть готовую функцию в интерфейсе, не расширение базы знаний). [`src/app/content-suggestions/page.tsx`](src/app/content-suggestions/page.tsx) — server component, тот же UI-паттерн, что `/integrations`/`/recommendations/[id]`; auth-gate, без «не найдено» состояния (генерация не привязана к существующему ресурсу по id). [`ContentSuggestionForm.tsx`](src/app/content-suggestions/ContentSuggestionForm.tsx) — client component: поле «Тема поста» + кнопка «Сгенерировать варианты», вызывает существующий `POST /api/ai/content-suggestions` без category-селектора в UI (бэкенд по умолчанию использует `hook_template`+`headline_rule` — единственные категории с переиспользуемыми текстовыми шаблонами; остальные категории в UI создали бы ложидание результата, которого не будет). [`contentSuggestionStatusMessages.ts`](src/app/content-suggestions/contentSuggestionStatusMessages.ts) — чистая функция форматирования, зеркально `explainStatusMessages.ts` (Task 9.2), 6 исходов. Кнопка НЕ нажимается в e2e (реальный внешний AI-вызов — тот же принцип, что «Синхронизировать»/«Сгенерировать объяснение»); честное поведение вместо этого **подтверждено вручную в реальном браузере**: зарегистрирован тестовый пользователь, реальная отправка формы с темой показала «AI-провайдер сейчас недоступен — скорее всего, не настроен ключ API. Варианты не сгенерированы.», пустая тема — «Тема не может быть пустой — опишите, о чём пост.». 5 новых тестов (3 юнит на форматирование + 2 e2e через реальный браузер). `npx tsc --noEmit`, `npm run lint`, `npm run build` (новая страница `/content-suggestions` зарегистрирована) — без ошибок; `npm test` — 213/215 (3 новых юнит-теста зелёные; 2 живых AI-теста пропущены без ключа, не регрессия); один нестабильный прогон `instagram-sync-live.smoke.test.ts` при полной параллельной нагрузке всего набора (стабильно проходит в изоляции с реальными данными — 25 публикаций/175 метрик, не связано с этой задачей); `npm run test:e2e` — 18/18 (2 новых, без регрессий); dev-БД `olga` после прогонов и ручной проверки — 1 user/1 externalAccount/15 content_knowledge, 0 прочих строк.

## В работе
Phase 3, Phase 4, Phase 5, Phase 6, Phase 7 и Phase 8 полностью завершены (Task 3.0–3.4, Task 4.1–4.3, Task 5.1–5.3, Task 6.1–6.2, Task 7.1–7.3, Task 8.1–8.4). Phase 9 — AI Layer (`29_AI_LAYER.md`, `42_IMPLEMENTATION_ROADMAP.md` §45–51) начата — Task 9.1 (decision explanation, первое реальное использование `AIService`, `DECISIONS.md` D-0028), Task 9.2 (экран `/recommendations/[id]`, показывает результат Task 9.1), Task 9.3 (`ContentKnowledge`, внешняя база приёмов создания контента, первые 15 записей, `DECISIONS.md` D-0030) и Task 9.4 (генерация текста на основе `ContentKnowledge`, зеркально Task 9.1, `AiRun` обобщена, `DECISIONS.md` D-0031) завершены. Следующие шаги Phase 9 не расписаны заранее (по обычному принципу этого проекта) — кандидаты: дальнейшее пополнение `ContentKnowledge` (Раздел 2/3 «Полное руководство.pdf», 130 страниц — загружен только Раздел 1, см. D-0030) — по плану Olga, теперь что генерация реально проверена; UI-экран для генерации текста (Task 9.4 — без UI, зеркально Task 9.1); отдельный Prompt Registry (§21, пока не нужен — один промпт на одну задачу), отдельный AI_CONTEXT builder модуль (§19), другие AI-задачи (content analysis §31/pattern interpretation §32), экран со списком всех рекомендаций пользователя (`GET /api/decision/recommendations` уже есть с Task 8.3, UI для списка — не сделан). Открытые пункты, унаследованные из Phase 7/8, естественно продолжатся в контексте Phase 9 (Olga, 15 августа 2026): risk/opportunity (§26 RISK/§27 OPPORTUNITY — нужен «experimental candidate»/hypothesis status, которого нет ни в схеме, ни в текущем CANDIDATE_GENERATOR; `Experiment`-модель, Task 1.1, по-прежнему не используется ни разу), см. `DECISIONS.md` D-0027; FRESHNESS_WEIGHT как decay-переоценка самого `computeBaseline` (Task 6.2/D-0019, затронуло бы ANALYSIS layer) — сознательно отложен до одного из трёх условий (реальная история за несколько месяцев / подход к Phase 10 / найден конкретный случай неверного вывода от плоского среднего), см. `DECISIONS.md` D-0029, не начинать до наступления условия; `RECOMMENDATION_CANDIDATES` (§27) и `RECOMMENDATION_EVIDENCE` (§29 из `25_DATABASE_SCHEMA.md`) — Olga сузила scope Task 8.3 явно только до §26/§28/§30, см. `DECISIONS.md` D-0026; `Memory` не читается Decision Engine — найдено при закрытии Phase 8, см. выше; возможное числовое смешивание метрик по весам приоритета, см. `DECISIONS.md` D-0025 — только если станет практически необходимо, новый риск false precision; `RecommendationReason.weight` остаётся `null`, пока нет реального расчёта значимости причины. §36–38 (weighting/historical decay/user memory), жизненный цикл Pattern (§17–19), комбинации признаков и градация отрицательных результатов (§30 из `26_DATA_PIPELINE.md`), формальные evidence references (`MemoryEvidence`/`PatternEvidence`) — документированные, но не реализованные части Phase 7, см. `DECISIONS.md` D-0022/D-0023. Вне Phase 7/8: encryption at rest для `ExternalAccount.accessToken` (`DECISIONS.md` D-0010) — по-прежнему открыт; персистентная `Baseline`-сущность с историей версий и non-global scope (`25_DATABASE_SCHEMA.md` §16–17), см. `DECISIONS.md` D-0019; сбор `followers_gained` (Ingestion) — сделал бы goal `followers` трекаемым, см. `DECISIONS.md` D-0018/D-0025; календарная семантика тренда — перед активным использованием в Phase 8, см. D-0020; батчинг multi-account запросов — при реальном multi-account, см. D-0021; incremental/change detection (`26_DATA_PIPELINE.md` §13–14) и очередь/scheduled sync — сознательно отложены. (Backlog, не блокирует): реальные staging/production ресурсы (Vercel, Neon, Upstash, R2) и branch protection для `main` — см. Backlog в `TASKS.md`, D-0007; расширение схемы (Hypothesis/Action/Outcome) — по мере необходимости, см. `DECISIONS.md` D-0008; rate limiting на auth endpoints — заблокировано отсутствием Redis-инфраструктуры (D-0007), см. `TASKS.md` Task 2.2; account recovery/role model — вне MVP-scope, см. закрытие Phase 2 выше.

## Заблокировано
Нет открытых блокеров.

**Разрешено 15 августа 2026: живая проверка Task 9.1/9.4 через реальный Anthropic API.** `ANTHROPIC_API_KEY` добавлен в `.env` 15 августа 2026 — изначально оба live-теста реально выполнялись (ключ подхватывался верно), но падали с настоящим `400 invalid_request_error` от Anthropic — «Your credit balance is too low to access the Anthropic API» (не проблема кода/ключа, подтверждено прямым запросом к API в обход приложения). После пополнения кредитов Olga — оба `npm run test:live-ai` теста реально прошли с настоящим сгенерированным текстом (см. `TASKS.md`/отчёт по итогам).

**Разрешено 15 августа 2026 (Task 9.3): «Instagram Graph API отвечает `"API access blocked."` на реальные запросы».** Было заблокировано с 13 августа 2026 (обнаружено при Task 5.1) — четырнадцать подряд полных прогонов `npm test` (Task 5.1–9.2) с одной и той же ошибкой от `tests/integration/instagram-live.smoke.test.ts` (Task 3.2) и `tests/integration/instagram-sync-live.smoke.test.ts` (Task 4.1/4.3), не устранялось никаким изменением кода проекта — подтверждалось RED-статусом, требующим проверки Meta App Dashboard самой Olga. Разрешилось при полном `npm test` в ходе Task 9.3 (несвязанной задачи) — оба теста прошли реально, с настоящими данными аккаунта Olga (25 публикаций, 175 метрик, ровно как в последнем успешном прогоне на момент Task 4.3). Код в области Instagram-интеграции не менялся ни в одной из задач Phase 9 — блокировка была исключительно внешней (на стороне Meta), разрешилась сама.

## Известные проблемы / отложено (не блокирует Phase 0)
- `06_RECOMMENDATION_ENGINE.md` считается superseded от `13_RECOMMENDATION_ENGINE.md`
- Три пары документов — кандидаты на консолидацию после MVP (см. TASKS.md, Backlog)
- `First.md` — пустой файл в Project Knowledge; можно удалить (Olga подтвердила), удаляется через панель файлов проекта, не через этот чат
- PDF-приложения — материалы бренд-войса, не архитектурная документация
- `npm audit`: 3 high severity — транзитивные `postcss`/`sharp` через Next.js 15. Фикс требует мажорного апгрейда до Next.js 16 (`npm audit fix --force`), это не выполнено автоматически — решение об апгрейде откладывается, не блокирует Phase 0 (`DECISIONS.md`, D-0005; подтверждено ChatGPT при review завершения Task 0.2)

## Текущее окружение
Node.js v24.19.0 / npm 11.17.0 установлены (Olga, вручную через официальный установщик). Репозиторий: `github.com/olgavladimirovna83-sketch/Project01` (`origin/main`, см. D-0006). PostgreSQL — Postgres.app, локально, порт 5432, база `olga` (по имени пользователя), trust auth, подключение через Unix socket. Зависимости установлены, dev-сервер, оба тестовых раннера (включая интеграционные тесты с реальной БД) и Prisma-миграции верифицированы локально (CI подтверждён зелёным, теперь с `postgres:16` service). Staging / production облачная инфраструктура сознательно отложена (D-0007). `ANTHROPIC_API_KEY` настроен в `.env` с 15 августа 2026 (Olga), с активными кредитами — живые AI-тесты доступны через `npm run test:live-ai`.

**Тестовые команды (15 августа 2026, правило Olga).** `npm test` — обычный прогон, НЕ включает живые тесты против платных/внешних API (Anthropic/Instagram), даже если credentials присутствуют в `.env`. `npm run test:live-ai` — только живые Anthropic-тесты (Task 9.1/9.4), реальные деньги — запускать по явному запросу Olga или когда только что менялся AI-код. `npm run test:live-instagram` — только живые Instagram-тесты (Task 3.2/4.1), бесплатно, но реальный внешний сервис — та же логика "не по умолчанию". `npm run test:live` — все 4 сразу. См. `vitest.config.ts`/`vitest.live.config.ts`/`vitest.shared.ts`.

## Следующая рекомендованная задача
Task 9.12 завершена — Task 9.13 ещё не сформулирована (по обычному принципу — один шаг формулируется, когда до него доходит очередь). Блокеров нет. Кандидаты по Phase 9: «Полное руководство.pdf» (130 страниц) полностью загружено (Task 9.3/9.6–9.12) — дальнейшее пополнение `ContentKnowledge` возможно из новых источников, если Olga принесёт новые материалы; retrieval/ранжирование `ContentKnowledge` по релевантности теме станет заметнее нужным при 101 записи в 10 категориях (сейчас простой фильтр по category, см. D-0031) — естественный следующий технический кандидат, если Olga решит им заняться; отдельный Prompt Registry (§21 PROMPT LAYER), теперь есть два промпта (`decision-explanation-v1`/`content-suggestion-v1`) — если появится третий или версия сменится, пересмотреть; отдельный AI_CONTEXT builder модуль (§19), если контекст усложнится; другие AI-задачи из `29_AI_LAYER.md` (content analysis §31, pattern interpretation §32); retrieval/ранжирование `ContentKnowledge` по релевантности теме (сейчас — простой фильтр по category), см. `DECISIONS.md` D-0031; экран со списком всех рекомендаций пользователя (`/recommendations/[id]`, Task 9.2, показывает одну по id — списочный экран поверх `GET /api/decision/recommendations`, Task 8.3, не сделан). Открытые технические доработки Recommendation Engine (risk/opportunity §26/§27 — нужен «experimental candidate»/hypothesis status, D-0027; FRESHNESS_WEIGHT-decay для `computeBaseline` — сознательно отложен до одного из трёх условий возврата, см. `DECISIONS.md` D-0029, не начинать раньше; `RECOMMENDATION_CANDIDATES`/`RECOMMENDATION_EVIDENCE` §27/§29, D-0026; `Memory` не читается Decision Engine, найдено при закрытии Phase 8; возможное числовое смешивание метрик, D-0025) — по решению Olga, естественно продолжатся в контексте Phase 9, не как отдельные технические задачи прямо сейчас. Кандидаты вне Phase 8/9: жизненный цикл Pattern (§17–19), см. `DECISIONS.md` D-0023; формальные evidence references (`MemoryEvidence`/`PatternEvidence`), см. `DECISIONS.md` D-0022/D-0023; §36–38 Phase 7 (weighting/historical decay/user memory); персистентная `Baseline`-сущность с историей версий (`25_DATABASE_SCHEMA.md` §16–17), см. D-0019; сбор `followers_gained` (Ingestion), см. `DECISIONS.md` D-0018; календарная (не по количеству) семантика тренда, см. D-0020; батчинг multi-account запросов, см. D-0021; UI-экраны — ни один пока не реализован; персистентная сущность «sync run» — `DECISIONS.md` D-0016; incremental/change detection (`26_DATA_PIPELINE.md` §13–14); encryption at rest для `ExternalAccount.accessToken` (`DECISIONS.md` D-0010) по-прежнему открыт. Дополнительные сущности из `22_DATA_MODEL.md` §54 (Hypothesis, Action, Outcome) — по мере практической необходимости; обязательная сверка перед продолжением Phase 9, см. `DECISIONS.md` D-0008/D-0012/D-0013/D-0015/D-0016/D-0017/D-0018/D-0019/D-0020/D-0021/D-0022/D-0023/D-0024/D-0025/D-0026/D-0027/D-0028/D-0029/D-0030/D-0031/D-0032/D-0033.
